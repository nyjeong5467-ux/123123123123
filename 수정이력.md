# web-hq 수정 이력

디자인 작업 중 변경된 코드의 기록입니다. 각 항목에 **변경 전 코드**가 그대로 남아 있어, 해당 부분을 되돌려 붙이면 롤백됩니다. 문서화(인수인계·보고) 시 이 파일을 그대로 발췌해 사용할 수 있습니다.

표기 규칙: `[번호] 날짜 — 파일 — 요약` / 변경 이유 / 변경 전·후 코드 / 영향 범위

---

## [001] 2026-07-24 — `src/styles/home.css` — 홈 히어로 카드 높이를 우측 컬럼에 맞춤

**요청**: 홈 화면에서 "법정업무 운영 공지" 카드가 우측 산재 알림 카드보다 먼저 끝나 아래 빈 여백이 생김 → 히어로를 늘려 우측 컬럼과 바닥선을 맞추고 여백을 채움.

**방식**: CSS만 수정 (TSX 코드·백엔드 계약 파일 미변경)

- `.hm-top`의 `align-items: start` → `stretch` (히어로 셀이 우측 컬럼 높이까지 확장)
- `.hm-cyc`를 세로 flex 컨테이너로 전환, `.hm-gantt`가 남는 높이를 흡수
- 업무 행(`.hm-gline.hm-grow`)들이 남는 높이를 균등 분배 (`flex: 1; min-height: 44px`)

**변경 전** (롤백 시 이 코드로 되돌리기 — home.css 77~83행 부근):

```css
.hm-top {
  display: grid;
  grid-template-columns: 1fr 356px;
  gap: 18px;
  align-items: start;
  margin-top: 14px;
}
```

**변경 후**:

```css
.hm-top {
  display: grid;
  grid-template-columns: 1fr 356px;
  gap: 18px;
  align-items: stretch; /* 히어로를 우측 컬럼(공지+산재) 높이에 맞춤 */
  margin-top: 14px;
}
/* 히어로 카드가 셀 높이를 채우고, 남는 공간은 간트 업무 행들이 고르게 나눠 갖는다 */
.hm-cyc {
  display: flex;
  flex-direction: column;
}
.hm-cyc .hm-gantt {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.hm-cyc .hm-gantt .hm-gline.hm-grow {
  flex: 1;
  min-height: 44px;
}
```

**롤백 방법**: 변경 후 블록 전체를 지우고 변경 전 블록으로 교체 (추가된 `.hm-cyc` 3개 규칙 삭제 포함).

**영향 범위**: 홈(/) 화면의 상단 히어로만. 우측 알림 개수가 늘면 히어로도 함께 늘어남. 1280px 이하 반응형(단일 컬럼 전환)에는 영향 없음.

---

## [002] 2026-08-04 — 홈 상단에 "오늘의 할 일" 히어로 신설, 연간 사이클은 아래로 이동

**요청**: 홈 최상단(구 운영 공지 자리)에 오늘 방문할 학교와 할 업무가 보이게. 연간 법정업무 운영 공지(간트)는 삭제하지 않고 오늘의 할 일 아래에 유지.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/TodayHero.tsx` | **신규** — 오늘의 할 일 카드. Home의 todayItems(캘린더 계획+방문 실적 병합)를 받아 학교별 미완 업무를 4개 API(inspections·risk·compliance·musculo)로 병렬 조회, 칩으로 표시. 행 클릭 → 학교 상세 |
| `src/pages/Home.tsx` | 상단 그리드(hm-top) 좌측 셀: `<CycleHero …/>` → `<TodayHero …/>`. CycleHero는 hm-top 닫힌 직후 `<div className="hm-mt">`로 감싸 전체 폭 배치(코드·기능 그대로, 위치만 이동). import에 TodayHero 추가 |
| `src/styles/home.css` | `.hm-today`·`.hm-td-*` 스타일 블록 추가 (기존 규칙 수정 없음, 순수 추가) |
| `mock-server.mjs` | visit-plans 문서에 오늘 날짜 방문 계획 4건 시드 (화면 확인용 샘플) |

**업무 칩 도출 규칙** (TodayHero.deriveTasks):
- 이번 달 제출/서명된 점검 없음 → `안전점검 · 이번 달 미실시` (주황)
- 위험성평가 status ≠ completed → `위험성평가 진행 N건` (보라)
- 이행점검 draft 존재 → `이행점검 작성 중` (보라)
- 증상조사표 needs_review 합 > 0 → `증상조사표 검수 N건` (빨강)
- 모두 해당 없음 → `특이 업무 없음 · 정기 방문` (회색)

**변경 전** (롤백용 — Home.tsx hm-top 내부 원래 코드):

```tsx
{/* ===== 1. 연간 법정업무 운영 공지 히어로 + 공지사항 ===== */}
<div className="hm-top">
  <CycleHero
    ay={AY}
    monthLabels={AY_MONTHS.map((m) => m.label)}
    monthEn={MONTH_EN}
    curAyIdx={curAyIdx}
    inspSegs={inspSegs}
    inspNum={ /* …방문 수 표기 JSX (변경 없음 — 이동만 됨)… */ }
    nowlineLeft={nowlineLeft}
    doc={cycleDoc}
    onSave={saveCycleDoc}
  />
  {/* 공지사항 */}
```

**롤백 방법**:
1. Home.tsx: hm-top 안의 `<TodayHero …/>`를 위 변경 전 `<CycleHero …/>` 블록으로 되돌리고, 아래에 새로 추가된 `{/* ===== 1-2. 연간 법정업무 운영 공지 … ===== */}` `<div className="hm-mt"><CycleHero …/></div>` 블록과 TodayHero import 줄 삭제 (CycleHero JSX 내용은 동일하므로 그 블록을 그대로 hm-top 안으로 옮겨도 됨)
2. `src/components/TodayHero.tsx` 파일 삭제
3. home.css의 `/* ---- 오늘의 할 일 히어로 (TodayHero) ---- */` 블록 삭제

**검증**: `tsc --noEmit` 통과 · 목서버 연동 실제 렌더링 스크린샷 확인 (완료 2곳 초록 체크·예정 3곳 업무 칩·간트 전체 폭 정상)

**영향 범위**: 홈(/) 화면만. CycleHero 기능(편집·저장·기한 알림 연동)은 그대로 유지. [001]의 stretch 레이아웃은 이제 TodayHero 카드에 적용되어 우측 컬럼(공지+산재)과 바닥선을 맞춤.

---

## [003] 2026-08-04 — `src/pages/Home.tsx` — "학교 방문률" 섹션 제거

**요청**: 홈에서 학교 방문률 카드(도넛 + 월별 방문률 막대) 삭제.

**변경**: Home.tsx의 `{/* ===== 2. 학교 방문률 ===== */}` 주석부터 해당 `<div className="hm-card hm-mt">…</div>` 블록 전체(57행)를 삭제. JSX만 제거 — 방문률 계산 로직(monthVisited·curPct 등)은 오늘의 할 일·간트·기한 알림이 계속 사용하므로 유지. home.css의 `.hm-visit`·`.hm-ring`·`.hm-donut`·`.hm-trend`·`.hm-bars` 스타일도 삭제하지 않음(롤백 대비).

**롤백 방법**: [002] 이전 원본 Home.tsx의 해당 블록(구 577~633행, `<BarChart3>` 헤더 + `hm-visit` 그리드)을 캘린더 섹션(`===== 3. 캘린더`) 바로 위에 다시 붙여넣기. 프로젝트 원본이 없으면 이 문서 관리자에게 요청 시 전체 블록 제공 가능.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(간트 → 캘린더로 자연 연결).

**영향 범위**: 홈(/) 화면만. 방문률 데이터 자체는 오늘의 할 일 요약·간트 이행현황·기한 알림에 계속 반영됨.

---

## [004] 2026-08-04 — `src/pages/Home.tsx` — "오늘 방문" 카드를 "이번 주 방문 예정"으로 전환

**요청**: 상단 "오늘의 할 일"과 캘린더 옆 "오늘 방문" 카드의 내용이 중복 → 오늘 방문을 "이번 주 방문 예정"으로 바꿔 역할 분담 (오늘은 위에서, 앞으로의 일정은 여기서).

**변경**:
- Home.tsx: `weekPlans` 계산 추가 — 방문 계획(plans)에서 내일부터 7일간을 날짜별로 그룹. 카드 JSX를 날짜 헤더(`hm-wk-day`) + 학교 행(hm-todo 재사용, 클릭 → 학교 상세, 대장 → 링크) 구조로 교체. 구 카드의 빠른 실행 버튼(안전점검 작성·근골 보고서)은 제거됨(오늘 목록이 사라졌으므로)
- home.css: `.hm-wk-day` 날짜 구분 헤더 스타일 추가
- mock-server.mjs: visit-plans에 내일·모레·+4일 계획 5건 시드 추가

**변경 전** (롤백용 — 구 "오늘 방문" 카드 핵심 구조):

```tsx
{/* 오늘 방문 */}
<div className="hm-card">
  <div className="hm-ch">
    <span className="hm-ic y"><CalendarDays size={16} /></span>
    <h3>오늘 방문 ({CUR_MONTH}/{today.getDate()} {DOW_LABELS[today.getDay()]})</h3>
    <div className="hm-r">{todayItems.length}개교</div>
  </div>
  {todayItems.map((it) => (
    <div className="hm-todo" ... onClick={() => it.school_id && nav('/schools/' + it.school_id)}>
      <div className="hm-todo-main">
        <div className="nm">{it.name}</div>
        <div className="meta"><span className={'pillx ' + (it.done ? 'ok' : 'doing')}>
          {it.done ? '방문 완료' : `${CUR_MONTH}월 안전점검`}</span></div>
        {it.school_id && (
          <div className="hm-quick">
            <button className="hm-qbtn" onClick={... nav(`/inspection/new?school=${it.school_id}`)}>
              <ClipboardCheck size={13} /> 안전점검 작성</button>
            <button className="hm-qbtn" onClick={... nav(`/musculo/report?school=${it.school_id}`)}>
              <Activity size={13} /> 근골 보고서</button>
          </div>
        )}
      </div>
      <span className="hm-go">대장 →</span>
    </div>
  ))}
</div>
```

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(5건 · 8/5·8/6·8/8 날짜 그룹 표시).

**영향 범위**: 홈(/) 캘린더 우측 컬럼만. 방문 계획 데이터·캘린더 편집 기능은 변경 없음. 빠른 실행 버튼이 필요하면 오늘의 할 일 행에 추가 가능(후속 검토).

---

## [005] 2026-08-06 — 위험성평가 1단계 "유해위험정보 검토 (부서별)" 화면 신설

**요청**: 위험성평가 사전조사 프로세스 디지털화 — 1단계로 부서(작업)별 유해위험정보(3-1 양식: 기계·기구/유해화학물질/그 밖의 유해위험정보)를 작년 작성분 기준으로 검토하는 화면. (2단계 청취조사는 후속 예정)

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/DeptHazardInfo.tsx` | **신규** — 부서별 유해위험정보 검토 컴포넌트. 부서 탭(급식·시설·미화·통학·당직) × 3구역(기계·기구 명칭/수량, 화학물질 명칭/취급여부, 기타 정보: 3년 재해·작업환경측정·작업허가·도급업체·취약계층). 작년(dept_info_prev) 프리필 + 행 단위 '변경'·'신규' 배지 + 부서별 검토 완료 체크(진행률 표시). 학교 대장 연동: 부서 근로자수 표기, MSDS 미등재 물질 추가 제안, 산재기록 3년 자동 집계 반영 버튼 |
| `src/pages/Risk.tsx` | SurveyData에 `dept_info`·`dept_info_prev` 옵션 필드 추가, 조사표 서브탭 맨 앞에 "① 유해위험정보 (부서별)" 추가 + 기본 탭으로 지정, DeptHazardInfo 렌더 연결. 저장은 기존 PUT /risk/survey 문서에 그대로 실림 — **백엔드 계약 변경 없음** |
| `mock-server.mjs` | s01(한빛초) 조사표에 작년(2025) dept_info_prev 시드 — 사용자 제공 실물 양식(시설 12종 기계·14종 화학물질, 통학 도급 '통학', 당직 취약계층 등) 그대로 |

**데이터 모델** (sections.dept_info — 조사표 문서 내 저장):

```ts
type DeptEquip = { name: string; qty: number }
type DeptChem = { name: string; handled: boolean }
type DeptEtc = {
  acc3y_accident: number; acc3y_other: number      // 최근 3년 재해(사고성/기타)
  env_measure: boolean                              // 작업환경측정 대상
  permit: boolean; permit_types: string[]           // 작업허가(고소·정전·밀폐공간)
  contractor: boolean; contractor_types: string[]; contractor_etc: string
  vulnerable: boolean; vulnerable_types: string[]   // 취약계층(60세 이상 등)
  reviewed: boolean                                 // 부서 검토 완료
}
type DeptInfo = { equips: DeptEquip[]; chems: DeptChem[]; etc: DeptEtc }
// sections.dept_info: Record<부서, DeptInfo> (올해) · sections.dept_info_prev: 작년(비교 기준)
```

**롤백 방법**: ① Risk.tsx에서 DeptHazardInfo import·`dept` 탭 항목·렌더 블록 삭제, SurveyTabKey에서 'dept' 제거, 기본 탭 'safety' 복원 ② `src/features/risk/DeptHazardInfo.tsx` 삭제. 저장된 dept_info 데이터는 문서에 남지만 화면에 표시만 안 됨(무해).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(시설 부서 — 사다리 3 등 12종 프리필, 화학물질 14종 취급 체크, 검토 완료 0/5 진행률).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 기존 6개 서브탭·평가 생성 플로우 변경 없음.

**보완 (같은 날)**:
1. 부서 헤더의 "근로자수 N명 (학교 대장 연동)" 표기 제거 — 유해위험정보는 근로자 수와 무관하다는 피드백 반영. 대장 연동은 MSDS 물질 제안·산재 자동 집계에만 사용.
2. MSDS 제안을 **부서별로 다르게** 표시 — 대장 MSDS의 구역(area)을 부서로 매핑(급식실→급식, 시설창고→시설 등, 키워드 기반 `AREA_PART_KEYWORDS`)해 해당 부서 탭에만 그 구역 물질을 제안. 매핑 불가 구역은 전 부서 공통 제안. 제안 칩에 구역명 병기, 유해화학물질 섹션·제안 박스에 법적 근거(「산업안전보건법」 제110·114조 MSDS 작성·게시·비치) 표기.

---

## [006] 2026-08-06 — 위험성평가 2단계 "청취조사 (개인별)" 화면 신설

**요청**: 청취조사에 의한 유해·위험요인 조사표를 모든 작업(부서) × 종사자 개인별로 작성하는 화면 — 실물 양식(실시방법·수행자/근로자/실시일·경험담 1~3·원인과 판단·개선 조건) 디지털화, 작년 작성분 프리필.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/HearingSurvey.tsx` | **신규** — 좌측: 부서별 종사자 명단(학교 대장 인원수 연동 자동 생성 + '인원 추가', 작성 완료 체크·부서별/전체 진행률, 작년 작성분 '작년' 배지) · 우측: 개인별 조사표(실시방법 고정 문구, 수행자=로그인 사용자 기본값, 경험담 1~3 각 서술+원인·판단, 육하원칙 가이드, 근로자 의견/수행자 의견). 작년 시트는 실시일·수행자만 올해 기준으로 갱신해 프리필 |
| `src/pages/Risk.tsx` | SurveyData에 `hearing`·`hearing_prev` 필드 추가, 조사표 서브탭 두 번째에 "② 청취조사 (개인별)" 추가, HearingSurvey 렌더 연결. 저장은 기존 PUT /risk/survey 문서 — 백엔드 계약 변경 없음 |
| `mock-server.mjs` | s01 hearing_prev 시드 — 실물 양식 샘플(김복자: 급식실 후문 계단 미끄러짐 경험담 + 미끄럼방지조치·안전난간 의견, 박정순: 국솥 화상 위험 경험담) |

**데이터 모델** (sections.hearing):

```ts
type HearingExp = { text: string; cause: string }   // 경험담 서술 + (원인·판단)
type HearingSheet = {
  part: string; worker_name: string; surveyor: string; date: string
  exps: HearingExp[]           // 경험담 1~3
  worker_opinion: string       // 근로자 의견 — 유해·위험 경험의 원인과 판단
  improve_cond: string         // 수행자 의견 — 개선에 대한 조건
  done: boolean                // 작성 완료
}
// sections.hearing: Record<`부서-번호`, HearingSheet> · sections.hearing_prev: 작년
```

**롤백 방법**: ① Risk.tsx에서 HearingSurvey import·'hearing' 탭 항목·렌더 블록 삭제, SurveyTabKey에서 'hearing' 제거 ② `src/features/risk/HearingSurvey.tsx` 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(급식 3·시설 1·미화 1·통학 2·당직 1 명단 자동 생성, 김복자·박정순 작년 경험담 프리필, 작성 완료 0/8 진행률).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 1단계(유해위험정보)·기존 서브탭·평가 생성 플로우 변경 없음.

---

## [007] 2026-08-06 — 위험성평가 3단계 "[정기평가] 위험성평가표 + 감소대책(3-3·3-4)" 화면 신설

**요청**: 공정(급식·당직·미화·통학)별 정기평가 위험성평가표를 점검자가 빈 표에 직접 작성. 가능성×중대성=위험성 자동 계산, **위험성 8점 이상이면 불안전한 행동(3-3)·불안전한 상태(3-4) 감소대책을 작업별로 작성**, 불안전한 상태는 개선 전·후 사진 포함.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/AssessmentTable.tsx` | **신규** — 공정(부서) 탭 × 3개 표. ⑴ 3-2 평가표: 연번·세부작업명·위험분류(선택)·법적근거·위험발생 상황·현재 조치·가능성(1~5)·중대성(1~5)·**위험성 자동 계산+라벨**(≤6 낮음/8 보통/9~12 약간 높음/15↑ 높음, 색상 구분)·감소대책·개선후 위험성·개선예정일·완료일·담당자. 8점 이상 행 노란 강조 + 상단 경고 배너. ⑵ 3-3 불안전한 행동: 단위작업명·재해형태·감소대책·조치결과·조치일자·교육·비고. ⑶ 3-4 불안전한 상태: +조치기한·담당자·완료일·**개선 전/후 사진**(파일명 메타 — 0709 회의 관례). 상단에 1·2단계(유해위험정보·청취조사) 내용 참고 패널(접기식) |
| `src/pages/Risk.tsx` | SurveyData에 `assess`·`reduce_behavior`·`reduce_state` 필드 추가, 서브탭 "③ 위험성평가표·감소대책" 추가, AssessmentTable 렌더 연결. 저장은 기존 PUT /risk/survey 문서 — 백엔드 계약 변경 없음 |

**데이터 모델** (sections.assess / reduce_behavior / reduce_state — 모두 `Record<공정, Row[]>`):

```ts
type AssessRow = { id, task, factor_class, legal_basis, situation, measure_current,
  likelihood, severity, /* 위험성=likelihood×severity 자동 */ reduction, after_risk, plan_date, done_date, owner }
type BehaviorRow = { id, task, accident_type, measure, result, result_date, education, note }
type StateRow = { id, task, accident_type, detail, result, due, owner, done_date, photo_before, photo_after }
```

**롤백 방법**: ① Risk.tsx에서 AssessmentTable import·'assess' 탭·렌더 블록·assessPart state 삭제, SurveyTabKey에서 'assess' 제거 ② `src/features/risk/AssessmentTable.tsx` 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(2×3=6 낮음/3×3=9 약간 높음 자동 계산·행 강조·8점 이상 경고 배너·3-3/3-4 표·사진 첨부 버튼).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 사진은 파일명 메타만 저장(실파일 업로드는 백엔드 연동 시).

---

## [008] 2026-08-06 — 위험성평가를 "정기 위험성평가 결과보고서" 체계로 재구성

**요청**: 최종 산출물인 정기 위험성평가 결과보고서(실물 PDF 21쪽)를 기준으로 화면 재구성 — 탭을 채우면 보고서가 채워지는 구조. 구 6탭(안전·보건/종사자 의견청취/안전·보건 자료/위험요인 파악/학교 순회/안전검사 대상 기계·기구)은 제거.

**보고서 ↔ 화면 매핑**:

| 보고서 | 화면 탭 | 비고 |
|---|---|---|
| 표지·PART 1 (학교 현황·재해발생 현황) | ① 학교 현황 | **신규** ReportInfo |
| PART 2 (실시규정·근거) | — | 고정 텍스트 (보고서 출력 시 자동 삽입 예정) |
| 3-1 유해위험정보 | ② 유해위험정보 | 기존 [005] |
| (작성 근거 자료) | ③ 청취조사 | 기존 [006] |
| 3-2 평가표 · 3-3/3-4 감소대책 | ④ 평가표·감소대책 | 기존 [007] |
| 3-5 참여자 명단 | ⑤ 참여자 명단 | **신규** Participants |

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/ReportInfo.tsx` | **신규** — 1-1 학교 현황(학교명 자동, 학교장·소재지·현업종사원 수는 학교 정보·대장 프리필 후 수정 가능, 업종·구성원·평가기간·평가자·작성 연월) + 1-2 재해발생 현황(최근 3년, 계·재해율 자동 계산) |
| `src/features/risk/Participants.tsx` | **신규** — 3-5 참여자 서명지(소속공정·성명·시작회의/결과교육 일시·서명 체크·비고). "청취조사 명단 불러오기"(올해 명단 없으면 작년 명단 폴백) |
| `src/pages/Risk.tsx` | 탭 재구성: ①~⑤ 보고서 목차 순서. **구 6탭 렌더 블록 전체 삭제(121행)**, SurveyTabKey 정리, 기본 탭 ①. 페이지 탭·카드 제목을 "정기 위험성평가 (보고서 작성)"/"정기 위험성평가 결과보고서 작성"으로 변경. SurveyData에 report_info·participants 추가 |

**롤백 방법**: [008] 이전 Risk.tsx로 복원(구 6탭 블록 포함 — 원본은 인수 시점 소스 기준) + ReportInfo.tsx·Participants.tsx 삭제. 구 6탭의 데이터 필드(policy 등)는 SurveyData 타입·저장 문서에 그대로 남아 있어 복원 시 데이터 손실 없음.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(① 학교장·소재지·종사원 8명 자동 프리필, 재해율 자동 계산 / ⑤ 작년 청취 명단 2명 불러오기).

**영향 범위**: 위험성평가(/risk)만. "학교별 평가"(평가 생성·점수 입력) 기존 플로우는 변경 없음.

**후속 예정**: 보고서 출력(표지·PART 2 고정 텍스트 포함 인쇄/PDF 미리보기) — 별도 요청 시 진행.

---

## [009] 2026-08-06 — "보고서 출력" — 정기 위험성평가 결과보고서 인쇄/PDF 생성

**요청**: ①~⑤ 탭에서 작성한 데이터를 표지·경영방침·PART 2 고정 텍스트와 합쳐 실물 보고서(A4 가로)와 같은 모양의 인쇄용 화면으로 조립 — 브라우저 인쇄 → "PDF로 저장"으로 최종 산출물 생성.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/RiskReport.tsx` | **신규** — 인쇄 미리보기 오버레이(document.body 포탈). 페이지 구성: 표지(학교명·연도·결재란 담당/행정실장/교장·작성 연월·물결 그래픽) → 안전보건 경영방침(전라남도교육청 고정) → 목차 → PART 1(①탭 데이터) → PART 2 3쪽(실시규정 제1~15조·역할표·표3 가능성·표4 중대성·표5 위험성 매트릭스 색상·표6 허용여부·2-2 실시근거 — 고정 텍스트) → 3-1 부서별 유해위험정보(②탭, 실물 양식 재현: 점선 행·V 체크·■/□) → 3-2 위험도 산출 기준 노트 + 공정별 평가표(④탭, 위험성 노란 강조) → 3-3/3-4 감소대책(사진 파일명 표기) → 3-5 참여자 명단(⑤탭, 최소 10행) |
| `src/styles/riskreport.css` | **신규** — rr- 접두사. A4 가로 페이지(1123×794px), `@page size A4 landscape`, 인쇄 시 앱 UI(#root) 숨김·페이지 분리(page-break) |
| `src/pages/Risk.tsx` | 카드 헤더에 "보고서 출력" 버튼 추가(학교 선택 시 활성), RiskReport 렌더 연결 |

**사용법**: 위험성평가 → 학교 선택 → 정기 위험성평가(보고서 작성) → 탭 작성 → **보고서 출력** → [인쇄 / PDF 저장] → 대상 "PDF로 저장" (배경 그래픽 인쇄 켜기 권장).

**롤백 방법**: Risk.tsx에서 RiskReport import·버튼·렌더 블록·reportOpen state 삭제 + RiskReport.tsx·riskreport.css 삭제.

**검증**: `tsc --noEmit` 통과 · 화면 미리보기 확인 · Playwright print-media PDF 생성 테스트(A4 가로 14쪽 — 표지·PART 1 데이터 반영·3-1 체크박스 재현 확인).

**영향 범위**: 위험성평가(/risk)만. 데이터 미작성 섹션은 자동 생략(평가표 없으면 안내 문구).

---

## [010] 2026-08-06 — `src/pages/Risk.tsx` — 학교별 평가 화면을 "작성된 보고서" 중심으로 재구성

**요청**: 학교 컨텍스트 화면에서 "표준 마스터 시드"·"평가 생성" 버튼 제거, 대신 이때까지 작성된 정기 위험성평가 보고서가 보이도록.

**변경**:
- 상단 "표준 마스터 시드"·"평가 생성" 버튼 제거 (평가 생성 모달 코드는 잔존하나 미노출 — 수시 평가는 산업재해 연동으로 생성).
- 학교 컨텍스트 최상단에 **"정기 위험성평가 보고서" 카드** 신설: GET /risk/survey로 해당 학교의 보고서 문서를 조회해 "{연도}년 정기 위험성평가 결과보고서 · 최근 저장일 · 위험성 8점 이상 N건" + 섹션별 진행 칩(① 학교 현황 ✓/－, ② 유해위험정보 n/5부서, ③ 청취조사 n/N명, ④ 평가표 N건, ⑤ 참여자 N명 — 완료 시 초록). 버튼: **[이어서 작성]**(보고서 작성 탭으로 이동) · **[보고서 출력]**(인쇄 미리보기 바로 열기). 미작성 시 "보고서 작성 시작" 안내.
- 기존 KPI(평가 수·총 불완전)와 "연도별 평가" 목록은 유지하되 부제 "수시(산재 연동) 포함 이력"으로 성격 명시, 빈 상태 문구에서 '평가 생성' 언급 제거.

**롤백 방법**: 보고서 카드 블록(즉시실행함수)·ctxReport state/effect 삭제, 상단 버튼 블록 복원:

```tsx
<button className="btn btn-ghost" disabled={busy} onClick={seedMaster}>표준 마스터 시드</button>
{sel && (
  <button className="btn btn-primary"
    onClick={() => { setProcess('catering'); setGenCategory('regular'); setActionErr(''); setOpen(true) }}>
    평가 생성
  </button>
)}
```

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(보고서 카드에 ① 완료 칩·최근 저장일·이어서 작성/보고서 출력 버튼, 연도별 평가 이력 유지).

**영향 범위**: 위험성평가(/risk) 학교별 평가 화면만.

---

## [011] 2026-08-06 — `src/pages/Risk.tsx` — 학교 컨텍스트에서 KPI·"연도별 평가" 이력 전부 제거

**요청**: [010]에서 유지했던 KPI 카드(평가 수·총 불완전)와 "연도별 평가" 목록(구 백엔드 평가 플로우 이력)의 존재 이유 질의 → **전부 제거** 결정. 학교 컨텍스트에는 "정기 위험성평가 보고서" 카드만 남김.

**변경**: 학교 컨텍스트 렌더에서 kpis 블록 + 연도별 평가 ledger 블록 + 하단 안내 문구(총 40행) 삭제. 관련 계산 로직(selItems·selUnsafe·yearGroups·renderGroup 등)과 데이터 로드는 코드에 잔존(다른 화면 집계·롤백 대비, 미표시일 뿐).

**유의**: 산업재해 → "수시 위험성평가 생성"으로 만들어진 건을 이 화면에서 볼 수 없게 됨(데이터는 서버에 계속 저장되고, 경영 대시보드·학교 상세의 5대 업무 현황 집계에는 계속 반영됨). 수시 평가 이력 표시가 다시 필요해지면 [010] 형태로 복원 가능.

**롤백 방법**: [010] 기록의 kpis + 연도별 평가 ledger 블록을 보고서 카드 아래에 복원.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(보고서 카드 단독 표시).

---

## [012] 2026-08-06 — 수시 위험성평가 프로세스 신설 (사고성 산업재해 연동)

**요청**: 사고성 산업재해 발생 시 프로세스 추가 — 산업재해 발생 → ① 산업재해조사표 → ② 재해 재발방지계획 → ③ 수시위험성평가 → ④ 수시위험성평가 교육 → 완료. 실물 양식 4종(산업재해조사표 별지 제30호서식·재해재발방지계획·수시위험성평가 보고서·안전보건교육일지) 기반.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/AdhocAssessment.tsx` | **신규** — 학교 컨텍스트의 "수시 위험성평가" 섹션. ⑴ 목록: 프로세스 안내 + 이 학교의 사고성 산재(kind=accident) 중 미착수 건을 빨간 배너로 표시("수시평가 시작" → 사고 정보 프리필로 케이스 생성), 케이스별 4단계 진행 칩·완료 배지, 수동 등록 버튼. ⑵ 에디터(스텝퍼): ① 조사표(재해자 정보·상해종류/부위·휴업일수·발생 개요/원인·재발방지계획·작성자 — 별지 30호 핵심 항목), ② 재발방지계획(재해 개요 자동 연동 + 원인 분석·대책), ③ 수시평가표(세부작업·상황·가능성×중대성 자동 위험성·감소대책 행 + 감소대책 실행), ④ 교육일지(교육구분 고정·인원·일시/장소/강사·주제/내용/자료·사진 파일명·참석자 명단 서명 체크). 단계별 완료 토글 → 4단계 완료 시 케이스 마무리 |
| `src/pages/Risk.tsx` | SurveyData에 `adhoc_cases` 추가, 학교 컨텍스트에서 정기 보고서 카드 아래 AdhocSection 렌더. 케이스 변경은 ctxReport 문서 상태에 반영, 저장은 PUT /risk/survey(saveCtxDoc) — 백엔드 계약 변경 없음 |

**프로세스 매핑**: 산업재해 메뉴에 사고 등록 → 위험성평가 학교 화면에 자동으로 "수시평가 시작" 배너 노출 → 4단계 순차 작성 → ④ 교육 완료로 마무리 (수시평가는 교육 진행으로 종결된다는 요구 반영).

**롤백 방법**: Risk.tsx에서 AdhocSection import·렌더·setCtxCases/saveCtxDoc/ctxBusy 삭제, SurveyData에서 adhoc_cases 제거, AdhocAssessment.tsx 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(미착수 산재 배너 → 케이스 생성 → 조사표 프리필·스텝퍼 동작).

**영향 범위**: 위험성평가(/risk) 학교 컨텍스트만. **후속 후보**: 수시 보고서 인쇄 출력(정기 RiskReport와 같은 방식, 초록 테마 13쪽 양식) — 요청 시 진행.

---

## [013] 2026-08-06 — 학교 컨텍스트: 정기 보고서·수시 평가 좌우 2단 배치

**요청**: 정기 아래 수시를 세로로 쌓는 배치에 대한 검토 → 좌우 2단 배치 선택.

**변경**:
- Risk.tsx: 학교 컨텍스트의 정기 보고서 카드 + AdhocSection을 `grid-template-columns: 1fr 1fr` 그리드로 감쌈 (1920px 기준 두 카드가 스크롤 없이 한눈에, 높이 stretch).
- AdhocAssessment.tsx: 수시 케이스 편집 화면은 `gridColumn: 1 / -1`로 전체 폭 사용. 목록 헤더에 미착수 사고 건수 빨간 강조 배지 추가.

**롤백 방법**: 그리드 wrapper div 제거 + 카드 marginBottom 18 복원 + gridColumn·미착수 배지 제거.

**검증**: `tsc --noEmit` 통과 · 1920px 렌더링 확인(2단 정렬·미착수 1건 배지).

---

## [014] 2026-08-06 — 학교 컨텍스트: 정기/수시 전환 버튼 + 전체 폭 리스트 (사용자 구상안 반영)

**요청**: 사용자 제공 구상 이미지 반영 — 좌우 2단([013]) 대신 상단 우측에 [정기 위험성평가] [수시 위험성평가] 전환 버튼을 두고, 아래에는 선택된 유형의 리스트를 전체 폭으로 표시.

**변경** (`src/pages/Risk.tsx`):
- 학교 헤더(rkh-schoolhead) 우측에 전환 버튼 2개 추가 — 활성 버튼은 btn-primary(보라). 수시 버튼에는 미착수 사고 건수 빨간 배지("미착수 N") 표시.
- `ctxTab`('regular'|'adhoc') 상태 신설 — 학교 진입 시 '정기'로 초기화. 미착수 집계용 산재 목록(ctxAccidents) 별도 조회.
- [013]의 2단 그리드 제거 → 선택된 유형만 전체 폭 렌더 (정기 보고서 카드 ↔ 수시 AdhocSection).

**롤백 방법**: [013]의 그리드 wrapper 복원, ctxTab·ctxAccidents·전환 버튼 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(정기/수시 전환·미착수 1 배지·전체 폭 리스트).

---

## [015] 2026-08-06 — `src/pages/Risk.tsx` — 상단 버튼 정리: [정기]/[수시] 2개로 통합

**요청**: 버튼 5개 중복 정리 — '학교별 평가' 탭 제거, '정기 위험성평가(보고서 작성)' 탭과 '정기 위험성평가' 전환 버튼을 하나로 합침. 최종적으로 학교 헤더 우측의 [정기 위험성평가] [수시 위험성평가] 두 버튼만 유지.

**변경**:
- 페이지 상단의 글로벌 탭 2개('학교별 평가'·'정기 위험성평가(보고서 작성)')와 조사표용 학교 선택 드롭다운 제거.
- `pageTab` state → `regularEdit`(boolean)로 대체: 보고서 작성 화면(①~⑤ 탭)은 이제 **정기 카드의 "이어서 작성/보고서 작성 시작" 버튼으로 진입**하는 인라인 편집 모드. 학교는 선택된 학교로 자동 지정(드롭다운 불필요), 편집 화면 헤더에 "← 보고서 개요" 복귀 버튼 추가.
- 학교 목록/컨텍스트 렌더 조건을 `!sel` / `sel` 기준으로 단순화. 학교 변경·[정기] 버튼 클릭 시 편집 모드 해제(개요 카드 표시).

**롤백 방법**: pageTab state·글로벌 탭 블록·survey 학교 select 복원, regularEdit 관련 조건 원복 ([014] 이전 구조).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(버튼 2개만 표시, 이어서 작성 → 인라인 ①~⑤ 편집 + 학교 자동 지정 + 복귀 버튼).

**영향 범위**: 위험성평가(/risk) 내비게이션 구조. AppShell 사이드바에서 위험성평가 진입 시 곧바로 학교 목록이 보이는 단일 흐름이 됨: 학교 목록 → 학교 → [정기|수시] → (정기) 개요 카드 ↔ 작성 화면 / (수시) 케이스 목록 ↔ 4단계 에디터.

---

## [016] 2026-08-06 — `src/pages/Risk.tsx` — 정기 카드를 "위험성평가 보고서 리스트"로 전환

**요청**: 헤더의 "보고서 작성 시작" 버튼 제거, 단일 "정기 위험성평가 보고서" 카드 대신 보고서 리스트 형태로.

**변경**: 정기 뷰를 테이블 리스트로 재구성 — 컬럼: 보고서명(연도) / 진행 상황(①~⑤ 섹션 칩 + 8점 이상 경고) / 상태(미작성·작성 중·작성 완료 — 5개 섹션 완성 여부로 자동 판정) / 최근 저장 / 작업([작성 or 이어서 작성]·[보고서 출력]). 행 클릭 = 작성 화면 진입. 헤더 버튼 제거, 우측에 "연 1회 정기 실시 · 연도별 1부" 안내. 하단에 연도 누적 안내 문구(해가 바뀌면 새 연도 행 추가·작년분 자동 프리필).

**참고**: 현재 저장 구조는 학교당 보고서 문서 1부(당해 연도)라 리스트에 당해 연도 행 1개가 표시됨. 과년도 보고서 보관(연도별 아카이브)이 필요해지면 문서에 `report_archive` 섹션을 추가해 같은 리스트에 쌓이도록 확장 가능 — 백엔드 계약 변경 없이 가능.

**롤백 방법**: [015] 시점의 카드형 블록으로 IIFE 내부 JSX 교체.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(리스트 행·상태 미작성·[작성] 버튼·행 클릭 진입).

---

## [017] 2026-08-06 — 보고서 작성 탭에 "다음 →" 이동 버튼 추가

**요청**: 작성 탭 하단 — 저장 버튼은 왼쪽에 두고, 오른쪽에 "다음" 버튼으로 다음 단계로 이동.

**변경**: ReportInfo·DeptHazardInfo·HearingSurvey·AssessmentTable 4개 컴포넌트에 `onNext` 옵션 prop 추가 — 하단 푸터 우측 끝(`marginLeft: auto`)에 "다음 →" 버튼 렌더. Risk.tsx에서 ①→②→③→④→⑤ 순서로 연결 (⑤ 참여자 명단은 마지막이라 다음 버튼 없음).

**롤백 방법**: 4개 컴포넌트의 onNext prop·버튼과 Risk.tsx의 onNext 연결 4곳 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(① 하단 우측 다음 버튼 → 클릭 시 ② 탭 전환).

**보완 (같은 날)**: 탭 라벨의 괄호 표기 제거 — "① 학교 현황 (PART 1)" → "① 학교 현황" 등 5개 탭 모두 간결하게.

---

## [018] 2026-08-06 — 이행점검을 "점검 조사지" 중심으로 전면 재구성

**요청**: 이행점검 프로세스 반영 — 교육청 공문 하달(상·하반기 각 1회) → 중대재해예방 의무이행사항 점검 조사지 작성(내용: 점검자 직접 작성) → **학교 행정선생님 면담 후 결과 체크** → 학교 메일로 제출. 실물 조사지(항목/내용/결과 3컬럼, 11개 항목) 기반.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/compliance/ComplianceSheet.tsx` | **신규** — ⑴ 조사지 폼: 11개 항목(성명 확인·관리감독자 교육·위험성평가·예산집행실적·산업재해·홈페이지 게시판·매뉴얼 교육·안전보건관리계획서·안전보건협의체·보호구 지급대장·안전보건경영방침)을 항목/내용(점검자 작성, 표준 문구 프리필)/결과(면담 후 체크) 3컬럼으로. 결과 위젯: O/X 토글·월 입력(원격 4월/집합 5월)·금액 입력(개선비 등)·날짜 입력(교육일·산재발생일)·성명 3인. 상단 면담 정보(공문 접수일·면담 대상 행정선생님·면담일) + "결과는 행정선생님 면담 후 작성" 안내 고정. ⑵ 인쇄 미리보기: A4 세로 1쪽 실물 양식 재현(인쇄→PDF 저장, 메일 첨부용) |
| `src/pages/Compliance.tsx` | **전면 재작성** — 학교 목록(상·하반기 상태 컬럼) → 학교별 조사지 리스트(반기별 행: 공문 접수일·면담·결과 기입 진행도 n/8·상태·제출일·작업[작성/인쇄/메일 제출]) → 조사지 작성 화면. **[학교 메일로 제출]**: 학교 이메일로 메일 앱 열림(제목·본문 자동 완성) + 제출일 기록·상태 '제출 완료'(해제 가능). 저장은 `/ops/docs/compliance-sheets` 문서(학교별×반기별) — 백엔드 계약 파일 수정 없음 |

**구 화면 대체**: 기존 이행점검 화면(학교별 연도·반기 이력 위계 뷰 + 항목 체크 모달, /compliance API 기반)은 조사지 체계로 대체. 서버의 기존 /compliance 데이터는 보존되나 화면에는 미표시(롤백 시 인수 시점 Compliance.tsx 원본으로 복원).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(조사지 리스트 상·하반기 2행 → 작성 화면 3컬럼·O/X 토글·프리필 문구).

**영향 범위**: 이행점검(/compliance)만. 홈 간트·대시보드의 이행점검 집계는 기존 /compliance API 기준이므로 조사지 제출 상태와는 별개(연동 필요 시 후속).

---

## [019] 2026-08-06 — `src/pages/Compliance.tsx` — 조사지를 "작성 버튼으로 생성"하는 방식으로 변경

**요청**: 상·하반기 행을 미리 만들어두는 방식에 대한 문제 제기(2027년 이후 연도 처리 불명확) → [조사지 작성] 버튼으로 그때그때 생성하는 전략 채택.

**변경**:
- 리스트에 당해 연도 상·하반기 행을 가상으로 미리 표시하던 로직 제거 — **실제 작성된 조사지만** 연도 역순으로 표시. 빈 상태: "교육청 공문이 내려오면 '조사지 작성'으로 시작하세요."
- 리스트 헤더에 **[+ 조사지 작성]** 버튼 → 생성 모달(연도 기본 올해, 반기 기본 현재 월 기준 자동, 공문 접수일 입력). 같은 반기 중복 생성 방지. 생성 즉시 작성 화면으로 이동.
- 연도 처리: 2027년이 되면 버튼으로 "2027년 상반기"를 생성 — 2026년 조사지들은 리스트에 이력으로 계속 남음. 학교 목록의 상·하반기 상태 컬럼은 당해 연도 기준 유지(미생성 = 미작성).

**롤백 방법**: periodKeys에 CUR_PERIODS 병합 복원, 생성 모달·버튼 제거 ([018] 형태).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(빈 리스트 → 생성 모달 → 2026 하반기 행 생성·작성 중 상태).

**보완 (같은 날)**: [조사지 작성] 버튼 위치를 위험성평가와 동일하게 학교 헤더(rkh-schoolhead) 우측·리스트 카드 위로 이동. 리스트 헤더에는 프로세스 안내 문구만 유지.

---

## [020] 2026-08-07 — 학교 허브(/schools) 상단 KPI 카드를 검색·필터 패널로 교체

**요청**: 학교 화면 상단의 통계 카드 3개(전체 학교·현업종사자 합계·인원 불일치) 영역을 학교명·지역명 검색 + 초/중/고 선택 조회 영역으로 변경.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | KPI 카드 3개(`.kpis` 블록) 제거 → **검색 패널**(`.shub-search`) 신설: 검색 인풋(학교명·지역명, 지우기 X 버튼) + 학교급 세그먼트 버튼(전체·유·초·중·고·기타) + 인원대조 드롭다운 + 결과 건수 표시. 검색 대상 `HUB_SEARCH`를 학교명+주소(지역명)로 변경(구: 학교명·담당자·학교장). 목록 헤더의 중복 `<FilterBar>` 제거 — 검색·필터는 상단 패널로 일원화(기존 useTableQuery 훅 그대로 소비, 로직 변경 없음) |
| `src/styles/schoolhub.css` | `.shub-search`·`.shub-search-input`·`.shub-seg`·`.shub-search-hint` 스타일 추가 (shub- 접두사 관례, tokens.css 변수 재사용 — 다크모드 자동 호환) |

**변경 전** (롤백용 — SchoolsHub.tsx 구 KPI 블록):

```tsx
<div className="kpis">
  <div className="kpi"><div className="l">전체 학교</div><div className="v">{rows.length}<small> 교</small></div><div className="d">등록된 학교(기관)</div></div>
  <div className="kpi"><div className="l">현업종사자 합계</div><div className="v">{totalWorkers}<small> 명</small></div><div className="d">5대 파트 전체</div></div>
  <div className={'kpi ' + (mismatchCount ? 'warn' : '')}><div className="l">인원 불일치</div><div className="v">{mismatchCount}<small> 교</small></div><div className="d">종사자수 vs 교육생수</div></div>
</div>
```

```tsx
const HUB_SEARCH = [
  (r: Row) => r.school.name,
  (r: Row) => r.school.manager ?? '',
  (r: Row) => r.school.principal ?? '',
]
```

(목록 헤더에는 `<FilterBar q={q} />`가 있었고, totalWorkers·mismatchCount 계산 2줄이 있었음)

**롤백 방법**: 위 변경 전 블록 복원(kpis·HUB_SEARCH·FilterBar·계산 2줄) + 신규 `.shub-search` JSX 블록·schoolhub.css의 "상단 검색 패널" 스타일 블록 삭제. git 기준으로는 커밋 `93c08eb` revert.

**검증**: `tsc --noEmit` 통과 · 목서버 연동 렌더링 확인 — 지역명 "경기도" 검색 3교, 학교급 [초] 필터 4교, 결과 건수 실시간 표시, 지우기 버튼 동작.

**영향 범위**: 학교 허브(/schools)만. KPI 수치(현업종사자 합계 등)는 화면에서 사라짐 — 필요 시 경영 대시보드에서 확인(복원 요청 가능). 정렬·CSV 내보내기·표/카드 토글·페이지네이션은 변경 없음.

**보완 (같은 날)**: 사용자 구상안 반영 — 통합 검색창 1개를 **[학교명]·[지역명] 라벨 분리형 입력창 2개**로 교체 (`.shub-search-input` → `.shub-field`, 라벨 박스 + 입력 결합형). 학교명은 학교(기관)명만, 지역명은 주소만 각각 부분일치 검색하며 둘 다 입력 시 AND 조건. 검색은 useTableQuery 밖에서 rows를 선필터링(useMemo)하고 level·인원대조 필터는 기존 훅 그대로 사용, 검색어 변경 시 1페이지로 리셋. 검증: `tsc --noEmit` 통과 · 렌더링 확인(지역명 "서울" × 학교급 [중] → 2교).

**보완 2 (같은 날)**: 인원대조 드롭다운·"등록/검색 결과 N교" 표시 제거, 우측에 **[조회] 버튼** 추가 — 검색이 입력 즉시가 아니라 [조회] 클릭(또는 입력창 Enter)으로 실행되는 확정형으로 변경(`applied` state). 학교급 버튼은 즉시 반영 유지, X 지우기는 해당 조건 즉시 해제. 인원대조 필터는 UI에서 제거(테이블 인원대조 컬럼·정렬은 유지, HUB_FILTERS에서 mismatch 정의 삭제). 검증: `tsc --noEmit` 통과 · 렌더링 확인(학교명 "초등" × 지역명 "서울" 조회 → 2교).

---

## [021] 2026-08-07 — 학교 허브: 담당/전체 토글 + 5대 업무 바로가기 버튼(상태 배지)

**요청**: 전략 논의 결과 반영 — 학교 탭을 "일하러 들어가는 입구"로. 표/카드 토글을 **[담당 학교] [전체 학교]** 토글로 교체하고, 학교 행마다 5대 업무(안전점검·위험성평가·근골격계·교육·이행점검) 바로가기 버튼을 상태 배지와 함께 배치. 버튼 클릭 → 해당 업무 페이지의 학교 컨텍스트로 즉시 진입(행 클릭은 기존대로 학교 상세).

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | ⑴ 표/카드 토글·카드형 보기 제거 → 담당/전체 토글(담당 배정 있으면 담당 기본, 없으면(관리자) 전체 기본 — `assigned_inspector_id === user.login` 기준). ⑵ "업무 바로가기" 컬럼 신설 — 학교별 상태 배지 칩 5개, 클릭 시 `?school=id`로 해당 업무 페이지 이동. 상태 조회: 학교별 inspections·risk·musculo 3개 API + 이행점검 조사지 문서(/ops/docs/compliance-sheets) 1회 병렬 조회. ⑶ 컬럼 정리: 관리감독자·안전점검기관 컬럼 제거(공간 확보 — CSV 내보내기에는 유지, 학교 상세에서 확인 가능) |
| `src/pages/Inspection.tsx`·`Risk.tsx`·`Musculo.tsx`·`Education.tsx` | `?school=id` 딥링크 — 학교 목록 로드 후 해당 학교로 `openSchool()` 자동 호출 (useSearchParams, 각 4~10행 추가) |
| `src/pages/Compliance.tsx` | 동일 딥링크 — `setSel(s)` 방식 |
| `src/styles/schoolhub.css` | `.shub-works-cell`·`.shub-w` 칩 스타일 추가 (상태색 점 + 경고/위험 시 테두리 강조) |

**상태 배지 규칙** (TodayHero.deriveTasks 규칙 재사용):
- 안전점검: 이번 달 제출/서명 있음 → 완료(초록) / 없음 → 이번 달 미실시(주황)
- 위험성평가: status≠completed N건 → 진행 N건(보라) / 전부 완료 → 완료(초록) / 없음 → 미작성(회색)
- 근골격계: needs_review 합 N>0 → 검수 N건(빨강) / 0 → 특이사항 없음(초록)
- 교육: 대장 인원대조 기준 — 불일치(주황) / 일치(초록)
- 이행점검: 당기 반기 조사지 — 제출 완료(초록) / 작성 중(보라) / 미작성(회색)
- 상세 상태는 칩 툴팁(title)으로 표시

**롤백 방법**: git 커밋 revert(단일 커밋). 카드형 보기 JSX·표/카드 토글은 [021] 이전 커밋에 보존. 각 업무 페이지의 딥링크 effect(주석 "학교 탭 바로가기")와 useSearchParams import 삭제로 페이지별 개별 롤백 가능.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인 — insp01 로그인 시 [담당 학교 4] 기본 선택·4교 표시, admin 로그인 시 [전체 학교] 기본, 한빛초 근골격계 빨간 배지(검수 1건)·위험성평가 버튼 클릭 → /risk 학교 컨텍스트(보고서 리스트) 자동 진입.

**영향 범위**: 학교 허브(/schools) + 5개 업무 페이지의 진입 동선. 각 업무 페이지의 기존 학교 목록·컨텍스트 화면 자체는 변경 없음(입구만 추가). 담당/전체는 보기 필터이며 접근 제한이 아님(전략 문서 기준).

**보완 (같은 날)**: ⑴ 업무 바로가기 컬럼을 **[담당 학교]에서만 표시** — 전체 학교는 조회용이므로 제거(요청 반영), colSpan 7/8 분기. ⑵ mock-server 시드 보완 — 데모 기본 계정(admin)에도 담당 학교 5교 배정(s01 한빛초·s04 샛별유치원·s06 해솔중·s10 한마음고·s12 바다초, 기존 insp01~03 배정 일부 이관)해 admin 로그인에서도 담당 학교 리스트가 채워지도록. 검증: admin 로그인 → [담당 학교 5] 기본·버튼 표시 / [전체 학교] 12교·버튼 컬럼 없음.

---

## [022] 2026-08-07 — `src/pages/Home.tsx` — 홈 "담당 학교" 표 섹션 제거

**요청**: 홈 하단의 담당 학교 표(학교명·학교급·담당자·당월 방문 + 검색/학교급 필터/학교 등록 버튼) 삭제 — 학교 목록 역할은 학교 탭([021] 담당/전체 토글·업무 바로가기)으로 일원화됨.

**변경**: `{/* ===== 4. 담당 학교 표 ===== */}` 주석부터 `<Pagination q={q} />` 포함 ledger 블록 전체(55행) 삭제. JSX만 제거 — 관련 로직(q 테이블 훅·visits·schoolModal state·학교 등록 모달 컴포넌트)은 코드에 잔존(미노출, 롤백 대비). 홈에서 학교 등록 진입점이 사라짐 — 학교 등록은 학교 탭의 [+ 학교 등록] 버튼 사용.

**롤백 방법**: git 커밋 revert 또는 [021] 시점 Home.tsx의 해당 블록을 캘린더 섹션 뒤에 복원.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(홈이 캘린더+이번 주 방문 예정+기한 알림으로 종료, "담당 학교" 텍스트 잔존 0).

**영향 범위**: 홈(/) 화면만.

---

## [023] 2026-08-07 — 업무 탭 5개 첫 화면을 "작성물 리스트"로 전환

**요청**: 안전점검·위험성평가·근골격계·교육·이행점검 탭의 첫 화면이 학교 목록이 아니라 **점검자들(+나)이 작성한 보고서/조사/조사지 리스트**가 되도록 — 전략 논의(학교 탭 = 입구, 업무 탭 = 작성물 검토·마감 관리) 반영.

**변경 파일** (각 페이지의 1단계 화면만 교체 — KPI 카드·학교 컨텍스트·작성 화면은 변경 없음, 행 클릭 = 해당 학교 컨텍스트 진입):

| 파일 | 첫 화면 | 데이터 |
|---|---|---|
| `Inspection.tsx` | "작성된 점검표" — 점검일·학교·담당자·공정·항목수·서명·상태·교육청 전송, 최신순, 상태·학교급 필터 | 기존 insMap 평탄화 (추가 API 없음) |
| `Risk.tsx` | "작성된 보고서" — 정기 보고서(섹션 n/5·8점 이상 N건·작성 중/완료) + 수시 케이스(단계 n/4), 구분 필터. 수시 행 클릭 시 수시 탭으로 진입 | 전 학교 GET /risk/survey 병렬 조회(docMap 신설). ctxTab 리셋을 effect→openSchool로 이동 |
| `Musculo.tsx` | "작성된 조사" — 조사일·학교·기본조사·증상조사표·부담작업·검수 대기, 부담작업·검수 필터 | 기존 surveyMap 평탄화 |
| `Education.tsx` | "교육 진도 기록" — 진도 데이터가 있는 학교만 표시(기록 없는 학교는 미표시) | 기존 schoolRows 필터 |
| `Compliance.tsx` | "작성된 조사지" — 반기·학교·결과 기입 n/8·상태·제출일, 상태 필터 | 기존 doc(조사지 문서) 평탄화 |
| `mock-server.mjs` | s01 조사표에 assess 2행 시드(리스트 확인용 샘플 — "작성 중 · 섹션 1/5 · 8점 이상 1건") | |

**설계 원칙**: 작성물 리스트는 "작성된 것"만 보여줌 — 미작성(누락) 관리는 학교 탭의 업무 바로가기 상태 배지([021])가 담당. 새 작성의 공식 입구도 학교 탭 바로가기(빈 상태 안내 문구로 유도).

**롤백 방법**: git 커밋 revert. 각 페이지의 구 학교 목록 로직(schoolRows·SCHOOL_QUERY·q)은 코드에 잔존(미렌더) — 페이지별로 ledger 블록 JSX만 이전 커밋 형태로 되돌리면 개별 복원 가능.

**검증**: `tsc --noEmit` 통과 · 5개 탭 렌더링 확인(점검표 7건 최신순·보고서 1건(시드)·조사 3건·진도 기록·조사지 빈 상태 안내) · 행 클릭 → 학교 컨텍스트 진입 확인.

**영향 범위**: 5개 업무 페이지의 첫 화면만. 학교 컨텍스트 이후 화면·작성 플로우·백엔드 계약 변경 없음.

**보완 (같은 날)**: 안전점검 탭 상단 KPI 카드 3개(학교 수·전체 점검·제출) 제거 — 첫 화면이 작성물 리스트로 바로 시작(요청 반영). totalCount·totalSubmitted 계산 로직은 잔존(롤백 대비). 이어서 위험성평가(학교 수·전체 평가·수시 평가)·근골격계(학교 수·전체 조사·검수 대기) 탭의 상단 KPI 카드도 동일하게 제거. 교육 탭 카드는 유지(별도 요청 없음). 또한 안전점검 학교 컨텍스트 헤더의 [점검 생성](빈 점검 생성 모달) 버튼 제거 — [점검표 작성] 버튼만 그 자리에 유지(생성 모달 코드는 잔존·미노출), 빈 상태 문구도 정리. 학교 컨텍스트의 KPI 카드 2개(점검 수·제출 수)도 제거.

---

## [024] 2026-08-07 — 안전점검 학교 컨텍스트: "점검 현황" 개칭 + [이어서 작성] 버튼

**요청**: "월별 점검 이력" 명칭을 "점검 현황"으로 변경, 위험성평가처럼 이어서 작성 버튼 추가.

**변경**:
- `Inspection.tsx`: 섹션 제목 변경. 월 그룹 표에 "작업" 컬럼 신설 — 작성중 건은 **[이어서 작성]**(→ `/inspection/new?school=&part=&resume=` 이동), 그 외는 [보기](상세 모달). 행 클릭 동작(상세 모달)은 유지.
- `InspectionForm.tsx`: `part`·`resume` 파라미터 지원 — part가 있으면 해당 공정만 점검대상으로 자동 선택, resume이 있으면 ⑴ 대상 점검의 기존 결과·비고를 폼에 프리필(코드 일치 항목만, 구 시드 ok/fix 값도 역매핑 수용) ⑵ 저장 시 새 점검을 만들지 않고 그 점검 ID에 이어서 기록.

**롤백 방법**: git revert. Inspection의 작업 컬럼·nav, InspectionForm의 partParam/resumeId 분기 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(세종고 작성중(미화) 건 [이어서 작성] → 작성 폼 진입).

**영향 범위**: 안전점검 학교 컨텍스트·점검표 작성 폼. 백엔드 계약 변경 없음(기존 POST/PUT 엔드포인트 그대로).

**보완 (같은 날)**: 점검 현황의 월별 그룹(접기식 박스)을 없애고 **하나의 통합 테이블**(점검일 최신순 · 점검일 컬럼 첫 번째)로 병합. 월 그룹 관련 코드(monthGroups·renderGroup)는 잔존(미사용, 롤백 대비).

---

## 부록: 신규 추가 파일 (프로젝트 소스 아님 — 개발 보조 도구)

| 날짜 | 파일 | 용도 |
|---|---|---|
| 2026-07-24 | `mock-server.mjs` | 가짜 백엔드(포트 3001). 백엔드 없이 화면 작업용 샘플 데이터 제공. 납품 시 제외 가능 |
| 2026-07-24 | `start.bat` | 목서버 + vite 동시 실행 스크립트. 납품 시 제외 가능 |
| 2026-07-24 | `수정이력.md` | 본 문서 |
| 2026-08-07 | `off.bat` | 개발 서버 일괄 종료 스크립트 — 창 제목·포트(3001/5173) 기준으로 남은 node 프로세스 정리. 납품 시 제외 가능 |

> 기존 소스 파일 중 실제로 수정된 것은 현재 `src/styles/home.css` 1건뿐입니다.
> 백엔드 계약 파일(`src/lib/api.ts`, `src/lib/auth.tsx`, `vite.config.ts` proxy)은 수정하지 않았습니다.
