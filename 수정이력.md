# web-hq 수정 이력

디자인 작업 중 변경된 코드의 기록입니다. 각 항목에 **변경 전 코드**가 그대로 남아 있어, 해당 부분을 되돌려 붙이면 롤백됩니다. 문서화(인수인계·보고) 시 이 파일을 그대로 발췌해 사용할 수 있습니다.

표기 규칙: `[번호] 날짜 — 파일 — 요약` / 변경 이유 / 변경 전·후 코드 / 영향 범위

---

## [001] 2026-07-24 — `src/styles/home.css` — 홈 히어로 카드 높이를 우측 컬럼에 맞춤

**요청**: 홈 화면에서 "법정업무 운영 공지" 카드가 우측 산재 알림 카드보다 먼저 끝나 아래 빈 여백이 생김 → 히어로를 늘려 우측 컬럼과 바닥선을 맞추고 여백을 채움.

**방식**: CSS만 수정 (TSX 코드·백엔드 계약 파일 미변경)

- `.hm-top`의 `align-items: start` → `stretch` (히어로 셀이 우측 컬럼 높이까지 확장)
- `.hm-cyc`를 세로 flex 컨테이너로 전환, `.hm-gantt`가 남는 높이를 흡수
- 업무 행(`.hm-gline.hm-grow`)들이 남는 높이를 균등 분배 (`flex: 1; min-height: 44px`)

**변경 전** (롤백 시 이 코드로 되돌리기 — home.css 77~83행 부근):

```css
.hm-top {
  display: grid;
  grid-template-columns: 1fr 356px;
  gap: 18px;
  align-items: start;
  margin-top: 14px;
}
```

**변경 후**:

```css
.hm-top {
  display: grid;
  grid-template-columns: 1fr 356px;
  gap: 18px;
  align-items: stretch; /* 히어로를 우측 컬럼(공지+산재) 높이에 맞춤 */
  margin-top: 14px;
}
/* 히어로 카드가 셀 높이를 채우고, 남는 공간은 간트 업무 행들이 고르게 나눠 갖는다 */
.hm-cyc {
  display: flex;
  flex-direction: column;
}
.hm-cyc .hm-gantt {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.hm-cyc .hm-gantt .hm-gline.hm-grow {
  flex: 1;
  min-height: 44px;
}
```

**롤백 방법**: 변경 후 블록 전체를 지우고 변경 전 블록으로 교체 (추가된 `.hm-cyc` 3개 규칙 삭제 포함).

**영향 범위**: 홈(/) 화면의 상단 히어로만. 우측 알림 개수가 늘면 히어로도 함께 늘어남. 1280px 이하 반응형(단일 컬럼 전환)에는 영향 없음.

---

## [002] 2026-08-04 — 홈 상단에 "오늘의 할 일" 히어로 신설, 연간 사이클은 아래로 이동

**요청**: 홈 최상단(구 운영 공지 자리)에 오늘 방문할 학교와 할 업무가 보이게. 연간 법정업무 운영 공지(간트)는 삭제하지 않고 오늘의 할 일 아래에 유지.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/TodayHero.tsx` | **신규** — 오늘의 할 일 카드. Home의 todayItems(캘린더 계획+방문 실적 병합)를 받아 학교별 미완 업무를 4개 API(inspections·risk·compliance·musculo)로 병렬 조회, 칩으로 표시. 행 클릭 → 학교 상세 |
| `src/pages/Home.tsx` | 상단 그리드(hm-top) 좌측 셀: `<CycleHero …/>` → `<TodayHero …/>`. CycleHero는 hm-top 닫힌 직후 `<div className="hm-mt">`로 감싸 전체 폭 배치(코드·기능 그대로, 위치만 이동). import에 TodayHero 추가 |
| `src/styles/home.css` | `.hm-today`·`.hm-td-*` 스타일 블록 추가 (기존 규칙 수정 없음, 순수 추가) |
| `mock-server.mjs` | visit-plans 문서에 오늘 날짜 방문 계획 4건 시드 (화면 확인용 샘플) |

**업무 칩 도출 규칙** (TodayHero.deriveTasks):
- 이번 달 제출/서명된 점검 없음 → `안전점검 · 이번 달 미실시` (주황)
- 위험성평가 status ≠ completed → `위험성평가 진행 N건` (보라)
- 이행점검 draft 존재 → `이행점검 작성 중` (보라)
- 증상조사표 needs_review 합 > 0 → `증상조사표 검수 N건` (빨강)
- 모두 해당 없음 → `특이 업무 없음 · 정기 방문` (회색)

**변경 전** (롤백용 — Home.tsx hm-top 내부 원래 코드):

```tsx
{/* ===== 1. 연간 법정업무 운영 공지 히어로 + 공지사항 ===== */}
<div className="hm-top">
  <CycleHero
    ay={AY}
    monthLabels={AY_MONTHS.map((m) => m.label)}
    monthEn={MONTH_EN}
    curAyIdx={curAyIdx}
    inspSegs={inspSegs}
    inspNum={ /* …방문 수 표기 JSX (변경 없음 — 이동만 됨)… */ }
    nowlineLeft={nowlineLeft}
    doc={cycleDoc}
    onSave={saveCycleDoc}
  />
  {/* 공지사항 */}
```

**롤백 방법**:
1. Home.tsx: hm-top 안의 `<TodayHero …/>`를 위 변경 전 `<CycleHero …/>` 블록으로 되돌리고, 아래에 새로 추가된 `{/* ===== 1-2. 연간 법정업무 운영 공지 … ===== */}` `<div className="hm-mt"><CycleHero …/></div>` 블록과 TodayHero import 줄 삭제 (CycleHero JSX 내용은 동일하므로 그 블록을 그대로 hm-top 안으로 옮겨도 됨)
2. `src/components/TodayHero.tsx` 파일 삭제
3. home.css의 `/* ---- 오늘의 할 일 히어로 (TodayHero) ---- */` 블록 삭제

**검증**: `tsc --noEmit` 통과 · 목서버 연동 실제 렌더링 스크린샷 확인 (완료 2곳 초록 체크·예정 3곳 업무 칩·간트 전체 폭 정상)

**영향 범위**: 홈(/) 화면만. CycleHero 기능(편집·저장·기한 알림 연동)은 그대로 유지. [001]의 stretch 레이아웃은 이제 TodayHero 카드에 적용되어 우측 컬럼(공지+산재)과 바닥선을 맞춤.

---

## [003] 2026-08-04 — `src/pages/Home.tsx` — "학교 방문률" 섹션 제거

**요청**: 홈에서 학교 방문률 카드(도넛 + 월별 방문률 막대) 삭제.

**변경**: Home.tsx의 `{/* ===== 2. 학교 방문률 ===== */}` 주석부터 해당 `<div className="hm-card hm-mt">…</div>` 블록 전체(57행)를 삭제. JSX만 제거 — 방문률 계산 로직(monthVisited·curPct 등)은 오늘의 할 일·간트·기한 알림이 계속 사용하므로 유지. home.css의 `.hm-visit`·`.hm-ring`·`.hm-donut`·`.hm-trend`·`.hm-bars` 스타일도 삭제하지 않음(롤백 대비).

**롤백 방법**: [002] 이전 원본 Home.tsx의 해당 블록(구 577~633행, `<BarChart3>` 헤더 + `hm-visit` 그리드)을 캘린더 섹션(`===== 3. 캘린더`) 바로 위에 다시 붙여넣기. 프로젝트 원본이 없으면 이 문서 관리자에게 요청 시 전체 블록 제공 가능.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(간트 → 캘린더로 자연 연결).

**영향 범위**: 홈(/) 화면만. 방문률 데이터 자체는 오늘의 할 일 요약·간트 이행현황·기한 알림에 계속 반영됨.

---

## [004] 2026-08-04 — `src/pages/Home.tsx` — "오늘 방문" 카드를 "이번 주 방문 예정"으로 전환

**요청**: 상단 "오늘의 할 일"과 캘린더 옆 "오늘 방문" 카드의 내용이 중복 → 오늘 방문을 "이번 주 방문 예정"으로 바꿔 역할 분담 (오늘은 위에서, 앞으로의 일정은 여기서).

**변경**:
- Home.tsx: `weekPlans` 계산 추가 — 방문 계획(plans)에서 내일부터 7일간을 날짜별로 그룹. 카드 JSX를 날짜 헤더(`hm-wk-day`) + 학교 행(hm-todo 재사용, 클릭 → 학교 상세, 대장 → 링크) 구조로 교체. 구 카드의 빠른 실행 버튼(안전점검 작성·근골 보고서)은 제거됨(오늘 목록이 사라졌으므로)
- home.css: `.hm-wk-day` 날짜 구분 헤더 스타일 추가
- mock-server.mjs: visit-plans에 내일·모레·+4일 계획 5건 시드 추가

**변경 전** (롤백용 — 구 "오늘 방문" 카드 핵심 구조):

```tsx
{/* 오늘 방문 */}
<div className="hm-card">
  <div className="hm-ch">
    <span className="hm-ic y"><CalendarDays size={16} /></span>
    <h3>오늘 방문 ({CUR_MONTH}/{today.getDate()} {DOW_LABELS[today.getDay()]})</h3>
    <div className="hm-r">{todayItems.length}개교</div>
  </div>
  {todayItems.map((it) => (
    <div className="hm-todo" ... onClick={() => it.school_id && nav('/schools/' + it.school_id)}>
      <div className="hm-todo-main">
        <div className="nm">{it.name}</div>
        <div className="meta"><span className={'pillx ' + (it.done ? 'ok' : 'doing')}>
          {it.done ? '방문 완료' : `${CUR_MONTH}월 안전점검`}</span></div>
        {it.school_id && (
          <div className="hm-quick">
            <button className="hm-qbtn" onClick={... nav(`/inspection/new?school=${it.school_id}`)}>
              <ClipboardCheck size={13} /> 안전점검 작성</button>
            <button className="hm-qbtn" onClick={... nav(`/musculo/report?school=${it.school_id}`)}>
              <Activity size={13} /> 근골 보고서</button>
          </div>
        )}
      </div>
      <span className="hm-go">대장 →</span>
    </div>
  ))}
</div>
```

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(5건 · 8/5·8/6·8/8 날짜 그룹 표시).

**영향 범위**: 홈(/) 캘린더 우측 컬럼만. 방문 계획 데이터·캘린더 편집 기능은 변경 없음. 빠른 실행 버튼이 필요하면 오늘의 할 일 행에 추가 가능(후속 검토).

---

## [005] 2026-08-06 — 위험성평가 1단계 "유해위험정보 검토 (부서별)" 화면 신설

**요청**: 위험성평가 사전조사 프로세스 디지털화 — 1단계로 부서(작업)별 유해위험정보(3-1 양식: 기계·기구/유해화학물질/그 밖의 유해위험정보)를 작년 작성분 기준으로 검토하는 화면. (2단계 청취조사는 후속 예정)

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/DeptHazardInfo.tsx` | **신규** — 부서별 유해위험정보 검토 컴포넌트. 부서 탭(급식·시설·미화·통학·당직) × 3구역(기계·기구 명칭/수량, 화학물질 명칭/취급여부, 기타 정보: 3년 재해·작업환경측정·작업허가·도급업체·취약계층). 작년(dept_info_prev) 프리필 + 행 단위 '변경'·'신규' 배지 + 부서별 검토 완료 체크(진행률 표시). 학교 대장 연동: 부서 근로자수 표기, MSDS 미등재 물질 추가 제안, 산재기록 3년 자동 집계 반영 버튼 |
| `src/pages/Risk.tsx` | SurveyData에 `dept_info`·`dept_info_prev` 옵션 필드 추가, 조사표 서브탭 맨 앞에 "① 유해위험정보 (부서별)" 추가 + 기본 탭으로 지정, DeptHazardInfo 렌더 연결. 저장은 기존 PUT /risk/survey 문서에 그대로 실림 — **백엔드 계약 변경 없음** |
| `mock-server.mjs` | s01(한빛초) 조사표에 작년(2025) dept_info_prev 시드 — 사용자 제공 실물 양식(시설 12종 기계·14종 화학물질, 통학 도급 '통학', 당직 취약계층 등) 그대로 |

**데이터 모델** (sections.dept_info — 조사표 문서 내 저장):

```ts
type DeptEquip = { name: string; qty: number }
type DeptChem = { name: string; handled: boolean }
type DeptEtc = {
  acc3y_accident: number; acc3y_other: number      // 최근 3년 재해(사고성/기타)
  env_measure: boolean                              // 작업환경측정 대상
  permit: boolean; permit_types: string[]           // 작업허가(고소·정전·밀폐공간)
  contractor: boolean; contractor_types: string[]; contractor_etc: string
  vulnerable: boolean; vulnerable_types: string[]   // 취약계층(60세 이상 등)
  reviewed: boolean                                 // 부서 검토 완료
}
type DeptInfo = { equips: DeptEquip[]; chems: DeptChem[]; etc: DeptEtc }
// sections.dept_info: Record<부서, DeptInfo> (올해) · sections.dept_info_prev: 작년(비교 기준)
```

**롤백 방법**: ① Risk.tsx에서 DeptHazardInfo import·`dept` 탭 항목·렌더 블록 삭제, SurveyTabKey에서 'dept' 제거, 기본 탭 'safety' 복원 ② `src/features/risk/DeptHazardInfo.tsx` 삭제. 저장된 dept_info 데이터는 문서에 남지만 화면에 표시만 안 됨(무해).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(시설 부서 — 사다리 3 등 12종 프리필, 화학물질 14종 취급 체크, 검토 완료 0/5 진행률).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 기존 6개 서브탭·평가 생성 플로우 변경 없음.

**보완 (같은 날)**:
1. 부서 헤더의 "근로자수 N명 (학교 대장 연동)" 표기 제거 — 유해위험정보는 근로자 수와 무관하다는 피드백 반영. 대장 연동은 MSDS 물질 제안·산재 자동 집계에만 사용.
2. MSDS 제안을 **부서별로 다르게** 표시 — 대장 MSDS의 구역(area)을 부서로 매핑(급식실→급식, 시설창고→시설 등, 키워드 기반 `AREA_PART_KEYWORDS`)해 해당 부서 탭에만 그 구역 물질을 제안. 매핑 불가 구역은 전 부서 공통 제안. 제안 칩에 구역명 병기, 유해화학물질 섹션·제안 박스에 법적 근거(「산업안전보건법」 제110·114조 MSDS 작성·게시·비치) 표기.

---

## [006] 2026-08-06 — 위험성평가 2단계 "청취조사 (개인별)" 화면 신설

**요청**: 청취조사에 의한 유해·위험요인 조사표를 모든 작업(부서) × 종사자 개인별로 작성하는 화면 — 실물 양식(실시방법·수행자/근로자/실시일·경험담 1~3·원인과 판단·개선 조건) 디지털화, 작년 작성분 프리필.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/HearingSurvey.tsx` | **신규** — 좌측: 부서별 종사자 명단(학교 대장 인원수 연동 자동 생성 + '인원 추가', 작성 완료 체크·부서별/전체 진행률, 작년 작성분 '작년' 배지) · 우측: 개인별 조사표(실시방법 고정 문구, 수행자=로그인 사용자 기본값, 경험담 1~3 각 서술+원인·판단, 육하원칙 가이드, 근로자 의견/수행자 의견). 작년 시트는 실시일·수행자만 올해 기준으로 갱신해 프리필 |
| `src/pages/Risk.tsx` | SurveyData에 `hearing`·`hearing_prev` 필드 추가, 조사표 서브탭 두 번째에 "② 청취조사 (개인별)" 추가, HearingSurvey 렌더 연결. 저장은 기존 PUT /risk/survey 문서 — 백엔드 계약 변경 없음 |
| `mock-server.mjs` | s01 hearing_prev 시드 — 실물 양식 샘플(김복자: 급식실 후문 계단 미끄러짐 경험담 + 미끄럼방지조치·안전난간 의견, 박정순: 국솥 화상 위험 경험담) |

**데이터 모델** (sections.hearing):

```ts
type HearingExp = { text: string; cause: string }   // 경험담 서술 + (원인·판단)
type HearingSheet = {
  part: string; worker_name: string; surveyor: string; date: string
  exps: HearingExp[]           // 경험담 1~3
  worker_opinion: string       // 근로자 의견 — 유해·위험 경험의 원인과 판단
  improve_cond: string         // 수행자 의견 — 개선에 대한 조건
  done: boolean                // 작성 완료
}
// sections.hearing: Record<`부서-번호`, HearingSheet> · sections.hearing_prev: 작년
```

**롤백 방법**: ① Risk.tsx에서 HearingSurvey import·'hearing' 탭 항목·렌더 블록 삭제, SurveyTabKey에서 'hearing' 제거 ② `src/features/risk/HearingSurvey.tsx` 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(급식 3·시설 1·미화 1·통학 2·당직 1 명단 자동 생성, 김복자·박정순 작년 경험담 프리필, 작성 완료 0/8 진행률).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 1단계(유해위험정보)·기존 서브탭·평가 생성 플로우 변경 없음.

---

## [007] 2026-08-06 — 위험성평가 3단계 "[정기평가] 위험성평가표 + 감소대책(3-3·3-4)" 화면 신설

**요청**: 공정(급식·당직·미화·통학)별 정기평가 위험성평가표를 점검자가 빈 표에 직접 작성. 가능성×중대성=위험성 자동 계산, **위험성 8점 이상이면 불안전한 행동(3-3)·불안전한 상태(3-4) 감소대책을 작업별로 작성**, 불안전한 상태는 개선 전·후 사진 포함.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/AssessmentTable.tsx` | **신규** — 공정(부서) 탭 × 3개 표. ⑴ 3-2 평가표: 연번·세부작업명·위험분류(선택)·법적근거·위험발생 상황·현재 조치·가능성(1~5)·중대성(1~5)·**위험성 자동 계산+라벨**(≤6 낮음/8 보통/9~12 약간 높음/15↑ 높음, 색상 구분)·감소대책·개선후 위험성·개선예정일·완료일·담당자. 8점 이상 행 노란 강조 + 상단 경고 배너. ⑵ 3-3 불안전한 행동: 단위작업명·재해형태·감소대책·조치결과·조치일자·교육·비고. ⑶ 3-4 불안전한 상태: +조치기한·담당자·완료일·**개선 전/후 사진**(파일명 메타 — 0709 회의 관례). 상단에 1·2단계(유해위험정보·청취조사) 내용 참고 패널(접기식) |
| `src/pages/Risk.tsx` | SurveyData에 `assess`·`reduce_behavior`·`reduce_state` 필드 추가, 서브탭 "③ 위험성평가표·감소대책" 추가, AssessmentTable 렌더 연결. 저장은 기존 PUT /risk/survey 문서 — 백엔드 계약 변경 없음 |

**데이터 모델** (sections.assess / reduce_behavior / reduce_state — 모두 `Record<공정, Row[]>`):

```ts
type AssessRow = { id, task, factor_class, legal_basis, situation, measure_current,
  likelihood, severity, /* 위험성=likelihood×severity 자동 */ reduction, after_risk, plan_date, done_date, owner }
type BehaviorRow = { id, task, accident_type, measure, result, result_date, education, note }
type StateRow = { id, task, accident_type, detail, result, due, owner, done_date, photo_before, photo_after }
```

**롤백 방법**: ① Risk.tsx에서 AssessmentTable import·'assess' 탭·렌더 블록·assessPart state 삭제, SurveyTabKey에서 'assess' 제거 ② `src/features/risk/AssessmentTable.tsx` 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(2×3=6 낮음/3×3=9 약간 높음 자동 계산·행 강조·8점 이상 경고 배너·3-3/3-4 표·사진 첨부 버튼).

**영향 범위**: 위험성평가(/risk) 조사표 탭만. 사진은 파일명 메타만 저장(실파일 업로드는 백엔드 연동 시).

---

## [008] 2026-08-06 — 위험성평가를 "정기 위험성평가 결과보고서" 체계로 재구성

**요청**: 최종 산출물인 정기 위험성평가 결과보고서(실물 PDF 21쪽)를 기준으로 화면 재구성 — 탭을 채우면 보고서가 채워지는 구조. 구 6탭(안전·보건/종사자 의견청취/안전·보건 자료/위험요인 파악/학교 순회/안전검사 대상 기계·기구)은 제거.

**보고서 ↔ 화면 매핑**:

| 보고서 | 화면 탭 | 비고 |
|---|---|---|
| 표지·PART 1 (학교 현황·재해발생 현황) | ① 학교 현황 | **신규** ReportInfo |
| PART 2 (실시규정·근거) | — | 고정 텍스트 (보고서 출력 시 자동 삽입 예정) |
| 3-1 유해위험정보 | ② 유해위험정보 | 기존 [005] |
| (작성 근거 자료) | ③ 청취조사 | 기존 [006] |
| 3-2 평가표 · 3-3/3-4 감소대책 | ④ 평가표·감소대책 | 기존 [007] |
| 3-5 참여자 명단 | ⑤ 참여자 명단 | **신규** Participants |

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/ReportInfo.tsx` | **신규** — 1-1 학교 현황(학교명 자동, 학교장·소재지·현업종사원 수는 학교 정보·대장 프리필 후 수정 가능, 업종·구성원·평가기간·평가자·작성 연월) + 1-2 재해발생 현황(최근 3년, 계·재해율 자동 계산) |
| `src/features/risk/Participants.tsx` | **신규** — 3-5 참여자 서명지(소속공정·성명·시작회의/결과교육 일시·서명 체크·비고). "청취조사 명단 불러오기"(올해 명단 없으면 작년 명단 폴백) |
| `src/pages/Risk.tsx` | 탭 재구성: ①~⑤ 보고서 목차 순서. **구 6탭 렌더 블록 전체 삭제(121행)**, SurveyTabKey 정리, 기본 탭 ①. 페이지 탭·카드 제목을 "정기 위험성평가 (보고서 작성)"/"정기 위험성평가 결과보고서 작성"으로 변경. SurveyData에 report_info·participants 추가 |

**롤백 방법**: [008] 이전 Risk.tsx로 복원(구 6탭 블록 포함 — 원본은 인수 시점 소스 기준) + ReportInfo.tsx·Participants.tsx 삭제. 구 6탭의 데이터 필드(policy 등)는 SurveyData 타입·저장 문서에 그대로 남아 있어 복원 시 데이터 손실 없음.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(① 학교장·소재지·종사원 8명 자동 프리필, 재해율 자동 계산 / ⑤ 작년 청취 명단 2명 불러오기).

**영향 범위**: 위험성평가(/risk)만. "학교별 평가"(평가 생성·점수 입력) 기존 플로우는 변경 없음.

**후속 예정**: 보고서 출력(표지·PART 2 고정 텍스트 포함 인쇄/PDF 미리보기) — 별도 요청 시 진행.

---

## [009] 2026-08-06 — "보고서 출력" — 정기 위험성평가 결과보고서 인쇄/PDF 생성

**요청**: ①~⑤ 탭에서 작성한 데이터를 표지·경영방침·PART 2 고정 텍스트와 합쳐 실물 보고서(A4 가로)와 같은 모양의 인쇄용 화면으로 조립 — 브라우저 인쇄 → "PDF로 저장"으로 최종 산출물 생성.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/RiskReport.tsx` | **신규** — 인쇄 미리보기 오버레이(document.body 포탈). 페이지 구성: 표지(학교명·연도·결재란 담당/행정실장/교장·작성 연월·물결 그래픽) → 안전보건 경영방침(전라남도교육청 고정) → 목차 → PART 1(①탭 데이터) → PART 2 3쪽(실시규정 제1~15조·역할표·표3 가능성·표4 중대성·표5 위험성 매트릭스 색상·표6 허용여부·2-2 실시근거 — 고정 텍스트) → 3-1 부서별 유해위험정보(②탭, 실물 양식 재현: 점선 행·V 체크·■/□) → 3-2 위험도 산출 기준 노트 + 공정별 평가표(④탭, 위험성 노란 강조) → 3-3/3-4 감소대책(사진 파일명 표기) → 3-5 참여자 명단(⑤탭, 최소 10행) |
| `src/styles/riskreport.css` | **신규** — rr- 접두사. A4 가로 페이지(1123×794px), `@page size A4 landscape`, 인쇄 시 앱 UI(#root) 숨김·페이지 분리(page-break) |
| `src/pages/Risk.tsx` | 카드 헤더에 "보고서 출력" 버튼 추가(학교 선택 시 활성), RiskReport 렌더 연결 |

**사용법**: 위험성평가 → 학교 선택 → 정기 위험성평가(보고서 작성) → 탭 작성 → **보고서 출력** → [인쇄 / PDF 저장] → 대상 "PDF로 저장" (배경 그래픽 인쇄 켜기 권장).

**롤백 방법**: Risk.tsx에서 RiskReport import·버튼·렌더 블록·reportOpen state 삭제 + RiskReport.tsx·riskreport.css 삭제.

**검증**: `tsc --noEmit` 통과 · 화면 미리보기 확인 · Playwright print-media PDF 생성 테스트(A4 가로 14쪽 — 표지·PART 1 데이터 반영·3-1 체크박스 재현 확인).

**영향 범위**: 위험성평가(/risk)만. 데이터 미작성 섹션은 자동 생략(평가표 없으면 안내 문구).

---

## [010] 2026-08-06 — `src/pages/Risk.tsx` — 학교별 평가 화면을 "작성된 보고서" 중심으로 재구성

**요청**: 학교 컨텍스트 화면에서 "표준 마스터 시드"·"평가 생성" 버튼 제거, 대신 이때까지 작성된 정기 위험성평가 보고서가 보이도록.

**변경**:
- 상단 "표준 마스터 시드"·"평가 생성" 버튼 제거 (평가 생성 모달 코드는 잔존하나 미노출 — 수시 평가는 산업재해 연동으로 생성).
- 학교 컨텍스트 최상단에 **"정기 위험성평가 보고서" 카드** 신설: GET /risk/survey로 해당 학교의 보고서 문서를 조회해 "{연도}년 정기 위험성평가 결과보고서 · 최근 저장일 · 위험성 8점 이상 N건" + 섹션별 진행 칩(① 학교 현황 ✓/－, ② 유해위험정보 n/5부서, ③ 청취조사 n/N명, ④ 평가표 N건, ⑤ 참여자 N명 — 완료 시 초록). 버튼: **[이어서 작성]**(보고서 작성 탭으로 이동) · **[보고서 출력]**(인쇄 미리보기 바로 열기). 미작성 시 "보고서 작성 시작" 안내.
- 기존 KPI(평가 수·총 불완전)와 "연도별 평가" 목록은 유지하되 부제 "수시(산재 연동) 포함 이력"으로 성격 명시, 빈 상태 문구에서 '평가 생성' 언급 제거.

**롤백 방법**: 보고서 카드 블록(즉시실행함수)·ctxReport state/effect 삭제, 상단 버튼 블록 복원:

```tsx
<button className="btn btn-ghost" disabled={busy} onClick={seedMaster}>표준 마스터 시드</button>
{sel && (
  <button className="btn btn-primary"
    onClick={() => { setProcess('catering'); setGenCategory('regular'); setActionErr(''); setOpen(true) }}>
    평가 생성
  </button>
)}
```

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(보고서 카드에 ① 완료 칩·최근 저장일·이어서 작성/보고서 출력 버튼, 연도별 평가 이력 유지).

**영향 범위**: 위험성평가(/risk) 학교별 평가 화면만.

---

## [011] 2026-08-06 — `src/pages/Risk.tsx` — 학교 컨텍스트에서 KPI·"연도별 평가" 이력 전부 제거

**요청**: [010]에서 유지했던 KPI 카드(평가 수·총 불완전)와 "연도별 평가" 목록(구 백엔드 평가 플로우 이력)의 존재 이유 질의 → **전부 제거** 결정. 학교 컨텍스트에는 "정기 위험성평가 보고서" 카드만 남김.

**변경**: 학교 컨텍스트 렌더에서 kpis 블록 + 연도별 평가 ledger 블록 + 하단 안내 문구(총 40행) 삭제. 관련 계산 로직(selItems·selUnsafe·yearGroups·renderGroup 등)과 데이터 로드는 코드에 잔존(다른 화면 집계·롤백 대비, 미표시일 뿐).

**유의**: 산업재해 → "수시 위험성평가 생성"으로 만들어진 건을 이 화면에서 볼 수 없게 됨(데이터는 서버에 계속 저장되고, 경영 대시보드·학교 상세의 5대 업무 현황 집계에는 계속 반영됨). 수시 평가 이력 표시가 다시 필요해지면 [010] 형태로 복원 가능.

**롤백 방법**: [010] 기록의 kpis + 연도별 평가 ledger 블록을 보고서 카드 아래에 복원.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(보고서 카드 단독 표시).

---

## [012] 2026-08-06 — 수시 위험성평가 프로세스 신설 (사고성 산업재해 연동)

**요청**: 사고성 산업재해 발생 시 프로세스 추가 — 산업재해 발생 → ① 산업재해조사표 → ② 재해 재발방지계획 → ③ 수시위험성평가 → ④ 수시위험성평가 교육 → 완료. 실물 양식 4종(산업재해조사표 별지 제30호서식·재해재발방지계획·수시위험성평가 보고서·안전보건교육일지) 기반.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/risk/AdhocAssessment.tsx` | **신규** — 학교 컨텍스트의 "수시 위험성평가" 섹션. ⑴ 목록: 프로세스 안내 + 이 학교의 사고성 산재(kind=accident) 중 미착수 건을 빨간 배너로 표시("수시평가 시작" → 사고 정보 프리필로 케이스 생성), 케이스별 4단계 진행 칩·완료 배지, 수동 등록 버튼. ⑵ 에디터(스텝퍼): ① 조사표(재해자 정보·상해종류/부위·휴업일수·발생 개요/원인·재발방지계획·작성자 — 별지 30호 핵심 항목), ② 재발방지계획(재해 개요 자동 연동 + 원인 분석·대책), ③ 수시평가표(세부작업·상황·가능성×중대성 자동 위험성·감소대책 행 + 감소대책 실행), ④ 교육일지(교육구분 고정·인원·일시/장소/강사·주제/내용/자료·사진 파일명·참석자 명단 서명 체크). 단계별 완료 토글 → 4단계 완료 시 케이스 마무리 |
| `src/pages/Risk.tsx` | SurveyData에 `adhoc_cases` 추가, 학교 컨텍스트에서 정기 보고서 카드 아래 AdhocSection 렌더. 케이스 변경은 ctxReport 문서 상태에 반영, 저장은 PUT /risk/survey(saveCtxDoc) — 백엔드 계약 변경 없음 |

**프로세스 매핑**: 산업재해 메뉴에 사고 등록 → 위험성평가 학교 화면에 자동으로 "수시평가 시작" 배너 노출 → 4단계 순차 작성 → ④ 교육 완료로 마무리 (수시평가는 교육 진행으로 종결된다는 요구 반영).

**롤백 방법**: Risk.tsx에서 AdhocSection import·렌더·setCtxCases/saveCtxDoc/ctxBusy 삭제, SurveyData에서 adhoc_cases 제거, AdhocAssessment.tsx 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(미착수 산재 배너 → 케이스 생성 → 조사표 프리필·스텝퍼 동작).

**영향 범위**: 위험성평가(/risk) 학교 컨텍스트만. **후속 후보**: 수시 보고서 인쇄 출력(정기 RiskReport와 같은 방식, 초록 테마 13쪽 양식) — 요청 시 진행.

---

## [013] 2026-08-06 — 학교 컨텍스트: 정기 보고서·수시 평가 좌우 2단 배치

**요청**: 정기 아래 수시를 세로로 쌓는 배치에 대한 검토 → 좌우 2단 배치 선택.

**변경**:
- Risk.tsx: 학교 컨텍스트의 정기 보고서 카드 + AdhocSection을 `grid-template-columns: 1fr 1fr` 그리드로 감쌈 (1920px 기준 두 카드가 스크롤 없이 한눈에, 높이 stretch).
- AdhocAssessment.tsx: 수시 케이스 편집 화면은 `gridColumn: 1 / -1`로 전체 폭 사용. 목록 헤더에 미착수 사고 건수 빨간 강조 배지 추가.

**롤백 방법**: 그리드 wrapper div 제거 + 카드 marginBottom 18 복원 + gridColumn·미착수 배지 제거.

**검증**: `tsc --noEmit` 통과 · 1920px 렌더링 확인(2단 정렬·미착수 1건 배지).

---

## [014] 2026-08-06 — 학교 컨텍스트: 정기/수시 전환 버튼 + 전체 폭 리스트 (사용자 구상안 반영)

**요청**: 사용자 제공 구상 이미지 반영 — 좌우 2단([013]) 대신 상단 우측에 [정기 위험성평가] [수시 위험성평가] 전환 버튼을 두고, 아래에는 선택된 유형의 리스트를 전체 폭으로 표시.

**변경** (`src/pages/Risk.tsx`):
- 학교 헤더(rkh-schoolhead) 우측에 전환 버튼 2개 추가 — 활성 버튼은 btn-primary(보라). 수시 버튼에는 미착수 사고 건수 빨간 배지("미착수 N") 표시.
- `ctxTab`('regular'|'adhoc') 상태 신설 — 학교 진입 시 '정기'로 초기화. 미착수 집계용 산재 목록(ctxAccidents) 별도 조회.
- [013]의 2단 그리드 제거 → 선택된 유형만 전체 폭 렌더 (정기 보고서 카드 ↔ 수시 AdhocSection).

**롤백 방법**: [013]의 그리드 wrapper 복원, ctxTab·ctxAccidents·전환 버튼 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(정기/수시 전환·미착수 1 배지·전체 폭 리스트).

---

## [015] 2026-08-06 — `src/pages/Risk.tsx` — 상단 버튼 정리: [정기]/[수시] 2개로 통합

**요청**: 버튼 5개 중복 정리 — '학교별 평가' 탭 제거, '정기 위험성평가(보고서 작성)' 탭과 '정기 위험성평가' 전환 버튼을 하나로 합침. 최종적으로 학교 헤더 우측의 [정기 위험성평가] [수시 위험성평가] 두 버튼만 유지.

**변경**:
- 페이지 상단의 글로벌 탭 2개('학교별 평가'·'정기 위험성평가(보고서 작성)')와 조사표용 학교 선택 드롭다운 제거.
- `pageTab` state → `regularEdit`(boolean)로 대체: 보고서 작성 화면(①~⑤ 탭)은 이제 **정기 카드의 "이어서 작성/보고서 작성 시작" 버튼으로 진입**하는 인라인 편집 모드. 학교는 선택된 학교로 자동 지정(드롭다운 불필요), 편집 화면 헤더에 "← 보고서 개요" 복귀 버튼 추가.
- 학교 목록/컨텍스트 렌더 조건을 `!sel` / `sel` 기준으로 단순화. 학교 변경·[정기] 버튼 클릭 시 편집 모드 해제(개요 카드 표시).

**롤백 방법**: pageTab state·글로벌 탭 블록·survey 학교 select 복원, regularEdit 관련 조건 원복 ([014] 이전 구조).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(버튼 2개만 표시, 이어서 작성 → 인라인 ①~⑤ 편집 + 학교 자동 지정 + 복귀 버튼).

**영향 범위**: 위험성평가(/risk) 내비게이션 구조. AppShell 사이드바에서 위험성평가 진입 시 곧바로 학교 목록이 보이는 단일 흐름이 됨: 학교 목록 → 학교 → [정기|수시] → (정기) 개요 카드 ↔ 작성 화면 / (수시) 케이스 목록 ↔ 4단계 에디터.

---

## [016] 2026-08-06 — `src/pages/Risk.tsx` — 정기 카드를 "위험성평가 보고서 리스트"로 전환

**요청**: 헤더의 "보고서 작성 시작" 버튼 제거, 단일 "정기 위험성평가 보고서" 카드 대신 보고서 리스트 형태로.

**변경**: 정기 뷰를 테이블 리스트로 재구성 — 컬럼: 보고서명(연도) / 진행 상황(①~⑤ 섹션 칩 + 8점 이상 경고) / 상태(미작성·작성 중·작성 완료 — 5개 섹션 완성 여부로 자동 판정) / 최근 저장 / 작업([작성 or 이어서 작성]·[보고서 출력]). 행 클릭 = 작성 화면 진입. 헤더 버튼 제거, 우측에 "연 1회 정기 실시 · 연도별 1부" 안내. 하단에 연도 누적 안내 문구(해가 바뀌면 새 연도 행 추가·작년분 자동 프리필).

**참고**: 현재 저장 구조는 학교당 보고서 문서 1부(당해 연도)라 리스트에 당해 연도 행 1개가 표시됨. 과년도 보고서 보관(연도별 아카이브)이 필요해지면 문서에 `report_archive` 섹션을 추가해 같은 리스트에 쌓이도록 확장 가능 — 백엔드 계약 변경 없이 가능.

**롤백 방법**: [015] 시점의 카드형 블록으로 IIFE 내부 JSX 교체.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(리스트 행·상태 미작성·[작성] 버튼·행 클릭 진입).

---

## [017] 2026-08-06 — 보고서 작성 탭에 "다음 →" 이동 버튼 추가

**요청**: 작성 탭 하단 — 저장 버튼은 왼쪽에 두고, 오른쪽에 "다음" 버튼으로 다음 단계로 이동.

**변경**: ReportInfo·DeptHazardInfo·HearingSurvey·AssessmentTable 4개 컴포넌트에 `onNext` 옵션 prop 추가 — 하단 푸터 우측 끝(`marginLeft: auto`)에 "다음 →" 버튼 렌더. Risk.tsx에서 ①→②→③→④→⑤ 순서로 연결 (⑤ 참여자 명단은 마지막이라 다음 버튼 없음).

**롤백 방법**: 4개 컴포넌트의 onNext prop·버튼과 Risk.tsx의 onNext 연결 4곳 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(① 하단 우측 다음 버튼 → 클릭 시 ② 탭 전환).

**보완 (같은 날)**: 탭 라벨의 괄호 표기 제거 — "① 학교 현황 (PART 1)" → "① 학교 현황" 등 5개 탭 모두 간결하게.

---

## [018] 2026-08-06 — 이행점검을 "점검 조사지" 중심으로 전면 재구성

**요청**: 이행점검 프로세스 반영 — 교육청 공문 하달(상·하반기 각 1회) → 중대재해예방 의무이행사항 점검 조사지 작성(내용: 점검자 직접 작성) → **학교 행정선생님 면담 후 결과 체크** → 학교 메일로 제출. 실물 조사지(항목/내용/결과 3컬럼, 11개 항목) 기반.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/features/compliance/ComplianceSheet.tsx` | **신규** — ⑴ 조사지 폼: 11개 항목(성명 확인·관리감독자 교육·위험성평가·예산집행실적·산업재해·홈페이지 게시판·매뉴얼 교육·안전보건관리계획서·안전보건협의체·보호구 지급대장·안전보건경영방침)을 항목/내용(점검자 작성, 표준 문구 프리필)/결과(면담 후 체크) 3컬럼으로. 결과 위젯: O/X 토글·월 입력(원격 4월/집합 5월)·금액 입력(개선비 등)·날짜 입력(교육일·산재발생일)·성명 3인. 상단 면담 정보(공문 접수일·면담 대상 행정선생님·면담일) + "결과는 행정선생님 면담 후 작성" 안내 고정. ⑵ 인쇄 미리보기: A4 세로 1쪽 실물 양식 재현(인쇄→PDF 저장, 메일 첨부용) |
| `src/pages/Compliance.tsx` | **전면 재작성** — 학교 목록(상·하반기 상태 컬럼) → 학교별 조사지 리스트(반기별 행: 공문 접수일·면담·결과 기입 진행도 n/8·상태·제출일·작업[작성/인쇄/메일 제출]) → 조사지 작성 화면. **[학교 메일로 제출]**: 학교 이메일로 메일 앱 열림(제목·본문 자동 완성) + 제출일 기록·상태 '제출 완료'(해제 가능). 저장은 `/ops/docs/compliance-sheets` 문서(학교별×반기별) — 백엔드 계약 파일 수정 없음 |

**구 화면 대체**: 기존 이행점검 화면(학교별 연도·반기 이력 위계 뷰 + 항목 체크 모달, /compliance API 기반)은 조사지 체계로 대체. 서버의 기존 /compliance 데이터는 보존되나 화면에는 미표시(롤백 시 인수 시점 Compliance.tsx 원본으로 복원).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(조사지 리스트 상·하반기 2행 → 작성 화면 3컬럼·O/X 토글·프리필 문구).

**영향 범위**: 이행점검(/compliance)만. 홈 간트·대시보드의 이행점검 집계는 기존 /compliance API 기준이므로 조사지 제출 상태와는 별개(연동 필요 시 후속).

---

## [019] 2026-08-06 — `src/pages/Compliance.tsx` — 조사지를 "작성 버튼으로 생성"하는 방식으로 변경

**요청**: 상·하반기 행을 미리 만들어두는 방식에 대한 문제 제기(2027년 이후 연도 처리 불명확) → [조사지 작성] 버튼으로 그때그때 생성하는 전략 채택.

**변경**:
- 리스트에 당해 연도 상·하반기 행을 가상으로 미리 표시하던 로직 제거 — **실제 작성된 조사지만** 연도 역순으로 표시. 빈 상태: "교육청 공문이 내려오면 '조사지 작성'으로 시작하세요."
- 리스트 헤더에 **[+ 조사지 작성]** 버튼 → 생성 모달(연도 기본 올해, 반기 기본 현재 월 기준 자동, 공문 접수일 입력). 같은 반기 중복 생성 방지. 생성 즉시 작성 화면으로 이동.
- 연도 처리: 2027년이 되면 버튼으로 "2027년 상반기"를 생성 — 2026년 조사지들은 리스트에 이력으로 계속 남음. 학교 목록의 상·하반기 상태 컬럼은 당해 연도 기준 유지(미생성 = 미작성).

**롤백 방법**: periodKeys에 CUR_PERIODS 병합 복원, 생성 모달·버튼 제거 ([018] 형태).

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(빈 리스트 → 생성 모달 → 2026 하반기 행 생성·작성 중 상태).

**보완 (같은 날)**: [조사지 작성] 버튼 위치를 위험성평가와 동일하게 학교 헤더(rkh-schoolhead) 우측·리스트 카드 위로 이동. 리스트 헤더에는 프로세스 안내 문구만 유지.

---

## [020] 2026-08-07 — 학교 허브(/schools) 상단 KPI 카드를 검색·필터 패널로 교체

**요청**: 학교 화면 상단의 통계 카드 3개(전체 학교·현업종사자 합계·인원 불일치) 영역을 학교명·지역명 검색 + 초/중/고 선택 조회 영역으로 변경.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | KPI 카드 3개(`.kpis` 블록) 제거 → **검색 패널**(`.shub-search`) 신설: 검색 인풋(학교명·지역명, 지우기 X 버튼) + 학교급 세그먼트 버튼(전체·유·초·중·고·기타) + 인원대조 드롭다운 + 결과 건수 표시. 검색 대상 `HUB_SEARCH`를 학교명+주소(지역명)로 변경(구: 학교명·담당자·학교장). 목록 헤더의 중복 `<FilterBar>` 제거 — 검색·필터는 상단 패널로 일원화(기존 useTableQuery 훅 그대로 소비, 로직 변경 없음) |
| `src/styles/schoolhub.css` | `.shub-search`·`.shub-search-input`·`.shub-seg`·`.shub-search-hint` 스타일 추가 (shub- 접두사 관례, tokens.css 변수 재사용 — 다크모드 자동 호환) |

**변경 전** (롤백용 — SchoolsHub.tsx 구 KPI 블록):

```tsx
<div className="kpis">
  <div className="kpi"><div className="l">전체 학교</div><div className="v">{rows.length}<small> 교</small></div><div className="d">등록된 학교(기관)</div></div>
  <div className="kpi"><div className="l">현업종사자 합계</div><div className="v">{totalWorkers}<small> 명</small></div><div className="d">5대 파트 전체</div></div>
  <div className={'kpi ' + (mismatchCount ? 'warn' : '')}><div className="l">인원 불일치</div><div className="v">{mismatchCount}<small> 교</small></div><div className="d">종사자수 vs 교육생수</div></div>
</div>
```

```tsx
const HUB_SEARCH = [
  (r: Row) => r.school.name,
  (r: Row) => r.school.manager ?? '',
  (r: Row) => r.school.principal ?? '',
]
```

(목록 헤더에는 `<FilterBar q={q} />`가 있었고, totalWorkers·mismatchCount 계산 2줄이 있었음)

**롤백 방법**: 위 변경 전 블록 복원(kpis·HUB_SEARCH·FilterBar·계산 2줄) + 신규 `.shub-search` JSX 블록·schoolhub.css의 "상단 검색 패널" 스타일 블록 삭제. git 기준으로는 커밋 `93c08eb` revert.

**검증**: `tsc --noEmit` 통과 · 목서버 연동 렌더링 확인 — 지역명 "경기도" 검색 3교, 학교급 [초] 필터 4교, 결과 건수 실시간 표시, 지우기 버튼 동작.

**영향 범위**: 학교 허브(/schools)만. KPI 수치(현업종사자 합계 등)는 화면에서 사라짐 — 필요 시 경영 대시보드에서 확인(복원 요청 가능). 정렬·CSV 내보내기·표/카드 토글·페이지네이션은 변경 없음.

**보완 (같은 날)**: 사용자 구상안 반영 — 통합 검색창 1개를 **[학교명]·[지역명] 라벨 분리형 입력창 2개**로 교체 (`.shub-search-input` → `.shub-field`, 라벨 박스 + 입력 결합형). 학교명은 학교(기관)명만, 지역명은 주소만 각각 부분일치 검색하며 둘 다 입력 시 AND 조건. 검색은 useTableQuery 밖에서 rows를 선필터링(useMemo)하고 level·인원대조 필터는 기존 훅 그대로 사용, 검색어 변경 시 1페이지로 리셋. 검증: `tsc --noEmit` 통과 · 렌더링 확인(지역명 "서울" × 학교급 [중] → 2교).

**보완 2 (같은 날)**: 인원대조 드롭다운·"등록/검색 결과 N교" 표시 제거, 우측에 **[조회] 버튼** 추가 — 검색이 입력 즉시가 아니라 [조회] 클릭(또는 입력창 Enter)으로 실행되는 확정형으로 변경(`applied` state). 학교급 버튼은 즉시 반영 유지, X 지우기는 해당 조건 즉시 해제. 인원대조 필터는 UI에서 제거(테이블 인원대조 컬럼·정렬은 유지, HUB_FILTERS에서 mismatch 정의 삭제). 검증: `tsc --noEmit` 통과 · 렌더링 확인(학교명 "초등" × 지역명 "서울" 조회 → 2교).

---

## [021] 2026-08-07 — 학교 허브: 담당/전체 토글 + 5대 업무 바로가기 버튼(상태 배지)

**요청**: 전략 논의 결과 반영 — 학교 탭을 "일하러 들어가는 입구"로. 표/카드 토글을 **[담당 학교] [전체 학교]** 토글로 교체하고, 학교 행마다 5대 업무(안전점검·위험성평가·근골격계·교육·이행점검) 바로가기 버튼을 상태 배지와 함께 배치. 버튼 클릭 → 해당 업무 페이지의 학교 컨텍스트로 즉시 진입(행 클릭은 기존대로 학교 상세).

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | ⑴ 표/카드 토글·카드형 보기 제거 → 담당/전체 토글(담당 배정 있으면 담당 기본, 없으면(관리자) 전체 기본 — `assigned_inspector_id === user.login` 기준). ⑵ "업무 바로가기" 컬럼 신설 — 학교별 상태 배지 칩 5개, 클릭 시 `?school=id`로 해당 업무 페이지 이동. 상태 조회: 학교별 inspections·risk·musculo 3개 API + 이행점검 조사지 문서(/ops/docs/compliance-sheets) 1회 병렬 조회. ⑶ 컬럼 정리: 관리감독자·안전점검기관 컬럼 제거(공간 확보 — CSV 내보내기에는 유지, 학교 상세에서 확인 가능) |
| `src/pages/Inspection.tsx`·`Risk.tsx`·`Musculo.tsx`·`Education.tsx` | `?school=id` 딥링크 — 학교 목록 로드 후 해당 학교로 `openSchool()` 자동 호출 (useSearchParams, 각 4~10행 추가) |
| `src/pages/Compliance.tsx` | 동일 딥링크 — `setSel(s)` 방식 |
| `src/styles/schoolhub.css` | `.shub-works-cell`·`.shub-w` 칩 스타일 추가 (상태색 점 + 경고/위험 시 테두리 강조) |

**상태 배지 규칙** (TodayHero.deriveTasks 규칙 재사용):
- 안전점검: 이번 달 제출/서명 있음 → 완료(초록) / 없음 → 이번 달 미실시(주황)
- 위험성평가: status≠completed N건 → 진행 N건(보라) / 전부 완료 → 완료(초록) / 없음 → 미작성(회색)
- 근골격계: needs_review 합 N>0 → 검수 N건(빨강) / 0 → 특이사항 없음(초록)
- 교육: 대장 인원대조 기준 — 불일치(주황) / 일치(초록)
- 이행점검: 당기 반기 조사지 — 제출 완료(초록) / 작성 중(보라) / 미작성(회색)
- 상세 상태는 칩 툴팁(title)으로 표시

**롤백 방법**: git 커밋 revert(단일 커밋). 카드형 보기 JSX·표/카드 토글은 [021] 이전 커밋에 보존. 각 업무 페이지의 딥링크 effect(주석 "학교 탭 바로가기")와 useSearchParams import 삭제로 페이지별 개별 롤백 가능.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인 — insp01 로그인 시 [담당 학교 4] 기본 선택·4교 표시, admin 로그인 시 [전체 학교] 기본, 한빛초 근골격계 빨간 배지(검수 1건)·위험성평가 버튼 클릭 → /risk 학교 컨텍스트(보고서 리스트) 자동 진입.

**영향 범위**: 학교 허브(/schools) + 5개 업무 페이지의 진입 동선. 각 업무 페이지의 기존 학교 목록·컨텍스트 화면 자체는 변경 없음(입구만 추가). 담당/전체는 보기 필터이며 접근 제한이 아님(전략 문서 기준).

**보완 (같은 날)**: ⑴ 업무 바로가기 컬럼을 **[담당 학교]에서만 표시** — 전체 학교는 조회용이므로 제거(요청 반영), colSpan 7/8 분기. ⑵ mock-server 시드 보완 — 데모 기본 계정(admin)에도 담당 학교 5교 배정(s01 한빛초·s04 샛별유치원·s06 해솔중·s10 한마음고·s12 바다초, 기존 insp01~03 배정 일부 이관)해 admin 로그인에서도 담당 학교 리스트가 채워지도록. 검증: admin 로그인 → [담당 학교 5] 기본·버튼 표시 / [전체 학교] 12교·버튼 컬럼 없음.

---

## [022] 2026-08-07 — `src/pages/Home.tsx` — 홈 "담당 학교" 표 섹션 제거

**요청**: 홈 하단의 담당 학교 표(학교명·학교급·담당자·당월 방문 + 검색/학교급 필터/학교 등록 버튼) 삭제 — 학교 목록 역할은 학교 탭([021] 담당/전체 토글·업무 바로가기)으로 일원화됨.

**변경**: `{/* ===== 4. 담당 학교 표 ===== */}` 주석부터 `<Pagination q={q} />` 포함 ledger 블록 전체(55행) 삭제. JSX만 제거 — 관련 로직(q 테이블 훅·visits·schoolModal state·학교 등록 모달 컴포넌트)은 코드에 잔존(미노출, 롤백 대비). 홈에서 학교 등록 진입점이 사라짐 — 학교 등록은 학교 탭의 [+ 학교 등록] 버튼 사용.

**롤백 방법**: git 커밋 revert 또는 [021] 시점 Home.tsx의 해당 블록을 캘린더 섹션 뒤에 복원.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(홈이 캘린더+이번 주 방문 예정+기한 알림으로 종료, "담당 학교" 텍스트 잔존 0).

**영향 범위**: 홈(/) 화면만.

---

## [023] 2026-08-07 — 업무 탭 5개 첫 화면을 "작성물 리스트"로 전환

**요청**: 안전점검·위험성평가·근골격계·교육·이행점검 탭의 첫 화면이 학교 목록이 아니라 **점검자들(+나)이 작성한 보고서/조사/조사지 리스트**가 되도록 — 전략 논의(학교 탭 = 입구, 업무 탭 = 작성물 검토·마감 관리) 반영.

**변경 파일** (각 페이지의 1단계 화면만 교체 — KPI 카드·학교 컨텍스트·작성 화면은 변경 없음, 행 클릭 = 해당 학교 컨텍스트 진입):

| 파일 | 첫 화면 | 데이터 |
|---|---|---|
| `Inspection.tsx` | "작성된 점검표" — 점검일·학교·담당자·공정·항목수·서명·상태·교육청 전송, 최신순, 상태·학교급 필터 | 기존 insMap 평탄화 (추가 API 없음) |
| `Risk.tsx` | "작성된 보고서" — 정기 보고서(섹션 n/5·8점 이상 N건·작성 중/완료) + 수시 케이스(단계 n/4), 구분 필터. 수시 행 클릭 시 수시 탭으로 진입 | 전 학교 GET /risk/survey 병렬 조회(docMap 신설). ctxTab 리셋을 effect→openSchool로 이동 |
| `Musculo.tsx` | "작성된 조사" — 조사일·학교·기본조사·증상조사표·부담작업·검수 대기, 부담작업·검수 필터 | 기존 surveyMap 평탄화 |
| `Education.tsx` | "교육 진도 기록" — 진도 데이터가 있는 학교만 표시(기록 없는 학교는 미표시) | 기존 schoolRows 필터 |
| `Compliance.tsx` | "작성된 조사지" — 반기·학교·결과 기입 n/8·상태·제출일, 상태 필터 | 기존 doc(조사지 문서) 평탄화 |
| `mock-server.mjs` | s01 조사표에 assess 2행 시드(리스트 확인용 샘플 — "작성 중 · 섹션 1/5 · 8점 이상 1건") | |

**설계 원칙**: 작성물 리스트는 "작성된 것"만 보여줌 — 미작성(누락) 관리는 학교 탭의 업무 바로가기 상태 배지([021])가 담당. 새 작성의 공식 입구도 학교 탭 바로가기(빈 상태 안내 문구로 유도).

**롤백 방법**: git 커밋 revert. 각 페이지의 구 학교 목록 로직(schoolRows·SCHOOL_QUERY·q)은 코드에 잔존(미렌더) — 페이지별로 ledger 블록 JSX만 이전 커밋 형태로 되돌리면 개별 복원 가능.

**검증**: `tsc --noEmit` 통과 · 5개 탭 렌더링 확인(점검표 7건 최신순·보고서 1건(시드)·조사 3건·진도 기록·조사지 빈 상태 안내) · 행 클릭 → 학교 컨텍스트 진입 확인.

**영향 범위**: 5개 업무 페이지의 첫 화면만. 학교 컨텍스트 이후 화면·작성 플로우·백엔드 계약 변경 없음.

**보완 (같은 날)**: 안전점검 탭 상단 KPI 카드 3개(학교 수·전체 점검·제출) 제거 — 첫 화면이 작성물 리스트로 바로 시작(요청 반영). totalCount·totalSubmitted 계산 로직은 잔존(롤백 대비). 이어서 위험성평가(학교 수·전체 평가·수시 평가)·근골격계(학교 수·전체 조사·검수 대기) 탭의 상단 KPI 카드도 동일하게 제거. 교육 탭 카드는 유지(별도 요청 없음). 또한 안전점검 학교 컨텍스트 헤더의 [점검 생성](빈 점검 생성 모달) 버튼 제거 — [점검표 작성] 버튼만 그 자리에 유지(생성 모달 코드는 잔존·미노출), 빈 상태 문구도 정리. 학교 컨텍스트의 KPI 카드 2개(점검 수·제출 수)도 제거.

---

## [024] 2026-08-07 — 안전점검 학교 컨텍스트: "점검 현황" 개칭 + [이어서 작성] 버튼

**요청**: "월별 점검 이력" 명칭을 "점검 현황"으로 변경, 위험성평가처럼 이어서 작성 버튼 추가.

**변경**:
- `Inspection.tsx`: 섹션 제목 변경. 월 그룹 표에 "작업" 컬럼 신설 — 작성중 건은 **[이어서 작성]**(→ `/inspection/new?school=&part=&resume=` 이동), 그 외는 [보기](상세 모달). 행 클릭 동작(상세 모달)은 유지.
- `InspectionForm.tsx`: `part`·`resume` 파라미터 지원 — part가 있으면 해당 공정만 점검대상으로 자동 선택, resume이 있으면 ⑴ 대상 점검의 기존 결과·비고를 폼에 프리필(코드 일치 항목만, 구 시드 ok/fix 값도 역매핑 수용) ⑵ 저장 시 새 점검을 만들지 않고 그 점검 ID에 이어서 기록.

**롤백 방법**: git revert. Inspection의 작업 컬럼·nav, InspectionForm의 partParam/resumeId 분기 제거.

**검증**: `tsc --noEmit` 통과 · 렌더링 확인(세종고 작성중(미화) 건 [이어서 작성] → 작성 폼 진입).

**영향 범위**: 안전점검 학교 컨텍스트·점검표 작성 폼. 백엔드 계약 변경 없음(기존 POST/PUT 엔드포인트 그대로).

**보완 (같은 날)**: 점검 현황의 월별 그룹(접기식 박스)을 없애고 **하나의 통합 테이블**(점검일 최신순 · 점검일 컬럼 첫 번째)로 병합. 월 그룹 관련 코드(monthGroups·renderGroup)는 잔존(미사용, 롤백 대비).

---

## [025] 2026-08-09 — 학교 허브(/schools) 목록 화면 정리 — 학교급 필터 확정형·인원대조 제거·대장 버튼

**요청**: ⑴ 학교급 버튼(전체~기타)을 눌러도 목록이 바로 바뀌지 않고 [조회] 클릭 시에만 반영 ⑵ 학교 목록의 인원대조 컬럼 제거 ⑶ [담당 학교] 토글 옆 숫자 배지 제거 ⑷ 학교명·지역명 입력창의 예시 placeholder 제거(빈칸) ⑸ 각 학교 행에 대장 바로가기 버튼 추가.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | ⑴ 학교급 선택을 `levelQ`(대기) state로 분리 — 버튼 클릭은 선택 표시만 하고, `doSearch()`([조회]/Enter)에서 `q.setFilter('level', levelQ)`로 확정 반영 (학교명·지역명과 동일한 확정형 UX로 통일). ⑵ 테이블의 인원대조 th·td 삭제 — 그 자리에 **[대장] 버튼 컬럼** 신설(행 클릭과 동일하게 `/schools/{id}` 학교 상세=대장 이동, BookOpen 아이콘, stopPropagation). 담당/전체 양쪽 스코프 모두 표시. CSV 내보내기(HUB_EXPORT)의 인원대조 항목과 교육 배지(인원 일치/불일치)는 유지 — 데이터 로직 변경 없음. ⑶ 담당 학교 토글 라벨에서 `{mineCount}` 숫자 제거(변수는 기본 스코프 판정·빈 상태 문구에 계속 사용). ⑷ 학교명·지역명 input의 placeholder 속성 삭제 |
| `src/styles/schoolhub.css` | `.shub-ledger` 대장 버튼 스타일 추가 (shub-w 칩과 동일 계열 — 알약형, 호버 시 보라 강조) |

**롤백 방법**: git 커밋 revert(단일 커밋). 개별 복원 시 — levelQ state 제거 후 학교급 버튼 onClick을 `q.setFilter('level', l)` 직접 호출로, 대장 컬럼 th·td를 인원대조 th·td로 교체, placeholder·mineCount 배지 문자열 복원, `.shub-ledger` 블록 삭제.

**검증**: `tsc --noEmit` 통과 · 목서버 연동 1920px 렌더링 확인 — [초] 클릭 시 목록 유지(5교) → [조회] 클릭 시 초등 2교로 필터, 인원대조 컬럼 없음·대장 버튼 표시·담당 학교 토글 숫자 없음·입력창 placeholder 없음, [대장] 클릭 → 학교 상세 진입.

**영향 범위**: 학교 허브(/schools)만. 정렬·CSV·담당/전체 토글·업무 바로가기·페이지네이션 변경 없음. 백엔드 계약 파일 미수정.

---

## [026] 2026-08-09 — 목서버에 한국산업안전협회 계약 학교 771건 실데이터 시드

**요청**: "한국산업안전협회 계약 학교 목록.xlsx"(771건 — 초 379·중 204·고 125·교육청 30·단설유 24·특수 9, 전남 22개 시·군, 전부 계약 완료)를 학교 목록에 등록.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `mock-schools.mjs` | **신규** — 엑셀 원본 771건을 목서버 시드 배열(REAL_SCHOOLS)로 변환. 매핑: 기관명→name, 학교구분→school_level(초등학교→초·중학교→중·고등학교→고·단설유→유·특수학교/교육청→기타), 담당자→manager(협회 담당), 설립 사립→is_private, 주소·이메일(복수 기재 시 첫 주소만 추출, 결측 4건 빈값). 참고용으로 지역·사소(본사/목포/광주)·전화·FAX·사업자번호도 보존(화면 미사용). 학교장·종사자수는 원본에 없어 비워둠 |
| `mock-server.mjs` | REAL_SCHOOLS import — 데모 학교(s01~s12) 뒤에 추가하고 학교별 빈 대장(ledger) 생성. 총 783교 |

**유의**: 프론트 소스 변경 없음 — 순수 개발용 샘플 데이터. 실제 백엔드 연동 시에는 이 시드가 아니라 백엔드 DB에 등록해야 함(엑셀 원본은 web-hq-handoff 폴더에 보관). 실데이터 학교는 assigned_inspector_id가 비어 있어 [전체 학교]에서만 표시되고, 종사자수는 대장 미입력 상태라 0명으로 표시됨.

**롤백 방법**: git 커밋 revert, 또는 mock-server.mjs의 REAL_SCHOOLS import·추가 루프 삭제 + mock-schools.mjs 삭제.

**검증**: 목서버 재기동 후 GET /schools 783건 · 1920px 렌더링 — [전체 학교] 783교, 지역명 "목포시"×[초] 조회 → 25교, 엑셀 담당자명 표시 확인. 783교 로딩 약 7초(학교별 대장 조회 783건 — 개발 환경 한정, 실서버는 백엔드 최적화 영역).

**영향 범위**: 목서버 데이터만. 납품 시 mock-schools.mjs는 mock-server.mjs와 함께 제외 가능.

---

## [027] 2026-08-09 — 김소현 담당자 (26-07)월 점검계획표 48교를 담당 학교로 지정

**요청**: 실물 (26-07)월 점검계획표(담당자 김소현) 사진에 수록된 학교들을 [담당 학교]에 표시.

**변경**: `mock-schools.mjs` — 계획표의 48개 학교(기관)에 `assigned_inspector_id: 'admin'` 지정 (데모 로그인 계정 기준). 약칭↔정식 명칭 매칭은 엑셀의 김소현 담당 58교 목록과 대조해 확정 — 삼학초→목포삼학초등학교, 마고→목포마리아회고등학교, 부설초→광주교육대학교목포부설초등학교, 온라인학교→전남온라인학교, 제일여고→목포제일여자고등학교, 완도교육지원청→전라남도완도교육지원청 등. 48교 구성: 목포 24 · 완도 12(교육지원청 포함) · 무안 8 · 나주 4(반남·양산·왕곡) · 영암 2(소림·은광).

**유의**: 김소현 담당 58교 중 계획표에 없는 10교(목포한빛초·산정초·유달중·하당중·항도여중·상동초, 무안 삼향동초·일로초·일로동초·무안중)는 미지정 — 다른 달 계획표 반영 시 추가 가능. 기존 데모 학교 5교(s01 등)의 admin 배정은 유지(5대 업무 샘플 데이터 연동용) — 담당 학교 합계 53교.

**롤백 방법**: git 커밋 revert, 또는 mock-schools.mjs에서 "admin" 지정 48건을 ""로 되돌리기.

**검증**: 목서버 재기동 · 1920px 렌더링 — admin 로그인 [담당 학교] 53교, 계획표 학교들 업무 바로가기 칩 표시 확인.

**영향 범위**: 목서버 데이터만. 프론트 소스 변경 없음.

---

## [028] 2026-08-09 — 홈 방문 일정에 담당 학교(7월 점검계획표) 반영

**요청**: [027]에서 지정한 담당 학교 목록이 홈 화면에도 반영되도록 — 방문 일정 방식 선택.

**변경**: `mock-server.mjs` — visit-plans 시드를 데모 4교 고정 계획에서 **(26-07)월 점검계획표의 주간 패턴(월~금 3주, 48교)을 이번 주 월요일 기준으로 배치**하는 동적 생성으로 교체(PLAN_WEEKS 상수 + 날짜 계산). 계획표의 사무실·순천·연차·제헌절 칸(학교 아님)은 제외. 서버 재시작 시점 기준으로 항상 "지난주~다다음주"에 배치되어 데이터가 살아있음.

**홈 화면 효과**: 월간 캘린더에 3주간 실제 담당 학교 표시(지난 날짜는 방문 실적 없으면 미이행 빨강) · "이번 주 방문 예정" 카드에 다음 주 학교들(대장 링크 포함) · "오늘의 할 일"은 해당 요일 계획 학교 표시(주말이면 0곳). 기한 알림의 "8월 안전점검 미방문 N개교"는 전체 783교 기준으로 집계되므로 숫자가 커짐(기존 로직 그대로 — 담당 학교 기준 집계로 바꾸려면 별도 요청).

**롤백 방법**: git 커밋 revert, 또는 PLAN_WEEKS·planSeed 블록을 [026] 이전의 고정 visit-plans 객체(pl1~pl9)로 교체.

**검증**: 목서버 재기동 · GET /ops/docs/visit-plans 14개 날짜(8/3~8/21 평일, 제헌절 자리 금요일 제외) · 홈 렌더링 — 캘린더 3주 학교 표시·이번 주 방문 예정 15건·대장 링크 확인.

**영향 범위**: 목서버 데이터만. 프론트 소스 변경 없음. 홈 캘린더의 "일정 편집"으로 저장하면 시드 대신 저장본이 사용됨(기존 동작).

---

## [029] 2026-08-09 — 학교 허브 검색창 위에 "이번 달 법정업무" 축약 배너 신설

**요청**: 홈의 "2026학년도 법정업무 운영 공지"(연간 간트)를 학교 탭 검색창 위에도 — 전체 간트를 그대로 옮기면 검색·목록이 스크롤 아래로 밀리는 문제 논의 후 **축약 배너** 방식 채택. 홈 간트는 그대로 유지.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/CycleBanner.tsx` | **신규** — 읽기 전용 축약 배너. 홈과 같은 서버 문서(/ops/docs/annual-cycle)를 읽어 ⑴ 접힌 상태: "{이번 달}월 법정업무" + 이번 달에 걸친 업무 칩 한 줄(안전점검 매월 진행·미방문 N교는 목록 배지 집계 연동, 나머지는 간트 바 기간·제출월 기준) ⑵ 펼침: 12개월(3~2월) 미니 간트(현재 월 강조·읽기 전용) + "홈에서 자세히 보기·일정 편집 →" 링크. 일정 편집은 홈에서만 |
| `src/pages/SchoolsHub.tsx` | 검색 패널 위에 CycleBanner 배치. 배너용 안전점검 미방문 수 계산(inspUnvisited — 현재 스코프(담당/전체) 기준, 업무 바로가기 배지 데이터 재사용) |
| `src/styles/schoolhub.css` | `.shub-cycle*` 스타일 블록 추가 (칩은 기존 .shub-w 재사용, 미니 간트는 110px 라벨 + 12열 그리드) |

**롤백 방법**: git 커밋 revert. 개별 복원 시 — SchoolsHub의 CycleBanner import·렌더·inspUnvisited 블록 삭제 + CycleBanner.tsx 삭제 + schoolhub.css의 shub-cycle 블록 삭제.

**검증**: `tsc --noEmit` 통과 · 1920px 렌더링 — 접힌 배너 칩(안전점검 미방문 51교·근골격계 착수·이행점검 공지 하달·교육 수시 진행), 펼침 시 12개월 미니 간트·현재 월(8월) 강조·홈 링크 동작. 배너가 얇아(접힘 기준 약 44px) 검색창·목록 도달에 지장 없음.

**영향 범위**: 학교 허브(/schools)만. 홈 간트·문서 저장 구조 변경 없음(같은 문서를 읽기만 함 — 홈에서 편집하면 배너에도 반영됨).

---

## [030] 2026-08-09 — 홈 "연간 법정업무 운영 공지" 간트 제거

**요청**: 홈의 연간 법정업무 운영 공지(간트) 섹션 삭제 — 연간 일정 표시는 학교 탭의 축약 배너([029])로 일원화.

**변경**:
- `src/pages/Home.tsx` — `{/* ===== 1-2. 연간 법정업무 운영 공지 ===== */}` CycleHero 렌더 블록(30행) 삭제. **cycleDoc 로드·기한 알림(deadlines) 연동 로직은 유지** — 기한 알림 카드와 학교 탭 배너가 같은 문서를 계속 사용.
- `src/components/CycleBanner.tsx` — 펼침 하단의 "홈에서 자세히 보기·일정 편집 →" 링크 제거(홈에 간트가 없어졌으므로), 안내 문구로 교체.

**유의**: 간트의 **일정 편집 UI(편집 버튼)가 화면에서 사라짐** — annual-cycle 문서를 수정할 화면이 현재 없음(서버 문서·기본값은 그대로 유지되어 기한 알림·배너는 정상 동작). 일정 편집이 다시 필요하면 ⑴ 홈 간트 복원 또는 ⑵ 설정 화면에 편집 폼 신설 중 택일(후속 요청 시).

**롤백 방법**: git 커밋 revert, 또는 [002] 기록의 CycleHero 블록을 오늘의 할 일 아래에 복원 + CycleBanner 링크 문구 원복.

**검증**: `tsc --noEmit` 통과 · 홈 렌더링 — "법정업무 운영 공지" 텍스트 잔존 0, 오늘의 할 일 → 캘린더 자연 연결, 기한 알림 정상 표시.

**영향 범위**: 홈(/)과 학교 탭 배너 문구만. CycleHero 컴포넌트 파일은 삭제하지 않음(롤백 대비).

---

## [031] 2026-08-09 — 홈 화면 개인별 커스텀 — [홈 편집]으로 블록 순서·크기 조절

**요청**: 홈에 편집 버튼을 두고, 각 섹션(오늘의 할 일·캘린더 등)의 크기를 줄이고 늘리고 위치를 바꿀 수 있게 — 개인별로 저장.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/Home.tsx` | 홈을 **6개 블록**(오늘의 할 일·공지사항·산재 알림·캘린더·이번 주 방문 예정·기한 알림)의 6열 그리드로 재구성. 기존 고정 2단 레이아웃(hm-top·hm-plan·hm-side-col) 제거 — 각 카드 JSX는 renderBlock() 조각으로 이동(내용·기능 변경 없음). 우상단 **[홈 편집]** 버튼 → 편집 모드: 블록마다 컨트롤 바(이름·크기 배지·[◀][▶] 순서 이동·[−][＋] 크기 조절, 라이브 미리보기) + 상단 [기본 배치]/[취소]/[저장]. 크기 단계: 1/3 → 1/2 → 2/3 → 전체(6열 그리드 span 2·3·4·6). **개인별 저장**: `/ops/docs/home-layout-{로그인ID}` 서버 문서(normalizeLayout으로 검증·누락 블록 자동 보충 — 향후 블록 추가에도 안전). 저장 전 취소 시 원래 배치 복원 |
| `src/styles/home.css` | `.hm-custombar`·`.hm-grid`·`.hm-block`·`.hm-block-ctl` 스타일 추가. 편집 중 블록에 보라 점선 아웃라인. 1280px 이하에서는 모든 블록 전체 폭(기존 반응형 동작 유지) |

**기본 배치** (저장본 없을 때): 오늘의 할 일(2/3)+공지사항(1/3) / 캘린더(2/3)+이번 주 방문 예정(1/3) / 기한 알림(1/2)+산재 알림(1/2).

**롤백 방법**: git 커밋 revert. 저장된 개인 레이아웃 문서는 남지만 화면에 영향 없음(무해).

**검증**: `tsc --noEmit` 통과 · 1920px 렌더링 — 기본 배치 확인 → 편집 모드 진입(컨트롤 바·점선 표시) → 캘린더 맨 앞 이동+전체 폭·오늘의 할 일 1/2 축소 → 저장 → **새로고침 후 유지** 확인. 백엔드 계약 파일 미수정(범용 /ops/docs API 사용).

**영향 범위**: 홈(/) 레이아웃 구조. 각 카드의 내용·기능(캘린더 일정 편집, 공지 등록 등)은 변경 없음.

---

## [032] 2026-08-09 — 홈 편집: 화살표 대신 드래그로 자유 이동 (아이폰 홈 방식)

**요청**: [031]의 ◀▶ 화살표 이동 대신, 아이폰에서 앱을 꾹 눌러 옮기듯 편집 모드에서 블록을 눌러 끌면 위치가 자유롭게 바뀌게.

**변경**:
- `src/pages/Home.tsx` — 컨트롤 바의 ◀▶ 버튼 제거(moveBlock → dragOverBlock 대체), 블록에 HTML5 드래그 적용: 편집 모드에서 `draggable`, 끌던 블록이 다른 블록 위를 지나면 **그 자리로 즉시 끼워넣기**(라이브 재배치). 컨트롤 바에 그립(⋮⋮) 아이콘 추가 — 크기 −/＋ 버튼은 유지. 끌던 블록은 반투명 표시.
- `src/styles/home.css` — 아이폰 홈 편집처럼 편집 중 블록이 **살짝 흔들리는 애니메이션**(hm-wiggle) + grab/grabbing 커서. 편집 중에는 카드 내부 클릭 차단(pointer-events: none — 드래그 방해 방지, 캘린더 등 카드 기능은 편집 종료 후 정상).

**롤백 방법**: git 커밋 revert ([031]의 화살표 방식으로 복귀).

**검증**: `tsc --noEmit` 통과 · 렌더링 — 편집 모드에서 공지사항 블록을 캘린더 위로 드래그 → 즉시 맨 앞으로 재배치, 저장 → 새로고침 후 순서 유지 확인.

**영향 범위**: 홈 편집 모드의 이동 방식만. 크기 조절·저장·기본 배치 동작은 [031] 그대로.

---

## [033] 2026-08-09 — 홈 상단 시간대별 인사말 추가

**요청**: 홈 최상단(오늘의 할 일 위)에 "00님 안녕하세요. 좋은 아침입니다."처럼 접속 시간대에 맞는 인사 문구 표시.

**변경**:
- `src/pages/Home.tsx` — 홈 편집 바 좌측에 인사말 추가. **이름**: 계정 목록(GET /users)에서 로그인 ID로 실명 조회(admin→관리자, insp01→김조사 등), 실패 시 로그인 ID 폴백. **시간대별 문구**(접속 기기 현재 시각 기준): 05~11시 "좋은 아침입니다." / 11~14시 "점심 시간이네요." / 14~18시 "활기찬 오후입니다." / 18~22시 "좋은 저녁입니다." / 22~05시 "늦은 시간까지 수고가 많으시네요." + 각각 어울리는 보조 문구 한 줄.
- `src/styles/home.css` — `.hm-greet` 스타일(제목 21px + 보조 문구).

**롤백 방법**: git 커밋 revert, 또는 hm-greet 블록·displayName/greeting 로직·css 블록 삭제.

**검증**: `tsc --noEmit` 통과 · 렌더링 — "관리자님, 안녕하세요. (시간대 문구)" 표시 확인.

**영향 범위**: 홈(/) 상단만. [031] 홈 편집 버튼은 같은 줄 우측에 유지.

**보완 (같은 날)**: 문구를 밝고 부드러운 안전 테마로 교체(안전회사 정체성 반영) — 아침 "좋은 아침이에요! / 오늘도 우리 학교들의 안전, 힘차게 지키러 가볼까요?" · 점심 "맛있는 점심 드셨나요? / 든든하게 충전했으니, 오후 방문길도 안전 운전이에요!" · 오후 "활기찬 오후예요! / 잠깐 스트레칭 한 번 하고, 남은 점검도 안전하게 마무리해요." · 저녁 "오늘도 정말 수고 많으셨어요! / 덕분에 학교가 한 뼘 더 안전해진 하루였어요." · 심야 "늦은 시간까지 애쓰시네요! / 안전은 충분한 휴식에서 시작돼요." 인사 형식도 "OO님, 안녕하세요. 좋은 아침입니다." → "OO님, 좋은 아침이에요!"로 간결하게.

---

## [034] 2026-08-09 — 학교 목록: 대장 컬럼 맨 오른쪽 텍스트화 · 가나다순 기본 정렬 · 담당자 실명

**요청**: ⑴ 대장을 맨 오른쪽으로 옮기되 버튼일 필요 없음(행 클릭으로 진입) ⑵ 학교 가나다순 정렬 ⑶ 담당자 실명 표기.

**변경**:
- `src/pages/SchoolsHub.tsx` — ⑴ [029]의 대장 버튼 컬럼 제거 → 테이블 맨 오른쪽에 "대장 →" **텍스트 표시** 컬럼(업무 바로가기보다 뒤). 행 어디를 클릭해도 학교 상세(대장)로 진입하는 기존 동작 그대로 — 표시는 행 호버 시 보라색으로 강조. ⑵ 목록 로드 시 학교명 `localeCompare(ko)` **가나다순 기본 정렬**(컬럼 헤더 정렬 클릭 시 그 기준으로 전환되는 것은 기존대로).
- `src/styles/schoolhub.css` — `.shub-ledger` 버튼 스타일 → `.shub-golink` 텍스트 스타일로 교체.
- `mock-server.mjs` — 데모 학교 12곳의 담당자를 가칭(김담당·이담당 등) → **협회 실명 담당자**로 교체(김소현·이재훤·정원형·이지현·이숙현·신하정·송연섭·박건민·오은서·이경록·박영진·김주향). 실데이터 771건은 엑셀 실명 그대로.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 담당 학교 첫 행부터 가나다순(광주교대목포부설초→군외중→군외초→나주반남중→남악유치원…), 담당자 실명 표시, 대장 → 맨 오른쪽 텍스트·행 클릭 시 학교 상세 진입(URL /schools/r077) 확인.

**영향 범위**: 학교 허브(/schools) + 목서버 데모 담당자명. 과거 점검 기록의 서명자명(김담당 등)은 이력이므로 유지.

---

## [035] 2026-08-09 — 학교 상세(대장): 상단 정리 — 작성 버튼 삭제·KPI 카드 줄 제거·복귀 링크 변경

**요청**: ⑴ [안전점검 작성]·[근골 보고서] 버튼 삭제 ⑵ "대장으로" → "학교 목록으로" ⑶ 현업종사자~산재 현황 KPI 카드 4개 줄 전부 제거.

**변경** (`src/pages/SchoolDetail.tsx`):
- 헤더 버튼에서 안전점검 작성·근골 보고서 Link 2개 삭제 (종사자 편집·수정·삭제는 유지). 작성 진입은 학교 탭의 업무 바로가기([021])로 일원화.
- 복귀 링크 "대장으로"(→ 홈) → **"학교 목록으로"(→ /schools)** 로 라벨·목적지 모두 변경.
- KPI 카드 4개(현업종사자·교육생 수·인원 대조·산재 현황) `.kpis` 블록 삭제 — 현업종사자 표가 바로 시작. 수치 데이터 로직은 아래 섹션들(종사자 표·산재 현황 카드)에서 계속 사용.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 버튼 2개·KPI 카드 0개 확인, [학교 목록으로] 클릭 → /schools 이동 확인.

**영향 범위**: 학교 상세(/schools/:id) 상단만. 하단 섹션(현업종사자·학교 특징·특이사항·MSDS·산재 현황 등) 변경 없음.

---

## [036] 2026-08-09 — 학교 상세: 버튼 3개를 [편집] 하나로 통합 — 페이지 전체 편집 모드

**요청**: 종사자 편집·수정·삭제 버튼을 [편집] 하나로 합치고, 누르면 현업종사자부터 월별 진행 이력까지 전부 편집 가능 + 버튼이 [수정 완료]로 바뀌고, 다시 누르면 [편집]으로 복귀.

**변경** (`src/pages/SchoolDetail.tsx` — `pageEdit` 상태 신설):
- **보기 모드(기본)**: 헤더에 [편집]·[학교 목록으로]만. 모든 섹션이 조회 전용 — 추가/삭제/편집 버튼·입력 폼·토글 숨김(학교 특징 칩은 클릭 무반응).
- **편집 모드**([편집] 클릭): 헤더가 [학교 정보 수정](구 수정 모달)·[학교 삭제]·[수정 완료]로 전환. 섹션별 편집 수단 표시 — 현업종사자 [종사자 편집], 학교 특징 토글 활성(+안내 문구), 특이사항 삭제 ✕·추가 폼, 결재선 프리셋·입력·저장, MSDS [추가], 산재 현황 [추가], 담당자 이력 [CSV 업로드]·[편집], 월별 진행 이력 [추가].
- **[수정 완료]** 클릭 → 보기 모드 복귀(버튼이 다시 [편집]으로). 각 편집은 기존처럼 즉시 저장되는 구조라 완료 시 별도 저장 없음.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 보기 모드(편집 1·추가 0·종사자 편집 0) → 편집 모드(수정 완료 1·추가 5·종사자 편집 1) → 완료 후 보기 모드 복귀 확인.

**영향 범위**: 학교 상세(/schools/:id)만. 각 편집 기능(모달·저장 로직)은 변경 없음 — 노출 시점만 편집 모드로 묶음.

---

## [037] 2026-08-09 — 학교 상세: 모달 없이 화면에서 직접 인라인 수정

**요청**: [036]의 편집 모드에서 "학교 정보 수정" 버튼을 눌러 모달을 여는 방식이 아니라, [편집]을 누르면 그 자리에서 바로 고칠 수 있게.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolDetail.tsx` | ⑴ **학교 정보 섹션 신설**(헤더 아래) — 보기 모드: 학교급·설립·학교장·관리감독자·담당자·안전점검기관·교육생 수·이메일·주소 요약 그리드(기존에는 상세 화면에 없던 정보). 편집 모드: 전 필드가 **인라인 입력창**(학교급·설립은 셀렉트). ⑵ **현업종사자 표 인라인 편집** — 편집 모드에서 5개 파트의 인원(숫자)·연락처·영양교사 체크박스를 표 안에서 직접 수정. ⑶ [수정 완료] = **일괄 저장**(PUT /schools/{id} + PUT /schools/{id}/workers) 후 보기 모드 복귀 — 저장 중 표시·실패 시 오류 문구·학교명 필수 검증. ⑷ 구 SchoolFormModal(학교 정보 수정 모달)·WorkersEditor(종사자 편집 모달) 사용 제거(컴포넌트 파일은 학교 등록 등 다른 화면용으로 보존) |
| `src/styles/schoolhub.css` | `.shub-infogrid` 3열 정보 그리드(보기·편집 공용) 스타일 추가 |
| `mock-server.mjs` | **PUT /schools/:id · PUT /schools/:id/workers 핸들러 추가** — 그동안 목서버에 없어서 구 수정 모달도 실제로는 저장 불가였던 것 보완. 학교 수정 시 대장(ledger)의 학교 정보·종사자 합계 동기화 |

**참고**: 학교 정보 전체 필드는 GET /schools 목록에서 조회(대장 API에는 일부 필드만 있음) — 저장 시 목록에서 가져온 원본 위에 수정분을 덮어써 기존 필드 유실 없음.

**롤백 방법**: git 커밋 revert ([036]의 모달 방식으로 복귀).

**검증**: `tsc --noEmit` 통과 · 렌더링 — [편집] → 학교 정보 10개 필드·종사자 표 인라인 입력 전환 확인, 학교장 "박새교장"·급식 인원 3→5 수정 → [수정 완료] → 보기 모드에 저장값 반영 확인.

**영향 범위**: 학교 상세(/schools/:id) + 목서버. 보기 모드에 학교 정보 요약 섹션이 새로 보임(기존에 없던 정보 노출 — 개선). 백엔드 계약 파일 미수정.

---

## [038] 2026-08-09 — 법정업무 배너: 업무 칩 클릭 시 담당 학교 완료율 도넛 표시

**요청**: [연간 일정]은 그대로 두고, 안전점검·위험성평가·근골격계·이행점검 칩을 클릭 가능하게 — 누르면 담당 학교의 해당 업무 완료율을 도넛 그래프로 표시 (안전점검부터 이행점검까지 각각).

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/CycleBanner.tsx` | 배너 헤더를 통짜 버튼 → 개별 요소로 재구성: 4대 업무 칩은 **클릭 버튼**(누르면 아래로 도넛 패널 펼침·다시 누르면 닫힘·선택 칩 보라 강조), [연간 일정]은 우측 독립 토글(기존 미니 간트 그대로). 안전보건교육 칩은 정보 표시만. 4대 업무 칩은 이번 달 일정이 없어도 항상 표시("이번 달 일정 없음"). **도넛 게이지**: SVG — 트랙 회색(라인 토큰)·진행 아크 보라(브랜드)·중앙 % 숫자(잉크 토큰), 옆에 "{업무} 완료율 · 담당 학교 N/M교 완료 · 판정 기준" 표기 |
| `src/pages/SchoolsHub.tsx` | 업무별 완료 수 집계(workStats) — 담당 학교 기준(담당 배정 없으면 전체), 학교 목록 배지와 동일 판정(안전점검=이번 달 완료 · 위험성평가=완료 · 근골격계=검수 대기 없음 · 이행점검=당기 조사지 제출). **배지 집계 대상을 담당 학교로 한정** — 기존 783교 전체 ×3 API(2,300여 요청)로 인한 요청 유실·오집계(근골 100%/안전 0% 현상)와 로딩 지연 해소 (업무 바로가기 컬럼·도넛 모두 담당 기준이라 동작 변화 없음) |
| `src/styles/schoolhub.css` | 칩 선택 강조(.shub-w.on)·도넛 패널(.shub-cycle-donut)·연간 일정 버튼 스타일 |

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 4개 칩 각각 클릭 → 안전점검 4%(2/53)·위험성평가 2%(1/53)·근골격계 98%(52/53)·이행점검 0%(0/53) 도넛 표시, 선택 칩 강조, [연간 일정] 토글 정상. 배지 로딩도 체감 단축(159 요청).

**영향 범위**: 학교 허브(/schools) 배너·배지 집계 범위. 백엔드 계약 파일 미수정.

---

## [039] 2026-08-09 — (추가 후 철회) 도넛 패널 "오늘 {업무} 할 학교" 바로가기

**경위**: 업무 칩 클릭 시 오늘 점검할 학교를 표시하고 클릭하면 해당 업무로 진입하는 기능을 구현했으나, 전략 재논의 끝에 **같은 날 철회**(커밋 24afc48 추가 → fb817b3 revert).

**철회 사유**: "오늘 갈 학교"는 홈(오늘의 할 일)이, "학교별 상태·작성 진입"은 아래 학교 목록의 업무 바로가기 배지가 이미 담당 — 배너에 세 번째 경로를 만들면 같은 질문에 답하는 화면이 중복돼 오히려 복잡해진다는 결론. 최종 역할 분담: **홈=오늘 일정 / 학교 목록=학교별 상태·진입 / 배너 도넛=업무별 진척 요약**([038] 상태 유지). "남은 학교 관리" 필요 시 각 업무 탭 첫 화면이 적합한 자리(후속 검토).

**복원 방법**: 필요해지면 `git revert fb817b3` 로 재적용 가능(이력 보존됨).

---

## [040] 2026-08-09 — 업무 바로가기 배지를 2색 이진 표시(완료/미완료)로 단순화

**요청**: 배지 색이 5가지(초록·주황·보라·빨강·회색)라 의미 파악이 어려움 — "했다/안 했다"만 구분되게.

**변경** (`src/pages/SchoolsHub.tsx` — deriveBadges·교육 칩):
- **완료 = 초록 점 / 미완료 = 회색 점** 2색으로 통일. 미완료를 경고색으로 하면 시작 단계 학교가 온통 경고색이 되므로 중립 회색 채택 — 완료가 하나씩 초록으로 켜지는 방식.
- 완료 판정: 안전점검=이번 달 점검 완료 · 위험성평가=전 건 완료 · 근골격계=**조사 존재 + 검수 대기 없음**(조사 자체가 없으면 미작성=회색) · 교육=**대장 입력 + 인원 일치**(대장 미입력이면 회색) · 이행점검=당기 조사지 제출 완료. — 데이터가 아예 없는 학교가 완료로 보이던 문제도 함께 보정.
- 세부 상태(진행 N건·검수 N건 대기·작성 중·대장 미입력 등)는 **툴팁(마우스 오버)** 으로 유지.
- 배너 도넛 집계(ok 기준)·미방문 수 계산도 새 판정과 일치하도록 정리. 상단 배너 칩의 색(월 일정 표시)은 별개로 유지.

**롤백 방법**: git 커밋 revert ([021] 5색 상태 배지로 복귀).

**검증**: `tsc --noEmit` 통과 · 렌더링 — 한빛초(안전·위험성·교육 초록 / 근골 회색 "검수 2건 대기" / 이행 회색 "미작성"), 실데이터 학교는 전부 회색(미작성·대장 미입력) 확인.

**영향 범위**: 학교 허브(/schools) 배지 색 체계·완료 판정. 도넛 %는 새 판정 기준을 따름(근골격계는 조사 없는 학교가 완료로 집계되던 것이 제외되어 수치가 현실화됨).

---

## [041] 2026-08-09 — 업무 바로가기 색 범례 추가

**요청**: 배지 색의 의미를 화면에서 알려줘야 한다는 지적 반영.

**변경**: `src/pages/SchoolsHub.tsx` — 학교 목록 헤더(담당/전체 토글 왼쪽)에 범례 "업무 바로가기: ● 완료 ● 미완료" 표시(담당 학교 스코프에서만 — 배지 컬럼이 보일 때만). 범례에 마우스를 올리면 "세부 상태는 배지에 마우스를 올리면 표시됩니다" 안내 툴팁. `src/styles/schoolhub.css` — `.shub-legend` 스타일.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 담당 학교 목록 헤더에 범례 표시 확인.

**영향 범위**: 학교 허브(/schools) 목록 헤더만.

---

## [042] 2026-08-09 — 배너 업무 칩: 주황(주의)일 때만 문구 표시

**요청**: 배너 칩의 문구를 주황불일 때만 옆에 표시("안전점검 · 미방문 51교" 식), 회색불일 때는 업무 이름만.

**변경**: `src/components/CycleBanner.tsx` — 칩 라벨 규칙 변경: 주황(warn)일 때만 상세 문구 병기(안전점검 미방문 N교 · 근골격계 착수·증상조사 등), 초록·회색·보라는 이름만. 숨긴 상세 문구(이번 달 일정 없음·수시 진행·공지 하달 등)는 **툴팁으로 이동**(마우스 오버 시 표시, 클릭 안내 포함).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — "안전점검 · 미방문 51교(주황)" / "위험성평가·이행점검·안전보건교육(이름만)" / "근골격계 · 착수 · 증상조사(주황)" 확인.

**영향 범위**: 학교 허브(/schools) 배너 칩 표기만. 도넛·연간 일정 동작 변경 없음.

---

## [043] 2026-08-09 — 전체 학교 스코프에 담당자 검색 칸 추가

**요청**: [전체 학교] 클릭 시 학교명·지역명에 더해 담당자 검색 칸도 표시.

**변경** (`src/pages/SchoolsHub.tsx`):
- [전체 학교] 스코프에서만 검색 패널에 **[담당자] 입력 칸** 추가(학교명·지역명과 동일한 확정형 — [조회]/Enter로 반영, X로 즉시 해제). 담당자 이름 부분일치, 다른 조건과 AND.
- [담당 학교]로 전환하면 담당자 칸이 숨겨지고 입력값·적용 필터도 자동 해제(숨은 필터가 몰래 남지 않도록).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 담당 스코프 2칸 → 전체 스코프 3칸, "강성래" 조회 → 42교(전원 담당자 강성래 — 엑셀 원본 42건과 일치), 담당 전환 시 53교 복귀·칸 숨김 확인.

**영향 범위**: 학교 허브(/schools) 검색 패널만.

---

## [044] 2026-08-09 — 오늘의 할 일: 담당 학교 한정 + 행 클릭 시 빠른 실행 버튼

**요청**: (임시로 admin=김소현 담당자로 간주하고 작업 중) ⑴ 오늘의 할 일이 로그인한 담당자의 일정만 표시되어야 함 ⑵ 행을 클릭하면 안전점검·근골격계로 갈 수 있는 버튼이 나와야 함.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/Home.tsx` | 오늘의 할 일(todayItems)을 **로그인 담당자의 담당 학교로 한정** — assigned_inspector_id 기준(담당 배정이 없는 계정은 기존대로 전체 표시, 학교 미연결 자유 입력 계획은 유지). 다른 담당자 학교의 방문 실적(예: 동산초)이 섞여 보이던 문제 해소 |
| `src/components/TodayHero.tsx` | 행 클릭 동작 변경: 학교 상세 즉시 이동 → **빠른 실행 버튼 펼침**([안전점검 작성](/inspection/new?school=) · [근골격계 보고서](/musculo/report?school=) · [대장 보기](/schools/{id})). 다시 클릭하면 접힘, 펼친 행은 연보라 배경 강조 |
| `src/styles/home.css` | `.hm-td-actions`·`.hm-td-row.open` 스타일 추가 |

**롤백 방법**: git 커밋 revert (행 클릭=학교 상세 이동으로 복귀).

**검증**: `tsc --noEmit` 통과 · 렌더링 — 오늘의 할 일에 한빛초만 표시(1/1곳 — 타 담당 동산초 제외), 행 클릭 → 버튼 3개 펼침, [안전점검 작성] 클릭 → /inspection/new?school=s01 진입 확인.

**영향 범위**: 홈 오늘의 할 일만. 캘린더·이번 주 방문 예정은 변경 없음(방문 계획 문서 자체가 담당자 일정).

---

## [045] 2026-08-09 — 점검표 작성: 자동 해당없음 배너 재구성 · 집계 박스 줄 제거

**요청**: 노란 배너의 글씨(코드 태그 나열)가 어색함 → 재구성. 양호~전체 항목 집계 박스 줄 삭제.

**변경**:
- `src/pages/InspectionForm.tsx` — ⑴ 배너 상세를 **"사유 — 항목들"** 형식으로 재구성: "엘리베이터 없음 — 시설 5 · 시설 6"처럼 사유(굵게) 뒤에 해당 항목 번호를 한 줄로 (구: 코드 칩 나열 뒤 사유 — 읽기 어려움). ⑵ 집계 바(양호·미흡·해당없음·미입력·전체 항목 5칸) JSX 삭제 — 집계 로직(tally)은 저장 시 요약·서명 문구에서 계속 사용.
- `src/styles/inspectform.css` — `.insf-auto-row` 스타일 추가(사유 뒤 — 구분자·항목 앰버색).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링(/inspection/new?school=r077) — 배너 6개 사유 행 정렬 표시·집계 박스 0개·기본정보 섹션 바로 연결 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [046] 2026-08-09 — 점검표 작성: 점검대상 버튼의 인원수 표기 제거

**요청**: 점검대상 체크 버튼(급식종사자 3명 등)에서 명수 삭제.

**변경**: `src/pages/InspectionForm.tsx` — 점검대상 버튼 라벨에서 `{n}명` 표기 제거(이름만 표시). 인원수는 각 파트 점검표 헤더("N개 항목 · N명")에는 유지.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 버튼 5개 이름만 표시 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [047] 2026-08-09 — 점검표 작성: 선택 해제 토글 · 추천값 미리선택 제거 · 임시저장

**요청**: ⑴ 같은 버튼(양호/미흡/해당없음)을 다시 누르면 선택이 해제되어야 함 ⑵ 값형 항목(국소배기장치·매주·지상)이 미리 눌러져 있지 않고 추천 버튼만 보이게 ⑶ 임시저장 버튼 추가.

**변경** (`src/pages/InspectionForm.tsx`):
- **선택 해제 토글**: 라디오(양호·미흡·해당없음)를 재클릭하면 해제(답변 삭제 → 미입력 복귀, 자동 해당없음 항목은 자동값 복귀). 추천 칩도 재클릭 시 해제.
- **추천값 미리선택 제거**: CARRY_VALUE 항목(급식 25·28·29)의 비고가 지난 점검값으로 미리 채워지던 것을 빈칸 시작으로 변경 — "지난 점검값 X (기본/기록)" 안내는 유지하되 사용자가 추천 버튼을 눌러야 선택됨. 입력창 placeholder "추천에서 선택하거나 직접 입력".
- **[임시저장]**: 상단 헤더·하단 제출 바에 버튼 추가 — 서명·제출 없이 현재 입력을 '작성중' 점검으로 저장(POST /inspections + 답변 PUT, sign/submit 미호출). 저장 후 초록 안내("점검 현황에서 [이어서 작성]으로 재개"). 같은 세션에서 재저장·최종 저장 시 임시저장분에 이어서 기록(draftIds — 중복 생성 방지).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 라디오 클릭→체크·재클릭→해제, 추천 칩 미선택 시작→클릭 선택→재클릭 해제, 임시저장 → "작성중" 저장·안내 메시지 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만. 백엔드 계약 변경 없음(기존 엔드포인트 그대로, sign/submit 생략 시 draft 상태 유지는 기존 서버 동작).

---

## [048] 2026-08-09 — 점검표: 자동 해당없음 항목도 재클릭 시 완전 해제

**요청**: [047]의 해제 토글이 자동 해당없음 항목에서는 다시 자동값으로 돌아가 계속 체크돼 보임 → 재클릭 시 아무것도 선택 안 된 상태가 되어야 함.

**변경** (`src/pages/InspectionForm.tsx`): answers에 **명시적 해제(null)** 상태 도입 — 자동 해당없음 항목의 체크된 라디오를 재클릭하면 자동값을 무시하고 빈 상태로 표시(앰버 강조·자동 태그도 해제). 다시 클릭하면 재선택. 해제 상태는 미입력으로 집계되고 저장 시 해당 항목은 기록되지 않음.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 급식-5(자동 해당없음): 체크 → 재클릭 → 라디오 3개 모두 해제 확인 → 재클릭 → 다시 선택.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [049] 2026-08-09 — 점검표: 자동 해당없음 안내를 수동 선택·해제 후에도 유지

**요청**: [048]에서 자동 해당없음 항목을 해제/재선택하면 주황 안내("자동 해당없음 · 사유")가 사라짐 → 계속 보이게.

**변경** (`src/pages/InspectionForm.tsx`): 안내 태그 표시 조건을 "자동값이 적용 중일 때"에서 **"자동 해당없음 대상 항목이면 항상"** 으로 변경 — 해제·재선택·다른 값 선택 후에도 사유가 계속 보임. 앰버 행 강조는 자동값이 실제 적용 중일 때만 유지(자동 vs 수동 구분).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 급식-5: 초기·해제·재선택 모두 태그 유지 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [050] 2026-08-09 — 점검표: 추천값 항목 비고 UI 정리

**요청**: 값형 항목(국소배기장치 등)의 입력창 보라 네모 제거 → 다른 항목과 동일한 "보완계획" 입력창으로. 보라 안내 문구는 "추천"으로 축약. 선택지 버튼들을 보라 네모로.

**변경**:
- `src/pages/InspectionForm.tsx` — carried 전용 입력 스타일 제거(일반 입력창 + placeholder "보완계획"), 안내 문구 "지난 점검값 X (기본/기록)" → **"추천"** (원래 문구는 마우스 오버 툴팁으로 유지).
- `src/styles/inspectform.css` — 추천 선택지(.insf-sg)를 알약형 회색 → **보라 테두리 네모 버튼**(둥근 사각형·보라 소프트 배경·굵은 글씨), 선택 시 보라 채움+흰 글씨, 호버 시 진해짐.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 일반 입력창 + "추천" 라벨 + 보라 네모 선택지 4개, 선택 시 채움 스타일 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [051] 2026-08-09 — 안전점검 첫 화면: 공정별 행 → "점검표 1장" 단위 리스트로 재구성

**요청**: 작성된 점검표 리스트가 공정(급식·시설 등)별 개별 행으로 나뉘어 있음 — 종사자 안전·보건 점검표는 1장 단위이므로 점검표 단위로 리스트업하고, 컬럼은 점검일·학교·담당자·작성현황·교육청 전송으로.

**변경** (`src/pages/Inspection.tsx`):
- **학교×점검일 단위 그룹핑**(groupToSheets) — 한 번 작성 시 함께 생성되는 공정별 점검들을 점검표 1장으로 묶음(작성중 건은 학교당 1행). 상태 집계: 작성중 포함 → 작성중 / 전 공정 제출 → 제출 / 그 외 → 서명완료. 교육청 전송: 전송완료 > 대기 > 실패 > — 순 보수 집계.
- **컬럼 정리**: 점검일 · 학교 · 담당자 · 작성현황 · 교육청 전송 (공정·항목수·서명 컬럼 제거 — 포함 공정은 행 툴팁·CSV 내보내기에 유지). 상태 필터 라벨 "작성현황"으로.
- **교육청 전송 라벨 불일치 보정** — 목서버 응답값('submitted')이 라벨 맵 키('success')와 달라 항상 "—"로 보이던 기존 문제 수정 → "전송완료" 정상 표시.

**롤백 방법**: git 커밋 revert ([023] 공정별 평탄화 리스트로 복귀).

**검증**: `tsc --noEmit` 통과 · 렌더링 — 15건 → 8건(한빛초 작성중 4공정 = 1행), 컬럼 5개, 제출 건 "전송완료" 표시, 행 클릭 → 학교 점검 현황 진입.

**영향 범위**: 안전점검(/inspection) 첫 화면만. 학교 컨텍스트(점검 현황)·작성 폼 변경 없음.

---

## [052] 2026-08-09 — 학교 컨텍스트 점검 현황도 점검표 1장 단위로 · 점검표 통째 이어서 작성

**요청**: [051]에 이어 학교 컨텍스트의 점검 현황도 공정별이 아닌 종사자 안전·보건 점검표 단위로.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/Inspection.tsx` | 학교 컨텍스트 점검 현황을 groupToSheets로 **점검표 1장 단위** 재구성 — 컬럼: 점검일 · 포함 공정(칩 클릭 시 해당 공정 상세 모달) · 작성현황 · 교육청 전송 · 작업. 작성중 점검표의 [이어서 작성]은 `?school=&resumeall=1`로 **전 공정 통째 재개**, 완료 점검표는 [보기] |
| `src/pages/InspectionForm.tsx` | **resumeall 모드 신설** — 이 학교의 공정별 최신 작성중 점검을 모두 찾아 ⑴ 기존 응답·비고 일괄 프리필 ⑵ 해당 공정 자동 선택 ⑶ 그 점검들에 이어서 기록(draftIds 연동 — 새 점검 중복 생성 없음). 기존 단일 공정 resume은 그대로 |

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 한빛초 점검 현황 3행(작성중 1장[급식·통학·시설·미화] + 서명완료 + 제출), 공정 칩 클릭 → 상세 · 임시저장(양호·미흡 2건) → resumeall 재진입 시 응답 복원 확인.

**영향 범위**: 안전점검 학교 컨텍스트·작성 폼. 백엔드 계약 변경 없음.

---

## [053] 2026-08-09 — 학교 컨텍스트 점검 현황: 포함 공정 컬럼 제거

**요청**: [052]의 포함 공정 칩 컬럼 삭제 — 점검일·작성현황·교육청 전송·작업만.

**변경** (`src/pages/Inspection.tsx`): 포함 공정 컬럼 제거(중복 칩 표시 문제도 함께 해소) — 컬럼 4개로 정리. 포함 공정 목록은 행 툴팁으로 유지(중복 제거). [보기]는 점검표의 첫 공정 상세를 엶.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 한빛초 3행 × 4컬럼 확인.

**영향 범위**: 안전점검 학교 컨텍스트만.

---

## [054] 2026-08-09 — 점검표 [보기] = 실물 양식(PDF 서식) 화면 + 첫 화면에도 보기 버튼

**요청**: 안전점검 첫 화면(전체 작성물 리스트)에도 작업 [보기] 버튼 추가, [보기] 클릭 시 제출용 PDF(종사자 안전·보건 점검표 실물 양식)와 같은 모양으로 표시. 이어서 작성 등 작성 버튼은 학교 컨텍스트(담당자 화면)에만.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/InspectionSheetView.tsx` | **신규** — 점검표 실물 양식 보기 오버레이. 제출 PDF 서식 재현: 제목+우상단 결재란(담당자 서명) → 기본정보 그리드(학교명·작성자·작성일·점검일 자동, 나머지 빈칸) → 점검대상 체크박스(포함 공정 ✓) → 공정별 점검표(점검항목/양호/미흡/해당없음 ✓ 표시 + 비고 박스). 상단 바 [인쇄 / PDF 저장](브라우저 인쇄)·[닫기] |
| `src/styles/inspectsheet.css` | **신규** — inss- 접두사. A4 폭 종이 스타일 + @media print(양식만 출력·A4 세로·행 분리 방지) |
| `src/pages/Inspection.tsx` | ⑴ 첫 화면 리스트에 **작업 컬럼 [보기]** 추가(전 행) — 클릭 시 실물 양식 오버레이. ⑵ 학교 컨텍스트의 [보기]도 공정 상세 모달 → **실물 양식 보기**로 교체(작성중 [이어서 작성]은 학교 컨텍스트에만 유지) |

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 첫 화면 9행 전부 [보기] 표시, 한빛초 8/1 제출 건 보기 → 결재란(서명자)·기본정보·점검대상 체크(급식)·항목 ✓/비고("기름때 누적") 표시 확인.

**영향 범위**: 안전점검(/inspection) 첫 화면·학교 컨텍스트. 데이터·백엔드 계약 변경 없음(조회 전용 화면).

**보완 (같은 날)**: 오버레이를 document.body 포탈로 전환 — 앱 레이아웃 내부 렌더 시 인쇄/PDF 출력이 빈 페이지로 나오던 문제 수정(RiskReport와 동일 방식). 헤드리스 인쇄로 실제 PDF 생성 검증 완료.

---

## [055] 2026-08-09 — 학교 컨텍스트 점검 현황에 학교·담당자 컬럼 추가

**요청**: 학교 컨텍스트 점검 현황도 첫 화면과 동일하게 점검일·학교·담당자·작성현황·교육청 전송·작업 구성으로.

**변경** (`src/pages/Inspection.tsx`): 점검 현황 표에 학교(굵게)·담당자 컬럼 추가 — 6컬럼 구성.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 헤더 "점검일 | 학교 | 담당자 | 작성현황 | 교육청 전송 | 작업", 3행 모두 한빛초등학교·김소현 표시.

**영향 범위**: 안전점검 학교 컨텍스트만.

---

## [056] 2026-08-09 — 양식 보기: 표준 점검항목 전체를 항상 표시

**요청·경위**: [보기]에서 8/7 건이 항목 1줄만 보임 — 원인은 과거 데모 시드 점검이 샘플 항목 1~2개만 저장돼 있고, 보기 화면이 저장된 항목만 그렸기 때문.

**변경**:
- `src/components/InspectionSheetView.tsx` — 공정별 표를 **표준 점검항목 전체**(InspectionForm의 PARTDEF 문항 — 급식 46·통학 12·시설 13·미화 문항 등)로 항상 그리고, 저장된 결과·비고를 항목 코드로 매칭해 ✓·비고 표시(미응답 항목은 빈칸 — 실물 양식과 동일). 표준 코드와 다른 과거 기록(구형식 GS-01·SI-01 등)은 표 하단에 이어 표시해 저장값 유실 없음. 부속 설명(||)은 항목 아래 작은 글씨로.
- `src/pages/InspectionForm.tsx` — PARTDEF export(문항 정의 공유).
- `src/styles/inspectsheet.css` — 부속 설명(.sub) 스타일.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 8/7 시드 건: 시설 표준 13문항 + 과거 기록 1행(✓) = 14행 / 샛별유치원 작성중 건: 전 문항 71행·체크 7곳(임시저장 응답·자동 해당없음 비고 포함) 정상 매핑.

**영향 범위**: 양식 보기 화면만. 저장 데이터 변경 없음.

---

## [057] 2026-08-09 — 양식 보기 완성: 기본정보·점검대상·기타 의견·사진대지·확인자 전부 표시

**요청**: [보기] 클릭 시 기본정보, 점검대상, 선택했던 대상의 점검항목, 기타 의견, 사진대지, 확인자가 모두 보여야 함.

**배경**: 기타 의견·사진대지·점검장소·확인자 서명 등은 그동안 화면 입력만 되고 저장되지 않았음(메일 문구에만 사용) — 보기 화면에 표시하려면 저장이 선행되어야 함.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/InspectionForm.tsx` | **부가정보 저장(saveExtras) 신설** — 임시저장·제출 시 기본정보(소속명·부서명·직책·작성자·작성일·점검일·점검장소·재해형태)·점검대상 체크(당직 등 점검표 없는 공정 포함)·기타 의견·사진대지(파일명·미리보기·설명)·확인자 서명을 `/ops/docs/inspection-extras` 문서에 점검표 단위(포함 점검 ID 목록 키)로 저장. InspExtra 타입 export |
| `src/pages/Inspection.tsx` | [보기] 시 부가정보 문서를 조회해 점검 ID로 매칭 → 양식에 전달 |
| `src/components/InspectionSheetView.tsx` | 양식에 섹션 추가 — **기본정보 전 필드 값 표시**(소속명·직책·점검장소·재해형태 등), **점검대상**은 부가정보 기준(점검표 없는 당직도 체크 표시), 공정별 점검항목(기존), **기타 의견** 박스, **사진대지**(공정별 사진 미리보기+설명, 없으면 안내), **확인자**(성명·(서명)·서명일) |
| `src/styles/inspectsheet.css` | 기타 의견·사진대지 그리드·확인자 서명줄 스타일 |

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · E2E — 샛별유치원 폼에서 응답 2건+점검장소+기타 의견 입력·이지현 서명 후 제출 → [보기]에서 점검장소·기타 의견·확인자(이지현·서명일)·사진대지 섹션 모두 표시 확인.

**영향 범위**: 안전점검 작성 폼(저장 시 부가정보 문서 추가 기록)·양식 보기. 백엔드 계약 파일 미수정(범용 /ops/docs 문서 사용 — 기존 패턴). 과거 기록(부가정보 없음)은 빈 값으로 표시.

---

## [058] 2026-08-10 — 홈 오늘의 할 일: 작성중 점검 감지 → [이어서 작성하기] 전환

**요청**: 홈에서 오늘 방문 학교의 종사자 안전·보건 점검표를 작성하다 임시저장하고 나오면, 홈에 다시 들어왔을 때 [안전점검 작성]이 [이어서 작성하기]로 바뀌어야 함.

**변경**:
- `src/components/TodayHero.tsx` — 업무 칩 조회용으로 이미 부르던 학교별 GET /inspections 응답에서 `status === 'draft'`를 감지(추가 API 호출 없음). deriveTasks 반환을 `{ chips, hasDraft }`로 확장 — 작성중 점검이 있으면 '안전점검 · 이번 달 미실시'(주황) 대신 **'안전점검 작성중 · 이어서 작성'**(보라) 칩 표시. 빠른 실행 버튼: hasDraft면 **[이어서 작성하기]** → `/inspection/new?school=&resumeall=1`(점검 현황 [052]와 동일한 점검표 단위 재개 경로), 아니면 기존 [안전점검 작성].
- `src/styles/home.css` — `.hm-qbtn-resume` 강조 스타일(보라 테두리+소프트 배경, 호버 시 보라 채움).

**유의**: '이어서 작성'은 폼에서 [임시저장]을 눌러 작성중 점검이 저장된 경우에만 성립(저장 없이 이탈하면 이어갈 데이터가 없음). 자동 임시저장 도입은 후순위 결정 사항.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 오늘 방문 학교(목포삼학초)에 작성중 점검 생성 후 홈 진입 → 칩 '안전점검 작성중 · 이어서 작성' + [이어서 작성하기] 버튼 표시, 클릭 시 resumeall 폼 진입·해당 공정(급식) 자동 선택 확인.

**영향 범위**: 홈 오늘의 할 일만. 백엔드 계약 변경 없음.

---

## [059] 2026-08-10 — 안전점검 탭 상단 "내 점검 작업" 스트립 신설

**요청**: 오늘 진행한 점검·내가 진행한 점검이 안전점검 탭에서 보이도록 — 전략 논의 결과 작성된 점검표 리스트(아카이브 뷰)는 그대로 두고, 그 위에 얇은 스트립만 얹는 방식 채택.

**변경**:
- `src/pages/Inspection.tsx` — 작성된 점검표 리스트 위에 **"내 점검 작업" 스트립** 신설: ⑴ 작성중 점검표 카드(학교명 · 포함 공정 · [이어서 작성] → `resumeall=1`) ⑵ '오늘 제출 N건' 요약 칩(학교명은 마우스 오버 툴팁). 담당 배정(assigned_inspector_id)이 있으면 담당 학교로 한정(홈 오늘의 할 일 [044]와 동일 규칙 — 배정 없으면 전체), 표시할 내용이 없으면 스트립 자체를 렌더하지 않음. useAuth 연결, School 타입에 assigned_inspector_id 옵션 필드 추가.
- `src/styles/inspecthier.css` — `.inh-mywork*` 스타일 블록(카드는 보라 소프트 배경).

**데이터 제약(알려진 한계)**: 점검 엔티티에 created_at·작성자 필드가 아직 없어 ⑴ '오늘 제출'은 제출일 기준(작성 시작일 판정 불가 — 작성중 건은 날짜 없음) ⑵ '내 작업'은 학교 담당 배정으로 추정. 백엔드에 필드가 추가되면 정확도 개선 가능(프론트는 created_at을 이미 방어적으로 수용).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — admin 로그인: 스트립에 작성중 2건 카드(한빛초 급식·시설 / 목포삼학초 급식) + '담당 학교 기준' 표기, 타 담당 학교(세종고 insp02)의 작성중 건은 카드에서 제외되고 아래 리스트에는 표시, [이어서 작성] 클릭 → `/inspection/new?school=r037&resumeall=1` 진입 확인.

**영향 범위**: 안전점검(/inspection) 첫 화면만. 백엔드 계약 변경 없음.

---

## [060] 2026-08-10 — 학교 허브: 대장 요약 조회 담당 한정 + 세션 캐시

**요청·경위**: 실데이터 771건 시드([026]) 이후 학교 탭 진입 때마다 목서버 콘솔에 학교별 GET /ledger 요청 783건이 순차로 찍히는 현상 문의 → 오류가 아닌 화면 구조(학교별 개별 조회)임을 확인하고 프론트 측 최적화 진행.

**변경** (`src/pages/SchoolsHub.tsx`):
- **대장 요약(종사자수·인원대조) 조회를 담당 학교로 한정** — 배지 집계 [038]과 동일 규칙(assigned_inspector_id 기준, 담당 배정 없는 계정은 기존대로 전체). 담당 외 학교는 종사자수가 '—'(미조회)로 표시됨(전체 학교 스코프는 조회용이므로 허용 — CSV 내보내기의 종사자수·인원대조도 미조회 학교는 빈값/일치로 나감).
- **세션 캐시(모듈 스코프)** — 학교 목록+대장 rows는 계정 기준, 업무 배지는 rows 스냅샷(객체 동일성) 기준으로 캐시. 탭 재진입 시 재조회 0건. 학교 등록·엑셀 일괄 업로드([reload]) 시 강제 재조회 후 캐시 갱신, 브라우저 새로고침 시 캐시 소멸(자연 갱신).

**효과** (목서버 E2E 실측, admin=담당 53교): 첫 진입 /ledger 783건 → 53건(+개발 StrictMode 중복 별도), 배지용 API 포함 총 요청 대폭 감소. 앱 내 탭 재진입 시 **0건**(캐시 적중).

**한계·후속 제안**: 경영 대시보드(/ledger 페이지)는 전 학교 집계가 목적이라 이번 최적화 대상에서 제외(진입 시 학교당 5개 API 호출 지속). 근본 해결은 백엔드에 목록용 벌크 요약 API(예: GET /schools?with=ledger_summary) 신설 요청 — 결합 후 실서버에서도 동일한 N+1 문제가 발생하는 구조이므로 백엔드 팀 전달 권장.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 담당 학교 53교 목록·배지 정상, 전체 학교 783교 목록 정상(종사자수 '—'), 앱 내 사이드바 이동으로 재진입 시 /ledger·/inspections 요청 0건 확인.

**영향 범위**: 학교 허브(/schools)만. 백엔드 계약 파일 미수정.

---

## [061] 2026-08-10 — 점검표 작성: 하단 [임시저장] 버튼 제거

**요청**: [임시저장] 버튼이 상단 헤더(제목 옆)와 하단 제출 바에 중복으로 있음 → 하단 것 제거.

**변경**: `src/pages/InspectionForm.tsx` — 하단 제출 바(insf-sub-r)의 [임시저장] 버튼 삭제. 상단 헤더 버튼과 임시저장 기능([047])은 그대로.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 렌더링 — 하단 바에 추후보완·메일 문구·PDF 변환·저장 버튼만 표시, 상단 임시저장 정상 동작 확인.

**영향 범위**: 안전점검 작성 폼(/inspection/new)만.

---

## [062] 2026-08-10 — 점검표 학교 메일 전송 — PDF 자동 첨부·주소 자동 입력·바로 전송

**요청**: [메일 문구](복사용 텍스트)가 아니라 실제 메일 전송 — [메일] 버튼을 누르면 전송 창이 뜨고, 작성한 안전점검표가 PDF로 자동 첨부, 학교 메일 주소 자동 입력, 본문 작성 후 바로 전송되어야 함.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/InspectionMailModal.tsx` | **신규** — 학교 메일 전송 모달. 받는 사람(학교 이메일 자동, 수정 가능)·제목(자동)·첨부(점검표 PDF 자동 생성, 쪽수 표시·미리보기 링크)·본문(기존 메일 문구 초안 프리필, 자유 수정). [보내기] → POST /mail/send(첨부 base64 포함) → 전송 완료 표시. **PDF 생성**: 양식 본문을 화면 밖에 렌더 → html2canvas로 캡처 → jsPDF로 A4 다중 페이지 조립(한글·체크·사진 그대로 래스터화) |
| `public/vendor/html2canvas.min.js` · `jspdf.umd.min.js` | **신규(동봉 라이브러리)** — CDN 의존 없이 오프라인에서도 PDF 생성 가능. npm 설치 불필요(정적 파일) |
| `src/components/InspectionSheetView.tsx` | 양식 본문을 `InspectionSheetBody`로 분리 export — 오버레이 보기([054])와 메일 PDF 캡처가 공용 사용(마크업 변경 없음) |
| `src/pages/InspectionForm.tsx` | [✉ 메일 문구] 버튼·복사 패널 제거 → **[✉ 메일]** 버튼(모달 열기). 저장 완료 시 모달 자동 오픈. 현재 입력 상태를 SheetData로 조립하는 buildSheet() 신설(자동 해당없음·서명 반영). 학교 이메일은 대장 응답에 없으면 학교 목록에서 폴백. 완료 안내 문구를 메일 버튼 안내로 교체 |
| `src/styles/inspectform.css` | `.insm-*` 모달 스타일 블록 추가 |
| `mock-server.mjs` | POST /mail/send 핸들러 추가(성공 응답 mock) — **적용 후 목서버 재시작 필요(off.bat → start.bat)** |

**백엔드 전달 사항**: 실서버에는 메일 발송 API `POST /mail/send {to, subject, body, attachments:[{name, content_base64}]}` 구현 필요(SMTP — 설정 페이지의 메일 연동 계정 활용 가능). 프론트는 이 규격으로 이미 호출.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 한빛초 작성 화면에서 [✉ 메일] 클릭 → 받는 사람 hanbit@sen.go.kr 자동 입력·첨부 "종사자안전보건점검표_한빛초등학교_2026-08-10.pdf · 4쪽" 생성·미리보기 정상, [보내기] → 전송 완료 표시. 첨부 PDF 실물 확인(4쪽, 기본정보·점검대상 체크·문항 표·자동 해당없음 비고 정상 렌더).

**영향 범위**: 안전점검 작성 폼(/inspection/new)·양식 보기 컴포넌트 내부 구조. 백엔드 계약 파일 미수정.

---

## [063] 2026-08-10 — 업무 탭 4곳 첫 화면 검색을 학교 탭과 동일한 패널로 교체

**요청**: 안전점검·위험성평가·근골격계·이행점검 첫 화면의 검색 바(학교명·담당자 검색 인풋 + 상태/학교급 드롭다운 + 내보내기)를 없애고, 학교 탭의 검색 영역(학교명·지역명 입력 + 학교급 세그먼트 + [조회])을 넣기.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/table/WorkSearchPanel.tsx` | **신규(공용)** — 학교 탭 검색 영역([020][025])과 동일한 확정형 패널. 학교명·지역명 입력(X 즉시 해제)·학교급 세그먼트·[조회]/Enter 확정. 스타일은 schoolhub.css shub-search 재사용 |
| `Inspection.tsx` · `Risk.tsx` · `Musculo.tsx` · `Compliance.tsx` | 첫 화면(작성물 리스트) 헤더의 FilterBar(검색+드롭다운)·내보내기 버튼 제거 → 리스트 위에 WorkSearchPanel 배치. 학교명·지역명은 useTableQuery 밖 선필터(주소 부분일치·AND), 학교급은 기존 level 필터로 반영(Risk·Musculo·Compliance 리스트 쿼리에 level 필터 정의 추가). School 타입에 address 필드 보강(Inspection·Compliance) |

**사라진 것(유의)**: 각 탭의 상태별 드롭다운 필터(작성현황·구분·부담작업/검수·상태)와 CSV 내보내기 버튼 — 첫 화면에서 제거됨(요청 반영). 상태는 리스트의 상태 칩·정렬로 확인 가능. 필요 시 복원 가능(코드 잔존: 각 쿼리 config의 필터 정의·EXPORT 컬럼 정의는 유지).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 4개 탭 모두 패널 표시·구 검색바 0건 확인, 안전점검에서 학교명 "한빛" 조회 → 7건 → 2건 필터 동작 확인.

**영향 범위**: 4개 업무 탭의 첫 화면 검색 영역만. 학교 컨텍스트·작성 화면 변경 없음(근골격계 학교 컨텍스트의 검색·내보내기는 유지). 백엔드 계약 파일 미수정.

---

## [064] 2026-08-10 — 학교 목록 헤더: 업무 바로가기 범례·내보내기 버튼 제거

**요청**: 학교 목록 헤더의 "업무 바로가기: ● 완료 ● 미완료" 범례([041])와 [내보내기] 버튼 제거 — [담당 학교]/[전체 학교] 토글만 유지.

**변경**: `src/pages/SchoolsHub.tsx` — shub-legend 범례 span·ExportButton 삭제(HUB_EXPORT 컬럼 정의는 잔존 — 복원 대비). 배지 의미(완료/미완료)와 세부 상태는 기존대로 배지 마우스 오버 툴팁으로 확인 가능.

**롤백 방법**: git 커밋 revert ([041] 범례·내보내기 복원).

**검증**: `tsc --noEmit` 통과 · 렌더링 — 헤더에 담당/전체 토글만 표시 확인.

**영향 범위**: 학교 허브(/schools) 목록 헤더만. CSV 내보내기 기능이 학교 탭에서 사라짐(필요 시 복원 요청).

---

## [065] 2026-08-10 — 오늘의 할 일: 모든 업무에 '이어서 작성하기' 확장

**요청**: [058]의 안전점검뿐 아니라 근골격계 등 오늘의 할 일에 뜨는 업무 전부가 진행중이면 '이어서 작성하기'가 되도록.

**변경** (`src/components/TodayHero.tsx`):
- **근골격계**: [근골격계 보고서] 버튼이 작성중 초안이 있으면 **[근골격계 이어서 작성하기]**(보라 강조)로 전환. 판정은 MusculoReport의 localStorage 초안(`mur:draft:v2:{학교id}`)에 의미 있는 입력(명단 성명·부담작업 선택·위험요인·사진·개선계획)이 있는지로 — 보고서 화면이 초안을 자동 저장·복원하므로 같은 URL 진입만으로 이어서 작성됨.
- **위험성평가**: 진행중 평가(status≠completed)가 있으면 **[위험성평가 이어서 작성하기]** 버튼 노출 → `/risk?school=` 딥링크(학교 컨텍스트의 이어서 작성 흐름 [010]).
- **이행점검**: 작성중(draft) 건이 있으면 **[이행점검 이어서 작성하기]** 노출 → `/compliance?school=`. (판정은 칩과 동일하게 기존 /compliance API 기준 — 조사지 문서 기반 판정 전환은 후속 검토)
- 안전점검 이어서 작성하기 라벨을 "안전점검 이어서 작성하기"로 명시(여러 업무 버튼과 구분). deriveTasks 반환을 업무별 진행 플래그(inspDraft·riskDoing·compDraft)로 확장 — 추가 API 호출 없음(기존 4개 API 응답 재사용).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 한빛초(근골 초안 시드): [안전점검 작성 | 근골격계 이어서 작성하기 | 대장 보기], 목포삼학초(점검 임시저장): [안전점검 이어서 작성하기 | 근골격계 보고서 | 대장 보기] 확인.

**영향 범위**: 홈 오늘의 할 일 빠른 실행 버튼만. 백엔드 계약 변경 없음.

---

## [066] 2026-08-10 — 위험성평가·근골격계·이행점검에도 "내 작업" 스트립 신설

**요청**: 안전점검 [059]의 "내 점검 작업" 스트립처럼 위험성평가·근골격계·이행점검 첫 화면에도 작성중 건 카드 + [이어서 작성]이 나오도록.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/components/table/MyWorkStrip.tsx` | **신규(공용)** — 작성중 카드(학교명 · 부제 · [이어서 작성]) 스트립. 건이 없으면 미렌더. 스타일은 inspecthier.css inh-mywork 재사용 |
| `src/lib/musDraft.ts` | **신규(공용)** — 근골격계 보고서 localStorage 초안 감지([065]의 판정을 모듈로 분리, 홈·근골 탭 공용) |
| `src/pages/Risk.tsx` | "내 평가 작업" — 정기 '작성 중'·수시 '진행 중' 건 카드, [이어서 작성] → 학교 컨텍스트(수시는 수시 탭) 진입 |
| `src/pages/Musculo.tsx` | "내 보고서 작업" — 보고서 초안(localStorage) 있는 학교 카드, [이어서 작성] → /musculo/report?school= (초안 자동 복원) |
| `src/pages/Compliance.tsx` | "내 조사지 작업" — 미제출(작성중) 조사지 카드, [이어서 작성] → 해당 조사지 편집 화면 직접 진입 |
| `src/components/TodayHero.tsx` | hasMusDraft를 lib/musDraft로 이동(중복 제거) |

공통 규칙([059]와 동일): 담당 배정(assigned_inspector_id)이 있으면 담당 학교 기준, 없으면 전체. School 타입에 assigned_inspector_id 보강(Risk·Musculo·Compliance).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 근골(한빛초 초안): "보고서 초안 작성중" 카드 → 보고서 화면 진입, 위험성평가(한빛초 정기 섹션 1/5): "정기 · 섹션 1/5" 카드 → 학교 컨텍스트 진입, 이행점검(조사지 draft 시드): "2026년 하반기" 카드 → 조사지 편집 화면 직접 진입 확인. 작성중 없는 탭은 스트립 미표시.

**영향 범위**: 3개 업무 탭 첫 화면 + 홈(내부 리팩터). 백엔드 계약 변경 없음.

---

## [067] 2026-08-11 — 학교 컨텍스트 점검 현황: 점검일 클릭 → 양식 보기

**요청**: 학교 컨텍스트(안전점검 › 학교) 점검 현황 리스트에서 점검일(예: 2026-08-09)을 누르면 점검표를 볼 수 있어야 함.

**변경**:
- `src/pages/Inspection.tsx` — 점검 현황 테이블의 점검일 셀을 클릭 가능한 링크 버튼(`.inh-datelink`)으로 전환. 제출·서명완료 건은 [보기]와 동일하게 양식 보기 오버레이(openSheet [054]) 열기, 작성중(draft) 건은 [이어서 작성]과 동일하게 `resumeall=1` 폼 진입(작업 컬럼 버튼과 행동 일치). 날짜 없는 건('—')은 비활성 텍스트 유지. 툴팁으로 '점검표 양식 보기'/'이어서 작성' 안내.
- `src/styles/inspecthier.css` — `.inh-datelink` 스타일(평시 본문색, 호버 시 보라 + 밑줄).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · vite build 통과 · 목서버 E2E(Playwright) — admin 로그인 → 한빛초등학교 컨텍스트 진입 → 2026-08-09 날짜 호버 시 보라 링크 표시, 클릭 시 양식 보기 오버레이(종사자 안전·보건 점검표 · 시설관리 체크) 정상 오픈 확인.

**영향 범위**: 안전점검 학교 컨텍스트(/inspection?school=)의 점검 현황 테이블만. 첫 화면 리스트·작성 화면·백엔드 계약 변경 없음.

---

## [068] 2026-08-11 — 점검일 클릭: 양식 보기 → 작성 화면(입력 폼)으로 변경

**요청**: [067]의 점검일 클릭이 읽기 전용 양식 보기로 열림 → 제출·서명완료 건도 작성 화면(입력 폼)으로 들어가 내용을 수정할 수 있어야 함.

**변경**:
- `src/pages/InspectionForm.tsx` — **수정 모드 신설**: URL 파라미터 `edit=점검ID,점검ID…`가 있으면 상태와 무관하게 해당 점검들의 기존 결과·비고를 폼에 프리필하고, 점검대상은 그 점검표에 포함된 공정만 선택. draftIds에 기존 점검 ID를 매핑해 [임시저장]·[저장] 모두 **새 점검을 만들지 않고 같은 점검에 덮어씀**(이어서 작성 [052]와 동일한 재사용 구조).
- `src/pages/Inspection.tsx` — 학교 컨텍스트 점검 현황의 점검일 클릭: 제출·서명완료 건은 양식 보기(openSheet) 대신 `edit=` 파라미터로 작성 화면 진입. 작성중 건은 기존대로 `resumeall=1`. [보기] 버튼(양식 보기)은 그대로 유지.

**유의(알려진 한계)**:
- 구 시드 데이터(i01·i02 등)는 항목 코드가 옛 규격(GS-01·SI-01)이라 프리필되지 않음(공정 선택까지만) — 폼으로 작성·제출한 점검표는 전부 정상 프리필. 실서버 결합 시에는 폼 규격 코드만 존재하므로 문제 없음.
- 기타의견·사진대지 등 부가정보(extras)는 프리필되지 않음(이어서 작성도 동일) — 필요 시 후속 작업.
- 수정 후 재저장 시 목서버가 submitted_at을 현재 시각으로 갱신 → 점검 현황의 점검일이 재제출일로 바뀜. 실서버 정책(수정 이력 관리) 확인 필요.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E(Playwright) — 폼 규격으로 제출된 점검표(시설 13항목: 양호 10·미흡 1(비고 '조명 교체 필요')·해당없음 2) 생성 후 점검일 클릭 → `/inspection/new?school=s01&edit=…` 진입, 시설관리만 선택·13개 항목 결과와 비고 전부 프리필 확인.

**영향 범위**: 안전점검 작성 폼의 URL 파라미터 처리 + 학교 컨텍스트 점검일 클릭 동작. 백엔드 계약 파일 미수정.

---

## [069] 2026-08-11 — 점검 현황: 날짜 링크 → 행 전체 클릭으로 변경

**요청**: [068]은 점검일 텍스트만 클릭 가능했음 → 행(점검일부터 [보기]까지의 줄) 어디를 눌러도 작성 화면으로 들어가야 함.

**변경**:
- `src/pages/Inspection.tsx` — 점검일 셀의 링크 버튼 제거(일반 텍스트 복원), 행(`<tr>`)에 onClick 부여: 작성중 → `resumeall=1`, 제출·서명완료 → `edit=` 수정 모드([068]과 동일 분기). [이어서 작성]·[보기] 버튼에는 `stopPropagation()`을 추가해 행 클릭과 분리 — [보기]는 기존대로 양식 보기 오버레이. 행 툴팁에 "클릭하면 작성 화면으로 이동" 안내 추가. 행 hover 배경·포인터 커서는 공용 `.tbl` 스타일이 이미 제공.
- `src/styles/inspecthier.css` — [067]의 `.inh-datelink` 스타일 블록 제거(미사용).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E(Playwright) — 제출 건 행의 담당자 칸 클릭 → `/inspection/new?school=s01&edit=…` 진입, 작성중 건 행 클릭 → `resumeall=1` 진입, [보기] 버튼 클릭 시 화면 이동 없이 양식 보기 오버레이만 열림 확인.

**영향 범위**: 학교 컨텍스트 점검 현황 테이블만. 백엔드 계약 변경 없음.

---

## [070] 2026-08-11 — 업무 탭 4곳 리스트에 구분(학교급) 컬럼 추가

**요청**: 학교 탭 목록처럼 안전점검·위험성평가·근골격계·이행점검 첫 화면 리스트에도 유/초/중/고 구분이 보이도록.

**변경** (`Inspection.tsx` · `Risk.tsx` · `Musculo.tsx` · `Compliance.tsx`):
- 각 첫 화면 리스트의 학교 컬럼 앞에 **구분 컬럼** 추가 — 학교 탭과 동일한 보라 칩(`pillx doing`), 미지정 학교는 '—'. 헤더는 정렬 가능(SortableTh, 유→초→중→고→기타 순), 각 리스트 쿼리에 level 정렬 접근자 추가. 로딩·빈 상태 행의 colSpan 보정.
- **위험성평가 유의**: 기존 '구분'(정기/수시) 컬럼과 이름이 겹쳐 기존 컬럼을 **'평가 구분'**으로 개명하고, 학교급 컬럼이 '구분'을 사용(학교 탭과 용어 통일).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 4개 탭 헤더에 구분 컬럼 확인(안전점검: 점검일·구분·학교…, 위험성평가: 최근 저장·평가 구분·보고서·구분·학교…), 안전점검 리스트에서 초/중/고 칩 정상 표시·정렬 동작 확인.

**영향 범위**: 4개 업무 탭의 첫 화면 리스트만. 학교 컨텍스트·작성 화면·백엔드 계약 변경 없음.

---

## [071] 2026-08-11 — 용어 통일: 작성중 · 제출 완료 · 구분

**요청**: 화면마다 어긋난 용어를 ⑴ draft 상태는 **작성중**(붙여쓰기) ⑵ submitted 상태 칩은 **제출 완료** ⑶ 학교급 라벨은 **구분**으로 통일.

**변경** (UI 노출 문자열만 — 코드 식별자·주석·백엔드 계약 미변경):

| 통일 | 변경 지점 |
|---|---|
| '작성 중' → **작성중** | `Compliance.tsx`(상태 칩·필터) · `Risk.tsx`(보고서 진행 2곳) · `MusculoReport.tsx`(진행 칩) · `SchoolsHub.tsx`(배지 툴팁) · `TodayHero.tsx`(홈 칩 '이행점검 작성중') |
| '제출' → **제출 완료** | `Inspection.tsx`(상태 칩·필터·학교 목록 CSV 헤더) · `SchoolDetail.tsx`(안전점검·이행점검 요약 칩 2곳) |
| '학교급'·'학교급(구분)' → **구분** | 필터 라벨 9곳(`Home`·`Inspection`×2·`SchoolsHub`·`SchoolStatus`·`Musculo`·`Compliance`×2·`Risk`·`Education`) · CSV 헤더 2곳(`Inspection`·`Education`) · 폼 라벨(`SchoolFormModal`·`SchoolStatus`·`SchoolDetail` 편집·정보) · 교육 테이블 헤더(`Education`) · 세션 발급 드롭다운 '구분 전체'(`Sessions`) · aria-label 2곳(`WorkSearchPanel`·`SchoolsHub`) |

**유지(의도적)**: '작성 완료'(다른 상태) · '전송완료'(교육청 전송 계열) · 경영 대시보드 차트 제목 '학교급별 안전점검 현황'(복합어라 유지) · CycleHero 간트 마크 '제출'(시점 표기).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 안전점검 리스트 칩이 작성중/서명완료/제출 완료/전송완료로 표시 확인.

**영향 범위**: 표시 문구만. API 값·필터 키·정렬 동작 변경 없음.

---

## [072] 2026-08-11 — 점검표 작성: 자동 해당없음 배너 간소화 + 링크 문구 '대장'

**요청**: 자동 해당없음 배너의 사유별 상세 나열(엘리베이터 없음 — 시설 5·시설 6 …)이 과함 → 요약 문구("학교 특징에 따라 N개 항목이 자동으로 '해당없음' 처리되었습니다")만 표시. 우측 링크 문구는 '학교 특징 확인' → **'대장'**으로 변경.

**변경** (`src/pages/InspectionForm.tsx`):
- 자동 해당없음이 있을 때 배너의 사유별 상세 행(insf-auto-row) 제거 — 한 줄 요약만. 사유는 기존대로 각 항목 행 비고("자동 해당없음 — {사유}")와 [학교 특징 반영] 힌트에서 확인 가능. 자동 처리 0건·특징 미저장 시의 안내 문구는 유지.
- 우측 링크 '학교 특징 확인 →' → **'대장 →'** (이동 경로 /schools/{id}는 동일).
- 미사용이 된 reasons 구조분해 정리(자동 판정 로직 autoExcl 자체는 변경 없음).

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 한빛초 작성 화면 배너가 요약 한 줄 + '대장 →'만 표시되는 것 스크린샷 확인.

**영향 범위**: 안전점검 작성 폼 배너 표시만. 자동 해당없음 판정·저장 동작 변경 없음.

---

## [073] 2026-08-11 — 안전점검·근골격계·위험성평가 탭 N+1 요청 폭주 해소 (세션 캐시)

**요청·경위**: 홈 공지사항 등이 "불러오는 중…"에서 멈추고 목서버 콘솔에 GET /inspections가 끝없이 찍히는 현상 문의(경로 충돌 의심) → 원인은 라우트 겹침이 아니라, 업무 탭 3곳이 첫 화면 리스트를 만들 때 **783개 학교 전체에 학교별 조회를 발사(N+1)**하는 구조. 탭에 들어갈 때마다 재발사되어 목서버가 밀리고 홈의 요청이 뒤로 밀렸음(목서버 로그는 쿼리스트링을 생략해 같은 경로처럼 보임). 학교 허브는 [060]에서 이미 캐시 처리 — 같은 패턴을 업무 탭에 적용.

**변경** (`Inspection.tsx` · `Musculo.tsx` · `Risk.tsx` — 모두 [060]과 동일 패턴):
- **모듈 스코프 세션 캐시** — 학교 목록 + 학교별 점검/조사/평가 맵을 계정 기준으로 캐시. 탭 재진입 시 재조회 0건, 브라우저 새로고침 시 캐시 소멸(자연 갱신).
- **개별 갱신 경로 유지** — 점검·조사·평가 생성 후 refetchSchool(해당 학교 1건만 재조회)이 화면 상태와 캐시를 함께 갱신.
- **위험성평가 보고서 문서(/risk/survey) 캐시 추가** — 문서 저장(학교 컨텍스트·조사표 탭) 시 첫 화면 보고서 리스트와 캐시에 즉시 반영(기존에는 재진입 전까지 미반영이던 것도 개선).
- 이행점검은 문서 1건 조회 구조라 N+1 없음(변경 불필요).

**효과** (목서버 E2E 실측): 안전점검 첫 진입 788건 → 사이드바 재진입 시 **0건**(홈 화면 자체 조회분 제외). 근골격계·위험성평가 동일.

**한계·후속 제안**: [060]과 동일 — 학교 등록·엑셀 업로드 직후에는 업무 탭 캐시에 신규 학교가 없을 수 있음(새로고침 시 반영). 근본 해결은 백엔드 벌크 API(예: GET /inspections?all=1 또는 /schools?with=summary) 신설 — 백엔드 팀 전달 권장.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과 · 목서버 E2E — 사이드바 이동으로 안전점검 재진입 시 학교별 조회 0건·리스트 정상 렌더 확인.

**영향 범위**: 3개 업무 탭의 데이터 로딩 방식만. 표시·백엔드 계약 변경 없음.

---

## [074] 2026-08-11 — 점검표 저장 버튼 문구: '저장 후 SHM 전송' → '전남교육청 업로드'

**요청**: 하단 제출 바의 보라색 버튼 문구 변경.

**변경**: `src/pages/InspectionForm.tsx` — 버튼 '저장 후 SHM 전송' → **'전남교육청 업로드'**, 진행 중 문구 '전송 중…' → '업로드 중…'. 동작(저장→서명→제출→교육청 전송)은 변경 없음.

**롤백 방법**: git 커밋 revert.

**검증**: `tsc --noEmit` 통과.

**영향 범위**: 안전점검 작성 폼 버튼 문구만.

---

## [075] 2026-08-12 — 인쇄/PDF 출력 시 배경 그래픽 강제 유지 (보고서 물결 등)

**요청·경위**: 정기 위험성평가 결과보고서를 인쇄/PDF 저장하면 표지의 물결 그래픽·색상 박스가 빠진 채 출력됨 — 원인은 물결이 이미지 파일이 아니라 CSS 배경(그라디언트+clip-path)이고, 브라우저가 인쇄 시 배경 그래픽을 기본으로 제외하기 때문(그동안은 인쇄 창의 "배경 그래픽" 체크로 우회 — [009] 권장사항). 체크 없이도 항상 나오도록 CSS에서 강제.

**변경**:

- `src/styles/riskreport.css` — `.rr-overlay`에 `-webkit-print-color-adjust: exact; print-color-adjust: exact;` 추가. 상속되는 속성이라 하위 전체(표지 물결·"2026" 박스·위험성 매트릭스 색·8점 이상 노란 강조 등)에 적용. **이행점검 조사지 인쇄(ComplianceSheet)도 rr-overlay·rr-page를 재사용하므로 함께 해결됨**(주석으로 명시).
- `src/styles/inspectsheet.css` — `.inss-overlay`에 동일 속성 추가 (안전점검표 양식 보기 인쇄).

**유의**: 향후 별도의 새 인쇄 화면(예: 수시 위험성평가 보고서)을 신설할 때는 그 화면의 최상위 컨테이너에도 같은 두 줄을 넣을 것(관례). 양식이 개정되어 화면 내용·배경이 바뀌어도 이 규칙은 자동 적용되므로 재작업 불필요. 메일 첨부 PDF(html2canvas 캡처)는 원래 배경이 포함되므로 영향 없음.

**롤백 방법**: 두 CSS의 `print-color-adjust` 2줄(+주석)씩 삭제.

**검증**: CSS만 수정(TSX 미변경). 인쇄 미리보기에서 "배경 그래픽" 체크 없이 물결·색상 출력 확인 요망(브라우저 인쇄 창 기본 상태로 확인).

**영향 범위**: 보고서 출력(RiskReport)·이행점검 조사지 인쇄·안전점검표 양식 보기 3곳의 인쇄 결과만. 화면 표시·데이터·백엔드 계약 변경 없음.

---

## [076] 2026-08-12 — 업무 탭 3곳 첫 화면 조회를 담당 학교로 한정 (N+1 축소)

**요청·경위**: 위험성평가 탭 진입 시 목서버 콘솔에 GET /risk/survey가 수백 건 쏟아지는 현상 문의. 원인은 첫 화면(작성된 보고서 리스트)이 **전체 학교 783교 각각에 학교별 API를 개별 호출**하는 구조 — [073]의 세션 캐시는 같은 브라우저 세션 내 재진입만 막아주고, 새로고침/첫 진입에는 여전히 783건 발생(개발 StrictMode에서는 최대 2배). 안전점검·근골격계 탭도 동일 구조. → **담당 학교 한정 조회** 채택([060] 학교 허브와 동일 규칙).

**변경** (`src/pages/Risk.tsx` · `Inspection.tsx` · `Musculo.tsx` — 각각 동일 패턴):

- 공용 헬퍼 `scopeToAssigned(schools, login)` 추가 — 로그인 계정의 담당 학교(assigned_inspector_id 일치)가 있으면 그 학교들만, 담당 배정이 없는 계정은 기존대로 전체 반환.
- 첫 화면의 학교별 병렬 조회 루프에 적용: Risk는 2곳(GET /risk + GET /risk/survey), Inspection(GET /inspections)·Musculo(GET /musculo)는 각 1곳. [073] 세션 캐시·특정 학교 재조회(refetchSchool)는 그대로.

**효과**: admin(담당 53교) 기준 첫 진입 요청 783건 → 53건 (Risk 탭은 2종이라 1,566 → 106건). 학교 컨텍스트 진입 시 해당 학교 개별 재조회는 유지되므로 데이터 최신성 문제 없음.

**트레이드오프(유의)**: 담당 배정이 있는 계정의 첫 화면 리스트에는 **담당 학교의 작성물만** 표시됨(다른 담당자 작성분 미표시 — "내 작업" 중심 방향과 일치). 전체 열람이 필요한 관리 계정은 담당 배정을 두지 않으면 전체 조회 유지. 근본 해결은 백엔드 벌크 API(예: GET /risk/surveys 일괄 목록) — [060]에 이어 재차 백엔드 팀 전달 권장.

**롤백 방법**: 3개 파일에서 `scopeToAssigned` 헬퍼와 호출 4곳(`// [076]` 주석)을 `schools`로 원복.

**검증**: esbuild 문법 통과 · 목서버 콘솔에서 위험성평가 탭 첫 진입 시 /risk/survey 요청 수 감소 확인 요망(783 → 53건).

**영향 범위**: 위험성평가·안전점검·근골격계 탭 첫 화면의 조회 범위만. 학교 컨텍스트·작성 화면·백엔드 계약 변경 없음. 이행점검은 문서 1건 조회 구조라 해당 없음.

---

## [077] 2026-08-12 — 정기 위험성평가 보고서 작성 화면 상단에 [임시저장] 버튼 추가

**요청·경위**: "정기 위험성평가에는 왜 임시저장이 없나" — 기존에도 각 탭(①~⑤) 하단의 [저장]이 제출 개념 없이 문서를 저장하는 사실상의 임시저장이었으나, 탭 하단까지 스크롤해야 보이고 명칭도 달라 임시저장이 없는 것으로 인지됨. 안전점검 폼([047])처럼 상단 헤더에 항상 보이는 버튼으로 추가.

**변경**: `src/pages/Risk.tsx` — 보고서 작성 화면(regularEdit) 헤더의 [보고서 출력] 왼쪽에 **[임시저장]** 버튼 추가. 기존 saveSurvey(PUT /risk/survey — 하단 [저장]과 동일 동작)를 호출하며, 저장 중에는 '저장 중…' 표시, 완료 시 저장 시각 메시지를 헤더에도 표시. 어느 탭에서 눌러도 ①~⑤ 전체 작성 내용이 문서에 저장됨(문서 단위 저장 구조).

**롤백 방법**: 헤더의 `// [077]` 주석 블록(버튼 + surveyMsg span) 삭제.

**검증**: esbuild 문법 통과 · 헤더 버튼 클릭 → 저장 메시지 표시·첫 화면 리스트 '작성중' 반영 확인 요망.

**영향 범위**: 위험성평가(/risk) 보고서 작성 화면 헤더만. 각 탭 하단 [저장]·[다음 →]은 그대로. 백엔드 계약 변경 없음.

---

## [078] 2026-08-12 — 위험성평가 "작성된 보고서" 리스트: 평가 구분 ↔ 구분 컬럼 위치 교환

**요청**: 첫 화면 리스트에서 "평가 구분"(정기/수시)과 "구분"(학교급) 컬럼의 위치를 서로 교환.

**변경**: `src/pages/Risk.tsx` — 컬럼 순서 최근 저장 · **구분(학교급)** · 보고서 · **평가 구분(정기/수시)** · 학교 · 진행 · 상태로 변경 (thead th 2개 + tbody td 2개 자리만 교환 — 정렬·데이터 로직 변경 없음).

**롤백 방법**: 두 th·두 td를 다시 맞바꾸기.

**검증**: esbuild 문법 통과.

**영향 범위**: 위험성평가(/risk) 첫 화면 리스트 컬럼 순서만.

---

## [079] 2026-08-12 — 업무 탭 3곳 첫 화면 리스트를 안전점검과 동일 컬럼 구성으로 통일

**요청**: 위험성평가 "작성된 보고서"·근골격계 "작성된 조사"·이행점검 "작성된 조사지" 리스트를 안전점검 "작성된 점검표"처럼 **날짜 · 구분 · 학교 · 담당자 · 작성현황 · 작업** 구성으로 통일하고, 위험성평가·근골격계에만 **정기/수시 구분(평가 구분)** 컬럼 추가.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/Risk.tsx` | 컬럼: 최근 저장 · 구분 · 학교 · 담당자 · 평가 구분(정기 보라/수시 주황) · 작성현황 · 작업[보기]. 보고서명·진행(섹션 n/5·8점 이상)은 작성현황 pill **툴팁**으로 이동. [보기]=행 클릭과 동일(학교 컨텍스트 진입, 수시는 수시 탭) |
| `src/pages/Musculo.tsx` | 컬럼: 조사일 · 구분 · 학교 · 담당자 · 평가 구분 · 작성현황 · 작업[보기]. Survey 타입에 `category?: 'regular'\|'adhoc'` 추가(없으면 정기로 간주 — 방어적). 작성현황 판정: 검수 대기 N건(주황) > 작성 완료(초록, 기본조사·증상조사표 있음) > 작성중(보라). 기본조사·증상조사표·부담작업 수치는 툴팁으로 |
| `src/pages/Compliance.tsx` | 컬럼: 반기 · 구분 · 학교 · 담당자 · 작성현황 · 작업[보기] (정기/수시 없음 — 요청 반영). 결과 기입 n/8·제출일은 작성현황 툴팁으로. 미사용된 ChevronRight import 정리 |
| `mock-server.mjs` | 근골격계 조사에 category 필드 시드 — 시드 3건·POST /musculo는 'regular', 산재 연동 생성(/accidents/:id/generate target=musculo)은 'adhoc'. **적용 후 목서버 재시작 필요** |

**백엔드 전달 사항**: 근골격계 조사 엔티티에 `category`('regular'|'adhoc') 필드 추가 필요(산재 연동 생성 시 'adhoc') — 없으면 화면에는 전부 '정기'로 표시됨(무해).

**롤백 방법**: git 커밋 revert. 각 파일 `// [079]` 주석 블록 기준 개별 원복 가능.

**검증**: esbuild 문법 통과(3개 TSX) · mock-server 파싱 정상 · 렌더링 확인 요망(목서버 재시작 후 세 탭 컬럼 구성·툴팁·[보기] 버튼).

**영향 범위**: 3개 업무 탭 첫 화면 리스트만. 행 클릭 동작·학교 컨텍스트·정렬 훅·백엔드 계약 파일 변경 없음.

---

## [080] 2026-08-12 — 안전점검 "작성된 점검표": 행 클릭 → 점검표 작성 화면 바로 열기

**요청·경위**: 첫 화면 리스트에서 행을 클릭하면 학교의 점검 현황(또 다른 목록)으로 이동해 "리스트 → 리스트"로 헛도는 동선 문제 제기. 전략 확정 — **업무 탭 리스트=문서함(행 클릭 시 그 점검표가 바로 열림) / 학교 탭 바로가기=작업장(학교별 목록+작성 버튼)** 역할 분담. 완료(서명완료·제출) 건도 행 클릭 시 작성 화면(수정 가능)으로 열기로 결정.

**변경** (`src/pages/Inspection.tsx`):

- 첫 화면 행 클릭: `openSchool(학교)` → **점검표 작성 화면 직접 진입** — 작성중은 `resumeall=1`(이어서 작성), 서명완료·제출 건은 `edit=점검ID들`(프리필+같은 점검에 덮어쓰기). 학교 컨텍스트 점검 현황의 [068][069] 규칙과 동일 — InspectionForm의 기존 resumeall·edit 모드 재사용(폼 변경 없음).
- [보기] 버튼(실물 양식)·하단 안내 문구 정리. 학교 컨텍스트 진입 경로는 학교 탭 바로가기로 유지.

**롤백 방법**: 행 onClick을 `openSchool(r.school)`로 원복 + 안내 문구 원복.

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 8/11 서명완료 행 클릭 → 작성 화면(응답 프리필) / 작성중 행 클릭 → 이어서 작성.

**영향 범위**: 안전점검(/inspection) 첫 화면 행 클릭 동선만. 위험성평가·근골격계·이행점검 리스트의 행 클릭(학교 화면 진입)은 아직 기존대로 — 같은 원칙 적용은 별도 요청 시.

---

## [081] 2026-08-12 — 위험성평가·근골격계·이행점검도 행 클릭 → 작성 화면 직행 ([080] 확장)

**요청**: [080]의 원칙(업무 탭 리스트=문서함, 행 클릭 시 해당 작성물이 바로 열림)을 나머지 3개 탭에도 적용.

**변경 파일**:

| 파일 | 행 클릭/[보기] 동작 (구 → 신) |
|---|---|
| `src/pages/Risk.tsx` | 학교 화면 진입 → **정기: ①~⑤ 보고서 작성 화면 직행** (openReportRow — 학교 변경 시 편집모드를 해제하는 기존 effect를 직행 진입에서만 건너뛰는 directEditRef 플래그 추가) · **수시: 해당 케이스 4단계 에디터 직행** (adhocOpenId state → AdhocSection initialCaseId, 케이스 목록 로드 완료 시 자동 선택). RiskReportRow에 caseId 추가. [정기] 전환 버튼·openSchool에서 직행 대상 초기화. 하단 안내 문구 갱신 |
| `src/features/risk/AdhocAssessment.tsx` | AdhocSection에 `initialCaseId` 옵션 prop 추가 — 케이스 목록 로드 후 해당 케이스 에디터 자동 오픈 |
| `src/pages/Musculo.tsx` | 학교 화면 진입 → **해당 조사 상세(검수·통계) 직행** — openSchool 뒤 setSurveyId(조사 id) (같은 클릭 핸들러 내 배치라 openSchool의 초기화보다 나중 값이 적용됨) |
| `src/pages/Compliance.tsx` | 학교 조사지 목록 진입 → **해당 조사지 편집 화면 직행** — setEditKey(반기 키) |

**롤백 방법**: git 커밋 revert. 개별: 각 파일 `[081]` 주석 지점의 onClick을 구 동작(openSchool/setSel+setEditKey(''))으로 원복, AdhocSection prop·effect 삭제.

**검증**: esbuild 문법 통과(4개 파일) · 렌더링 확인 요망 — 정기 보고서 행 → ①~⑤ 화면, 수시 행 → 케이스 에디터, 근골 조사 행 → 조사 상세, 이행점검 행 → 조사지 편집.

**영향 범위**: 3개 업무 탭 첫 화면의 행 클릭 동선만. 학교 탭 바로가기(학교별 목록+작성 버튼)는 기존대로 — [080] 역할 분담 완성.

---

## [082] 2026-08-12 — 근골격계 학교 화면: KPI·연도별 그룹 → "조사 현황" 통합 표

**요청**: 근골격계 학교 컨텍스트(조사 목록)도 안전점검 학교 컨텍스트의 점검 현황([055])처럼 — 조사일 · 학교 · 담당자 · 평가 구분 · 작성현황 · 작업 컬럼의 통합 표로. [보고서 작성] 버튼 유지.

**변경** (`src/pages/Musculo.tsx`):

- KPI 카드 3개(조사 수·부담작업 있음·검수 대기)와 연도별 접기식 그룹(조사 ID·부담작업·기본조사·증상조사표·검수대기 컬럼) 제거 → **"조사 현황" 단일 표** (조사일 최신순, 기존 flatQ 훅 그대로).
- 컬럼 구성: 조사일 · 학교 · 담당자 · 평가 구분(정기/수시) · 작성현황(검수 대기 N건 주황 > 작성 완료 초록 > 작성중 보라 — [079]와 동일 판정, 세부 수치는 툴팁) · 작업(**증상조사표 있으면 [검수/조사표 보기]**(기존 검수 모달), 없으면 [상세]). 행 클릭 = 조사 상세(통계) — 기존 동작 유지.
- 헤더의 [보고서 작성]·[근골격계 조사 생성] 버튼, 검색·내보내기, 부담작업 판정 탭, 조사 상세(TOTAL~NORMAL 통계)는 변경 없음. 구 연도별 그룹 코드(yearGroups·renderGroup·selNeeds·selBurden)는 잔존(미사용, 롤백 대비 — [024] 관례).

**롤백 방법**: `[082]` 주석 블록을 구 KPI+연도별 그룹 JSX로 원복(코드 잔존).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 한빛초 진입 시 통합 표 2행(검수 대기 2건 주황·작성중), [검수] 클릭 → 검수 모달.

**영향 범위**: 근골격계(/musculo) 학교 컨텍스트 조사 목록 화면만.

---

## [083] 2026-08-12 — 근골격계 상단 탭·[조사 생성] 버튼 제거

**요청**: 상단 우측의 [조사 목록 N]·[부담작업 판정] 탭과 [+ 근골격계 조사 생성] 버튼 삭제.

**변경** (`src/pages/Musculo.tsx`): bar 영역의 탭 2개·생성 버튼 JSX 삭제 — 학교 화면은 [082] 조사 현황 표 단일 흐름. tab state는 'list' 고정으로 동작(부담작업 판정 화면은 미노출 — 판정 기능 자체는 보고서 작성 플로우의 부담작업 선택으로 대체 이용 가능), 조사 생성은 [보고서 작성] 플로우에서 수행. createSurvey·tab state·부담작업 판정 렌더 블록은 잔존(미노출, 롤백 대비).

**유의**: 화면에서 '부담작업 판정' 단독 화면과 '빈 조사 생성' 진입점이 사라짐 — 다시 필요하면 `[083]` 주석 위치에 구 JSX 복원.

**롤백 방법**: `[083]` 주석 자리에 탭·버튼 JSX 복원.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계(/musculo) 상단 바만.

---

## [084] 2026-08-12 — 근골격계 [보고서 작성] 버튼을 안전점검 [점검표 작성]과 동일한 위치·색으로

**요청**: 학교 헤더 우측의 회색(ghost) [보고서 작성] 링크를 안전점검의 [점검표 작성]처럼 — 상단 바(페이지 제목 줄) 우측·보라(btn-primary)로.

**변경** (`src/pages/Musculo.tsx`): 상단 바 우측에 학교 선택 시 보라 [보고서 작성] 버튼 표시(→ /musculo/report?school=), 학교 헤더의 구 ghost 링크 제거. 미사용된 FileText·Plus import 정리.

**롤백 방법**: `[084]` 주석 2곳 기준 원복.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계(/musculo) 버튼 위치·스타일만.

---

## [085] 2026-08-12 — 근골격계 조사 통계 카드(TOTAL~NORMAL)·하단 안내 문구 제거

**요청**: 조사 행 클릭 시 하단에 나오던 통계 카드 7개(TOTAL·DONE·REVIEW·TODO·PAIN·MANAGED·NORMAL)와 "증상조사표 OMR 인식/검수는 현장 태블릿·후속…" 안내 문구 전부 삭제.

**변경** (`src/pages/Musculo.tsx`): 해당 렌더 블록(오류/로딩/통계 카드/안내 문구) 삭제. 통계 로드 로직(surveyId·stats state·GET /musculo/{id}/stats)과 numericEntries·STAT_LABEL은 잔존(미노출, 롤백 대비).

**유의**: 행 클릭 시 화면 변화가 없어짐(통계가 유일한 상세 표시였음) — 조사 관련 작업은 작업 컬럼의 [검수/조사표 보기]와 [보고서 작성]으로 수행. 행 클릭에 다른 동작(예: 보고서 작성 이동)을 연결하려면 별도 요청.

**롤백 방법**: `[085]` 주석 자리에 구 렌더 블록 복원.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계(/musculo) 학교 컨텍스트 하단 영역만.

---

## [086] 2026-08-12 — 업무 바로가기 배지: 점 제거 · 완료만 연초록 칩 · 연도 기준 판정 · 교육 중립화

**요청·전략**: 배지의 회색 점이 대부분 미완료인 목록에서 노이즈만 된다는 논의 → **점 전부 제거, 완료 업무만 칩 배경을 연초록으로** 구분. 추가로 ⑴ 위험성평가·근골격계는 연 1회 법정업무이므로 완료 판정을 **당해 연도 작성분 기준**으로 보완(기존은 누적 기준이라 해가 바뀌어도 완료로 남는 문제) ⑵ 교육 칩은 상태 표시 없는 순수 바로가기로.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/SchoolsHub.tsx` | deriveBadges: 위험성평가·근골격계를 created_at 연도 = 당해 연도 건들로 필터해 판정("2026년 미작성/진행 N건/완료" — 1월 1일 자동 리셋). StatusLite·MusLite에 created_at 추가. 칩 렌더: `<i/>` 점 삭제, 완료(cls 'ok')만 `done` 클래스, 교육 칩은 배지 없이 렌더(툴팁 "교육 진도표 바로가기"). 도넛 집계(workStats)는 badges cls를 읽으므로 연도 기준이 자동 반영됨 |
| `src/styles/schoolhub.css` | `.shub-w.done` — 연초록 배경·초록 테두리·초록 글자. 기존 `i` 점 규칙은 배너 칩(CycleBanner)이 계속 사용하므로 유지 |

**유의**: 교육 칩에서 인원 일치/불일치 표시가 사라짐(대장·학교 상세에서 확인 가능). 위험성평가 판정은 평가 건(created_at) 기준 — 정기 보고서 문서 작성 여부와는 별개(보고서 기준 판정 전환은 학교별 추가 조회가 필요해 후속 검토).

**롤백 방법**: git 커밋 revert, 또는 `[086]` 주석 2곳 원복 + done 클래스 CSS 삭제.

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 완료 학교만 연초록 칩, 나머지·교육은 무늬 없는 칩, 툴팁에 "2026년 …" 표기.

**영향 범위**: 학교 허브(/schools) 업무 바로가기 배지·도넛 완료율 판정 기준. 백엔드 계약 변경 없음.

---

## [087] 2026-08-12 — 근골격계 "작성된 조사" 행 클릭 → 보고서 작성 화면 직행

**요청·경위**: [081]에서 행 클릭을 조사 상세(학교 컨텍스트)로 연결했으나, [085]에서 통계 카드를 제거해 결과적으로 학교 목록 화면만 보이는 상태가 됨 → 안전점검 [080]과 동일하게 **행 클릭 시 작성 파일(보고서 작성 화면)이 바로 열리도록** 변경.

**변경** (`src/pages/Musculo.tsx`): 첫 화면 리스트의 행 클릭·[보기] 버튼 모두 `/musculo/report?school={학교id}` 직행 — 보고서 화면이 localStorage 초안을 자동 복원하므로 작성중 건은 이어서 작성됨. 학교 조사 현황(검수 포함)은 학교 탭의 [근골격계] 바로가기로 진입.

**롤백 방법**: `[087]` 주석 지점 onClick 2곳을 [081] 형태(openSchool+setSurveyId)로 원복.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계(/musculo) 첫 화면 행 클릭 동선만.

---

## [088] 2026-08-12 — 위험성평가 보고서 작성 탭을 근골격계 목차 스타일(숫자 칩)로 통일

**요청**: 위험성평가 보고서 작성의 탭 바("① 학교 현황" 세그먼트형)를 근골격계 보고서 목차("(1) 부담작업 체크리스트" 알약형 숫자 칩)와 같은 숫자·구성으로.

**변경** (`src/pages/Risk.tsx`): SURVEY_TABS를 `{num, label}` 구조로 변경(원문자 ①~⑤ 제거), 탭 버튼을 근골격계 mur-toc와 동일한 **알약형 칩**(흰 배경·테두리·둥근 모서리 + 20px 보라 원 숫자)으로 재스타일. 활성 탭은 보라 테두리·보라 글자·숫자 원 보라 채움(흰 숫자)으로 구분. 탭 전환·저장 동작 변경 없음.

**롤백 방법**: `[088]` 주석 지점의 SURVEY_TABS·SUBTAB_BAR·subTabStyle·subTabNumStyle을 구 형태로 원복.

**검증**: esbuild 문법 통과 · 렌더링 확인 요망(숫자 원 5개 칩·활성 강조).

**영향 범위**: 위험성평가(/risk) 보고서 작성 화면 탭 바 스타일만.

---

## [089] 2026-08-12 — 근골격계 보고서를 탭(스텝) 방식으로 재구성

**요청·전략**: 근골격계 보고서 화면(1~7)이 전 섹션을 한 페이지에 나열하고 숫자 칩은 스크롤 앵커였음 — 위험성평가([088] 이후)와 칩 모양은 같은데 동작이 달라 혼란. 전략 논의 후 **위험성평가와 동일한 탭 방식(선택 섹션만 표시 + 하단 [다음 →])** 채택.

**변경 파일**:

| 파일 | 변경 |
|---|---|
| `src/pages/MusculoReport.tsx` | `step` state(1~7) 신설. 목차 앵커(`<a href="#mur-sN">`) → **단계 탭 버튼**(클릭 시 해당 섹션만 표시·상단 스크롤). 7개 섹션(mur-s1~s7)에 `display: none` 전환 적용 — **언마운트가 아닌 display 전환이라 입력값·사진·명단 등 모든 상태 유지**(자동 저장 초안 포함). 섹션 7 아래 [다음 →] 버튼(1~6단계에서 표시, [017]과 동일 위치·스타일) |
| `src/styles/musculoreport.css` | `.mur-toc` 규칙에 button 셀렉터 추가 + 활성 탭(`.on`) 강조(보라 테두리·숫자 원 보라 채움 — [088]과 동일) |

**롤백 방법**: `[089]` 주석 지점 원복 — step state·goStep 삭제, 탭 버튼을 구 앵커로, 섹션 style 제거, 다음 버튼 삭제, css 활성 규칙 삭제.

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 1단계만 표시·탭 전환·[다음 →] 순차 이동·입력값 유지.

**영향 범위**: 근골격계 보고서 작성(/musculo/report) 화면 표시 방식만. 데이터 저장·제출 로직 변경 없음.

---

## [090] 2026-08-12 — 근골격계 [다음 →] 버튼을 섹션 카드 안쪽으로 이동

**요청·경위**: [089]의 [다음 →]이 섹션 카드(흰 박스) 바깥 페이지 레벨에 있어 회색 배경 위에 떠 보임 — 위험성평가는 카드 내부 하단에 있음. 동일하게 통일.

**변경** (`src/pages/MusculoReport.tsx`): 1~6단계 각 섹션 카드(mur-sec) 내부 하단에 [다음 →] 배치(우측 정렬, 다음 단계로 이동), [089]의 카드 밖 단일 버튼 제거. 7단계(제출)는 다음 없음.

**롤백 방법**: `[090]` 주석 6곳 삭제 + [089] 형태의 외부 버튼 복원.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계 보고서 작성 화면 버튼 위치만.

---

## [091] 2026-08-12 — 증상 조사표 명단 표 정렬 깨짐 수정 (전역 .row 유틸 충돌)

**증상**: 근골격계 보고서 4단계(증상 조사표)의 종사자 명단 표에서 헤더는 전체 폭에 퍼지는데 본문 행들은 왼쪽에 압축되어 컬럼이 전혀 맞지 않음.

**원인**: `components.css`의 범용 가로 정렬 유틸리티 `.row { display: flex }`가 명단 표의 행(`<tr class="row">` — 프로토타입 포팅 때부터 사용)에 적용되어, tr이 표 레이아웃(table-row)에서 이탈해 flex 컨테이너가 됨 → 셀들이 내용 크기로 쪼그라들어 왼쪽으로 몰림. (전수 확인 결과 `tr.row`를 쓰는 곳은 MusculoReport 한 곳뿐)

**변경** (`src/styles/musculoreport.css`): `.mur-roster tr.row`에 `display: table-row` 명시 — 더 구체적인 선택자라 전역 유틸을 덮음.

**검증**: Chromium 재현 테스트 — 전역 `.row(flex)` 규칙을 포함한 환경에서 수정 적용 후 thead th·tbody td의 x좌표가 9개 컬럼 모두 일치함을 좌표 측정으로 확인.

**롤백 방법**: 추가한 `display: table-row;` 한 줄 삭제 (원래 증상으로 복귀).

**영향 범위**: 근골격계 보고서 4단계 명단 표 렌더링만.

---

## [092] 2026-08-12 — 증상 조사표(4단계) 통계 카드·상단 버튼 2종 제거

**요청**: 4단계 상단의 통계 카드 5종(작성 완료·직접 입력/종이·OMR 인식 검수·관리대상자·통증호소자)과 [✉ 조사표 링크 일괄 발송]·[종이 스캔 업로드 (OMR)] 버튼 삭제.

**변경** (`src/pages/MusculoReport.tsx`): mur-svbar(카드 5종)와 발송·일괄 업로드 버튼(+숨김 파일 input) 삭제. **유지**: "전 종사자 N명 · 미제출 N명" 카운트, [안전보건공단 엑셀 내보내기] 버튼, 개인별 행의 [조사표 작성]·개인별 종이 스캔 업로드. omrAllRef·omrUploadAll 등 로직은 잔존(미노출).

**롤백 방법**: `[092]` 주석 자리에 구 JSX 복원.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계 보고서 4단계 상단 영역만.

---

## [093] 2026-08-12 — 작업조건 조사(2단계) 범례 표 좌측 쏠림 해소

**요청**: 작업 부하(A)·작업 빈도(B) 범례 표 2개가 내용 크기만큼만 차지해 카드 왼쪽에 몰려 보임.

**변경** (`src/styles/musculoreport.css`): `.mur-scale table`에 `flex: 1 1 0; min-width: 320px` — 두 표가 카드 폭을 절반씩 나눠 사용(1920px 기준 균등 배치, 좁은 화면에선 자동 줄바꿈). 점수 컬럼(td.sc) 폭 56px 고정.

**검증**: Chromium 재현 — 1600px 컨테이너에서 두 표가 750px씩 균등 분할됨을 측정 확인.

**롤백 방법**: `[093]` 주석 블록(flex·min-width·td.sc width) 삭제.

**영향 범위**: 근골격계 보고서 2단계 범례 표 폭만.

---

## [094] 2026-08-12 — 유해위험요인 사진(3단계): 사진별 설명 문구 + 기타 요인 직접 입력

**요청**: ⑴ 검수~청소·세척 각 작업에서 사진과 문구를 세트로 계속 추가할 수 있게 ⑵ 유해위험요인(복수 선택)에 '기타'처럼 직접 글을 써서 추가할 수 있게.

**변경**:

- `src/pages/MusculoReport.tsx` — ⑴ Shot 타입에 `cap`(사진별 설명) 추가, [사진 추가]로 사진을 올릴 때마다 썸네일 아래 **"사진 설명" 입력칸이 세트로 생김**(장수 제한 없음, 기존 작업 단위 설명 textarea는 유지). ⑵ 유해위험요인 칩 목록 아래 **"기타 요인 직접 입력" 입력칸 + [+ 추가]**(Enter 지원) — 입력한 요인이 해당 작업의 칩으로 추가되고 선택 상태가 됨, 칩 클릭으로 해제 가능. 사진 설명·기타 요인 모두 임시저장(localStorage 초안)에 자동 포함.
- `src/styles/musculoreport.css` — mur-shotwrap(사진+설명 세트)·mur-shotcap(설명 입력)·mur-hazadd(기타 입력줄) 스타일.

**롤백 방법**: `[094]` 주석 지점 원복 (Shot.cap·setShotCap·hzInput·addCustomHz·JSX 2곳·CSS 블록).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 사진 추가 시 설명칸 생성, 기타 요인 입력 후 Enter/추가 → 선택된 칩 생성, 임시저장 후 새로고침 시 복원.

**영향 범위**: 근골격계 보고서 3단계만. 서버 전송 계약 변경 없음(사진은 기존대로 파일명 메타만 전송, 설명·기타 요인은 초안 보존).

---

## [095] 2026-08-12 — 유해위험요인 사진: (사진 추가 + 작업 설명) 세트 추가 버튼

**요청·경위**: [094] 후에도 "사진 추가 + 작업 설명" 묶음 자체를 늘리는 버튼이 없다는 지적 — 작업당 세트가 1개 고정이었음.

**변경** (`src/pages/MusculoReport.tsx`):

- 각 작업(검수~청소·세척 등) 아래 **[+ 사진·설명 세트 추가]** 버튼 — 누를 때마다 (사진 추가 박스 + 작업 설명칸) 묶음이 하나 더 생김(제한 없음). 세트가 2개 이상이면 **[✕ 마지막 세트 삭제]** 버튼 표시.
- 구현: 작업 키에 `#번호`를 붙인 보조 키(`급식-검수#1`…)로 기존 shots·caps 구조를 그대로 재사용 — 첫 세트는 기존 키라 **이전에 저장한 초안과 호환**. 세트 수(cutN)는 초안(Draft)에 포함되어 임시저장·복원됨. 사진별 설명(cap)도 초안 메타에 보존되도록 buildDraft 보강.

**롤백 방법**: `[095]` 주석 지점 원복 (cutN state·Draft 필드·세트 루프 JSX → 단일 블록).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 세트 추가/삭제, 각 세트 독립 사진·설명, 임시저장 후 복원.

**영향 범위**: 근골격계 보고서 3단계만.

---

## [096] 2026-08-12 — 사진·설명 세트에 유해위험요인 패널 포함 ([095] 보완)

**요청**: [+ 사진·설명 세트 추가]로 세트가 생길 때 옆의 유해위험요인(복수 선택) 부분도 세트마다 함께 생겨야 함.

**변경**:

- `src/pages/MusculoReport.tsx` — 유해위험요인 패널(칩 + 기타 직접 입력)을 작업 단위에서 **세트 단위**로 이동. 세트 하나 = (사진 추가 + 사진별 설명 + 작업 설명) | (유해위험요인 칩 + 기타 입력) 2컬럼 묶음. 선택값은 세트 키(hz[`작업#n`])별로 독립 저장 — 첫 세트는 기존 키라 이전 초안 호환. [마지막 세트 삭제] 시 해당 세트의 유해위험요인 선택도 함께 정리.
- `src/styles/musculoreport.css` — riskrow를 130px+1fr 2컬럼으로, 세트(.mur-cutset)는 내부 1.3fr|1fr 2컬럼 + 세트 사이 점선 구분.

**롤백 방법**: `[096]` 주석 지점 원복 ([095] 형태 — 유해위험요인을 작업 단위 우측 컬럼으로).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 세트 추가 시 유해위험요인 패널 동반 생성·세트별 독립 선택.

**영향 범위**: 근골격계 보고서 3단계만.

---

## [097] 2026-08-12 — 증상 조사표 명단: 성명·부서를 학교 대장에서 자동 매핑

**요청**: 4단계 명단의 성명·부서가 "영양(교)사 1" 같은 자리표시자가 아니라 학교 이력관리 대장의 종사자에서 자동 연동되어야 함. 나머지(연령·성별·응답)는 [조사표 작성]으로 기입.

**경위**: 기존 대장(GET /schools/{id}/ledger workers)은 파트별 **인원수만** 보유 → 개인 실명이 없어 자리표시자로 전개하고 있었음([013] 주석에 명시).

**변경**:

- `mock-server.mjs` — 대장 종사자에 개인 명단 `names: string[]` 필드 추가(파트별 인원수만큼, 학교별 결정적 생성). **적용 후 목서버 재시작 필요**
- `src/pages/MusculoReport.tsx` — Worker 타입에 `names?` 추가(방어적 — 필드 없으면 기존 자리표시자 유지). rosterFromWorkers가 실명 우선 매핑. mergeRoster 보강: 자리표시자로 저장된 기존 초안 진행분(작성 완료 등)을 같은 부서·직무의 미작성 행에 순서대로 얹어 실명을 유지한 채 이어짐.

**백엔드 전달 사항**: 대장 종사자 엔티티에 개인 명단(성명 배열 또는 개별 종사자 행) 필요 — 증상조사표 성명·부서 자동 매핑의 전제. 대장 화면(SchoolDetail)에서 명단을 편집하는 UI는 후속 과제.

**검증**: esbuild 문법 통과 · 목서버 기동 후 GET /schools/s01/ledger 응답에 names 확인(급식 3명 강다혜·윤보라·서지훈 등) · 렌더링 확인 요망 — 4단계 명단 성명이 실명으로 표시.

**롤백 방법**: `[097]` 주석 지점 원복 (mock names 생성·Worker.names·rosterFromWorkers·mergeRoster).

**영향 범위**: 근골격계 보고서 4단계 명단 생성만. 실백엔드에 names 없으면 기존 동작 그대로(무해).

---

## [098] 2026-08-12 — 종사자 추가: 입력칸 나열 → [+ 추가] 버튼 + 조사표 모달에서 기입

**요청**: 명단 하단의 종사자 추가(성명·연령·성별·부서 입력칸 나열 + 추가)를 좌측 [+ 추가] 버튼 하나로 바꾸고, 누르면 조사표 작성 화면이 떠서 그 안에서 사람이 추가되도록.

**변경** (`src/pages/MusculoReport.tsx`):

- 표 아래 좌측에 **[+ 추가]** 버튼만 배치 — 클릭 시 빈 종사자 행 생성 + **증상조사표 모달 즉시 오픈**.
- 모달 Ⅰ부 기본 정보의 **성명·연령·성별·작업부서 입력을 명단 행과 실시간 동기화**(patchPerson — 기존에는 defaultValue 장식 입력이라 저장 안 됐음). 대장 매핑으로 이미 이름이 있는 사람을 열어도 동일하게 수정 가능.
- **성명을 비운 채 닫으면(닫기·임시저장) 신규 추가 취소로 보고 행 자동 제거** — 빈 행 잔존 방지.
- 구 입력칸 나열 UI·addPerson은 잔존(미노출).

**롤백 방법**: `[098]` 주석 지점 원복 (버튼→구 입력칸 행, Ⅰ부 입력 defaultValue 복귀, closeForm filter 제거).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — [+ 추가] → 모달에서 성명 기입 시 명단 실시간 반영, 성명 없이 닫으면 행 제거.

**영향 범위**: 근골격계 보고서 4단계 종사자 추가 동선·조사표 Ⅰ부 기본 정보 저장만.

---

## [099] 2026-08-12 — 증상 조사표: 진입점을 행 버튼 하나로 통일, 스캔 업로드는 모달 안으로

**요청·전략**: 행 펼침의 버튼 3개 정리 — [조사표 작성](행 오른쪽 버튼과 중복)·[조사표 링크 발송](API 미연결 파사드) 제거, [종이 스캔 업로드]는 조사표 모달 안으로. 동선 = "행 오른쪽 [조사표 작성/열기] → 모달에서 직접 입력 or 스캔 업로드".

**변경** (`src/pages/MusculoReport.tsx`):

- 행 펼침 상세: 미작성자에게 보이던 버튼 3개 → 안내 문구 한 줄("오른쪽 [조사표 작성]으로…")로 교체. 펼침은 응답 요약(판정 그리드) 전용.
- 조사표 모달 상단에 **[종이 스캔 업로드 (OMR)]** 버튼 추가(검수 대기 상태가 아닐 때 표시) — 업로드 시 해당 종사자가 OMR 인식 검수 대기로 전환되고 모달에 기존 검수 안내가 이어짐. 개인별 링크 발송 기능은 화면에서 완전히 제거(추후 실기능 개발 시 모달에 배치).

**롤백 방법**: `[099]` 주석 2곳 원복.

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 펼침에 버튼 없음, 모달 상단 스캔 업로드 → 검수 대기 전환.

**영향 범위**: 근골격계 보고서 4단계 행 펼침·조사표 모달만.

---

## [100] 2026-08-12 — 5단계를 "공단 엑셀 미리보기"로 개편 (B안)

**요청·전략**: 5단계(공단 엑셀 열 매핑)가 정적 설명서라 "또 작성해야 하나" 혼란 유발 → **B안** 채택: 5단계 = 4단계 응답으로 자동 생성되는 기본사항·판정 결과 미리보기(읽기 전용, 별도 작성 없음). 공단 엑셀 파일을 앱에 넣을 필요 없음 — 판정은 공단 엑셀 수식과 동일 기준의 앱 계산이며, 내보낸 값을 엑셀 '데이터' 시트에 붙여넣으면 엑셀 수식이 같은 결과를 재계산.

**변경** (`src/pages/MusculoReport.tsx`):

- 5단계: 제목 "공단 엑셀 미리보기 — 기본사항 · 판정 결과". 표 컬럼 = # · 성명 · 연령 · 성별(남1/여2) · 부서·작업 · 부위별 결과 6종(목~다리/발) · 전체 결과 — 공단 엑셀 결과부와 동일 배치, 미작성자는 '—'. **[안전보건공단 엑셀 내보내기] 버튼을 4단계에서 5단계 상단으로 이동.** 기존 열 매핑 안내표·판정 기준 설명은 하단 접이식(details)으로 유지. 단계 탭 라벨 '공단 엑셀 열 매핑' → '공단 엑셀 미리보기'.

**후속 후보**: 공단 엑셀 양식(.xlsx)을 내장해 값이 채워진 완성 엑셀을 바로 다운로드 — 양식 원본 파일 수령 후 서식 보존 검증 필요.

**롤백 방법**: `[100]` 주석 지점 원복 (미리보기 표 제거, 내보내기 버튼 4단계 복귀, details 해제, 탭 라벨 원복).

**검증**: esbuild 문법 통과 · 렌더링 확인 요망 — 4단계 작성 완료자의 판정이 5단계 표에 자동 표시, 내보내기 CSV 정상.

**영향 범위**: 근골격계 보고서 4·5단계 화면만. 데이터·내보내기 로직 변경 없음.

---

## [101] 2026-08-12 — 근골격계 7단계(제출) 삭제

**요청**: 단계 탭의 "7 제출" 제거.

**변경** (`src/pages/MusculoReport.tsx`): 7단계 섹션(백엔드 제출) 삭제 — 제출은 상단 헤더의 [보고서 제출] 버튼(기존부터 있던 동일 기능)으로 일원화. 단계 탭 6개(…6 작업환경 개선계획서)로 축소, 6단계의 [다음 →] 제거(마지막 단계). 제출 진행 로그(✓/✕)는 보고서 헤더 아래로 이동해 제출 시 그대로 표시. 6단계 하단 안내에 "작성이 끝나면 상단 [보고서 제출]" 문구 추가. submitReport·API 파이프라인 변경 없음.

**롤백 방법**: `[101]` 주석 지점 원복 (7단계 섹션·탭 라벨·다음 버튼·sublog 위치).

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계 보고서 단계 구성만. 제출 API 흐름 변경 없음.

---

## [102] 2026-08-12 — 근골격계 조사 주기 문구 정정: 3년 1회 → 1년 1회

**요청**: 보고서 헤더의 조사 주기가 "3년 1회"로 잘못 표기됨(프로토타입 문구 잔존) — 실제 운영 주기 1년 1회로 정정.

**변경** (`src/pages/MusculoReport.tsx`): 헤더 메타 "조사 주기" 값 **1년 1회** · 8월 착수 → 10월 제출. 다른 화면에는 3년 1회 표기 없음(전수 확인).

**롤백 방법**: 문구 원복.

**검증**: esbuild 문법 통과.

**영향 범위**: 근골격계 보고서 헤더 문구만.

---

## [103] 2026-08-13 — 오늘의 할 일: 방문 완료 항목 리스트에서 제외

**요청**: 방문 완료된 학교가 오늘의 할 일 리스트에 계속 남아 있음 — 완료되면 사라져야 함.

**변경** (`src/components/TodayHero.tsx`): 리스트는 미완료(pending) 항목만 표시(번호도 미완료 기준으로 재부여). 헤더의 "방문 예정 N곳 · 완료 N곳"과 우측 "N/M곳 오늘 방문" 집계는 완료 포함 그대로 유지. 전부 완료 시 "오늘 방문을 모두 완료했습니다" 안내 표시.

**롤백 방법**: `[103]` 주석 지점 원복 (items.map 복귀, 완료 안내 삭제, 번호 오프셋 복원).

**검증**: esbuild 문법 통과.

**영향 범위**: 홈 오늘의 할 일 리스트 표시만. 방문 완료 판정·집계 로직 변경 없음.

---

## [104] 2026-08-13 — 인쇄/제출물 결재란을 학교 대장 결재선과 연동

**요청**: 작성 화면은 담당자 서명만 받지만, 보고서 제출·인쇄물에는 이력관리 대장의 결재선(GET /schools/{id}/approval-line — 학교 상세에서 편집)에 따라 결재 칸이 늘어나서 나와야 함.

**변경 파일**:

| 화면 | 변경 |
|---|---|
| `components/InspectionSheetView.tsx` | SheetData에 `approval` 추가 — 결재란(담당자 1칸 고정)을 **결재선 단계 수만큼 칸 생성**. 담당(자) 칸에는 서명자 표기, 나머지는 수기 결재용 공란. 양식 보기·메일 첨부 PDF([062] 캡처) 공용이라 둘 다 반영 |
| `pages/Inspection.tsx` | openSheet에서 결재선 조회 후 양식에 전달 |
| `pages/InspectionForm.tsx` | buildSheet(메일 PDF)에 이미 로드된 결재선(approval) 전달 |
| `features/risk/RiskReport.tsx` | sid prop 추가, 표지 결재란(담당/행정실장/교장 고정 3칸) → **결재선 기준 동적 칸** (미조회 시 기본 3칸 유지) |
| `pages/Risk.tsx` | RiskReport에 sid 전달 |
| `features/compliance/ComplianceSheet.tsx` | 인쇄 조사지 우측 상단에 **결재란 신설**(결재선 기준, 기존에는 없었음) — sid prop·조회 로직 추가 |
| `pages/Compliance.tsx` | ComplianceSheetPrint에 sid 전달 |

**유의**: 결재선이 없는 학교는 목서버 기본값(담당자·행정실장·교장)이 반환됨. **근골격계는 인쇄용 실물 양식 화면이 아직 없어 미적용** — 양식 재현 작업 시 동일 규칙 적용 예정. 결재선 편집은 학교 상세(대장)의 결재선 관리에서.

**롤백 방법**: git 커밋 revert. 개별: 각 파일 `[104]` 주석 지점 원복.

**검증**: esbuild 문법 통과(7개 파일) · 렌더링 확인 요망 — 학교 상세에서 결재선 단계를 추가/변경 후 ⑴ 안전점검 [보기]·메일 PDF ⑵ 위험성평가 [보고서 출력] 표지 ⑶ 이행점검 [인쇄]에서 칸 수·직함 반영 확인.

**영향 범위**: 인쇄/제출 출력물의 결재란만. 작성 화면 서명 UX·백엔드 계약 변경 없음(기존 approval-line API 사용).

---

## 부록: 신규 추가 파일 (프로젝트 소스 아님 — 개발 보조 도구)

| 날짜 | 파일 | 용도 |
|---|---|---|
| 2026-07-24 | `mock-server.mjs` | 가짜 백엔드(포트 3001). 백엔드 없이 화면 작업용 샘플 데이터 제공. 납품 시 제외 가능 |
| 2026-07-24 | `start.bat` | 목서버 + vite 동시 실행 스크립트. 납품 시 제외 가능 |
| 2026-07-24 | `수정이력.md` | 본 문서 |
| 2026-08-07 | `off.bat` | 개발 서버 일괄 종료 스크립트 — 창 제목·포트(3001/5173) 기준으로 남은 node 프로세스 정리. 납품 시 제외 가능 |
| 2026-08-09 | `mock-schools.mjs` | 한국산업안전협회 계약 학교 771건 실데이터 시드(엑셀 변환). 납품 시 제외 가능 |

> 기존 소스 파일 중 실제로 수정된 것은 현재 `src/styles/home.css` 1건뿐입니다.
> 백엔드 계약 파일(`src/lib/api.ts`, `src/lib/auth.tsx`, `vite.config.ts` proxy)은 수정하지 않았습니다.
